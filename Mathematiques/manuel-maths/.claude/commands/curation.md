Lance la curation du chapitre $ARGUMENTS (LOT 2).
1. Lis `chapitres/$ARGUMENTS/contrat.yaml` (vérifie statut `valide`, sinon arrête-toi : LOT 0 non validé).
2. Adopte le prompt `prompts/curateur.md`.
3. Pour chaque capacité, utilise l'outil MCP `search_corpus` selon le processus du prompt.
4. Écris `chapitres/$ARGUMENTS/dossier_curation.json` puis `LOT-2_rapport.md`.
5. Arrête-toi : validation humaine requise avant LOT 3.
