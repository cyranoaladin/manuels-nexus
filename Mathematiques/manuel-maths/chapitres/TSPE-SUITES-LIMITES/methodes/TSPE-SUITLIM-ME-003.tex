% META: {"id": "TSPE-SUITLIM-ME-003", "chapitre": "TSPE-SUITES-LIMITES", "type_objet": "methode", "capacites_codes": ["C3"], "methodes": ["M3"], "sources_inspiration": [], "status": "approved"}
\begin{fichemethode}{M3}{Étudier une suite arithmetico-géométrique}

\quandUtiliser{%
  Utiliser cette méthode lorsque la suite est définie par $u_{n+1} = a\,u_n + b$ avec $a
\neq 1$ et $b
\neq 0$, ou lorsqu'un probleme de modélisation conduit a une telle relation.
}

\pasApas{%
  \begin{enumerate}
    \item \textbf{Déterminer le point fixe} $\ell$ en resolvant $\ell = a\,\ell + b$, soit $\ell = \dfrac{b}{1-a}$.
    \item \textbf{Poser la suite auxiliaire} $v_n = u_n - \ell$.
    \item \textbf{Montrer que $(v_n)$ est géométrique} : vérifier que $v_{n+1} = a \cdot v_n$.
    \item \textbf{Exprimer $v_n$} : $v_n = v_0 \cdot a^n = (u_0 - \ell) \cdot a^n$.
    \item \textbf{Revenir a $u_n$} : $u_n = \ell + (u_0 - \ell) \cdot a^n$.
    \item \textbf{Conclure sur la limite} : si $|a| < 1$, alors $a^n \to 0$ et $u_n \to \ell$.
  \end{enumerate}
}

\exempleRedige{%
  \textbf{Exemple.} Soit $(u_n)$ définie par $u_0 = 10$ et $u_{n+1} = 0{,}7\,u_n + 6$.

  \medskip
  \commentaireMarge{Étape 1 : point fixe.}%
  On resout $\ell = 0{,}7\,\ell + 6$ : $0{,}3\,\ell = 6$, d'ou $\ell = 20$.

  \commentaireMarge{Étape 2 : suite auxiliaire.}%
  On pose $v_n = u_n - 20$.

  \commentaireMarge{Étape 3 : vérifier que $(v_n)$ est géométrique.}%
  $v_{n+1} = u_{n+1} - 20 = 0{,}7\,u_n + 6 - 20 = 0{,}7\,u_n - 14 = 0{,}7(u_n - 20) = 0{,}7\,v_n$.

  $(v_n)$ est géométrique de raison $0{,}7$ et de premier terme $v_0 = 10 - 20 = -10$.

  \commentaireMarge{Étapes 4-5 : expression de $u_n$.}%
  $v_n = -10 \times 0{,}7^n$, donc $u_n = 20 - 10 \times 0{,}7^n$.

  \commentaireMarge{Étape 6 : limite.}%
  $|0{,}7| < 1$, donc $0{,}7^n \to 0$ et $\lim u_n = 20$.
}

\pieges{%
  \begin{itemize}
    \item \textbf{Piège 1.} Se tromper de signe dans $v_0 = u_0 - \ell$. Bien calculer $u_0 - \ell$ et non $\ell - u_0$.
    \item \textbf{Piège 2.} Appliquer la méthode avec $a = 1$ : dans ce cas la suite est arithmétique, pas arithmetico-géométrique.
  \end{itemize}
}

\verifier{%
  Vérifier que $u_0 = \ell + (u_0 - \ell) \cdot a^0 = \ell + u_0 - \ell = u_0$. Vérifier aussi $u_1$ par calcul direct et par la formule.
}

\sEntrainer{\refExos{M3}}

\end{fichemethode}
