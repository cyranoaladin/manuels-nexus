% META: {"id": "TSPE-SUITLIM-CR-013", "chapitre": "TSPE-SUITES-LIMITES", "type_objet": "cours", "capacites_codes": ["C4"], "sources_inspiration": [], "status": "approved"}

\section{Suite croissante non majorée}

% ============================================================
% STRATE 1 — Théorème et démonstration exigible
% ============================================================

\theoreme{
Toute suite croissante non majorée diverge vers $+\infty$.
}

\demonstration{
Soit $(u_n)$ une suite croissante non majorée. Montrons que $\lim_{n \to +\infty} u_n = +\infty$.

Soit $A$ un réel quelconque. Montrons qu'il existe un rang $N$ tel que pour tout $n \geq N$, $u_n \geq A$.

Puisque $(u_n)$ n'est pas majorée, le réel $A$ n'est pas un majorant de $(u_n)$. Il existe donc un entier $N$ tel que $u_N > A$.

Or la suite $(u_n)$ est croissante : pour tout $n \geq N$, on a $u_n \geq u_N > A$, soit $u_n \geq A$.

On a montre que pour tout réel $A$, il existe $N \in \mathbb{N}$ tel que pour tout $n \geq N$, $u_n \geq A$.

Par définition, $\lim_{n \to +\infty} u_n = +\infty$.
}

\margeAppui{
Ce théorème admet un énoncé analogue : toute suite \emph{décroissante} non minorée diverge vers $-\infty$.
}

\subsection{Théorème de la suite monotone bornee}

\theoreme{
Toute suite croissante et majorée est convergente.

Toute suite décroissante et minorée est convergente.
}

\margeAppui{
Ce théorème (admis) repose sur la propriété de la borne superieure des réels. Il ne donne pas la valeur de la limite, mais garantit son existence.
}

\exemple{
Soit $(u_n)$ définie par $u_0 = 0$ et $u_{n+1} = \sqrt{2 + u_n}$.

\textbf{Montrons par récurrence que $0 \leq u_n \leq 2$ pour tout $n$.}

Initialisation : $u_0 = 0 \in [0, 2]$.

Heredite : supposons $0 \leq u_n \leq 2$. Alors $2 \leq 2 + u_n \leq 4$, donc $\sqrt{2} \leq u_{n+1} = \sqrt{2 + u_n} \leq 2$. En particulier $0 \leq u_{n+1} \leq 2$.

\textbf{Montrons que $(u_n)$ est croissante.}

On a $u_{n+1} - u_n = \sqrt{2 + u_n} - u_n$. Posons $g(x) = \sqrt{2 + x} - x$ sur $[0, 2]$.

On a $g'(x) = \dfrac{1}{2\sqrt{2 + x}} - 1 < 0$ pour $x \geq 0$ (car $2\sqrt{2+x} \geq 2\sqrt{2} > 1$). Donc $g$ est décroissante sur $[0,2]$.

Or $g(2) = \sqrt{4} - 2 = 0$, donc $g(x) \geq 0$ pour $x \in [0, 2]$, ce qui donne $u_{n+1} \geq u_n$.

\textbf{Conclusion.} La suite $(u_n)$ est croissante et majorée par $2$, donc elle converge.

La limite $\ell$ vérifie $\ell = \sqrt{2 + \ell}$, soit $\ell^2 = 2 + \ell$, d'ou $\ell^2 - \ell - 2 = 0$, c'est-a-dire $(\ell - 2)(\ell + 1) = 0$. Comme $\ell \geq 0$, on obtient $\ell = 2$.
}

% ============================================================
% STRATE 2 — Marge d'appui et erreurs frequentes
% ============================================================

\margeAppui{
\textbf{Diverger vers $+\infty$ :} suite croissante et non majorée. \textbf{Converger :} suite monotone et bornee dans le bon sens. Conclure par le théorème adapte.
}

\erreurFrequente{
\textbf{Confondre « non majorée » et « strictement croissante ».}

La suite $u_n = (-1)^n \cdot n$ n'est pas majorée, mais elle n'est pas croissante. Le théorème ne s'applique que si la suite est \emph{a la fois} croissante \emph{et} non majorée.
}

% BEGIN-VERIFY
% from sympy import Rational
%
% # Le theoreme : croissante et non majoree entraine divergence vers +infini.
% # On l'instancie et on rejoue la demonstration : pour tout A, on exhibe le
% # rang N et on verifie que TOUS les rangs suivants depassent A.
% u = lambda k: k + Rational(1, k + 1)          # croissante, non majoree
% assert all(u(k) < u(k + 1) for k in range(300))
% for A in (5, 50, 500, 5000):
%     N = next(k for k in range(0, 10**5) if u(k) > A)
%     assert all(u(k) >= A for k in range(N, N + 80))
%
% # Les deux hypotheses sont necessaires, et separement.
% # Croissante mais MAJOREE : elle converge, elle ne diverge pas.
% bornee = lambda k: 1 - Rational(1, k + 1)
% assert all(bornee(k) < bornee(k + 1) for k in range(300))
% assert all(bornee(k) < 1 for k in range(300))
% # Non majoree mais NON croissante : elle ne tend pas vers +infini, car elle
% # repasse indefiniment par des valeurs negatives.
% oscillante = lambda k: k * (-1)**k
% assert max(abs(oscillante(k)) for k in range(200)) > 100
% assert any(oscillante(k) < 0 for k in range(200, 300))
% assert not all(oscillante(k) >= 100 for k in range(200, 300))
% END-VERIFY
