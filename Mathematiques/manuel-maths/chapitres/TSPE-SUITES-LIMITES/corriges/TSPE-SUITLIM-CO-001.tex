% META: {"id": "TSPE-SUITLIM-CO-001", "chapitre": "TSPE-SUITES-LIMITES", "type_objet": "corrige", "exercice_ref": "TSPE-SUITLIM-EX-001", "capacites_codes": ["C1"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% n = symbols('n', integer=True, nonnegative=True)
% u = (2*n + 1)/(n + 3)
% assert u.subs(n, 0) == Rational(1, 3)
% assert u.subs(n, 1) == Rational(3, 4)
% assert u.subs(n, 10) == Rational(21, 13)
% assert limit(u, n, oo) == 2
% END-VERIFY
\begin{corrige}{TSPE-SUITLIM-EX-001}

\commentaireMarge{Substituer $n$ dans la formule.}

\textbf{Question 1.}
$u_0 = \dfrac{1}{3}$, \quad $u_1 = \dfrac{3}{4}$, \quad $u_{10} = \dfrac{21}{13}$.

\commentaireMarge{Factoriser par $n$ au numerateur et au denominateur.}

\textbf{Question 2.} On factorise par $n$ :
\[
u_n = \frac{n(2 + 1/n)}{n(1 + 3/n)} = \frac{2 + 1/n}{1 + 3/n}.
\]
Or $\lim \dfrac{1}{n} = 0$ et $\lim \dfrac{3}{n} = 0$, donc $\lim_{n \to +\infty} u_n = \dfrac{2}{1} = \boxed{2}$.

\end{corrige}
