% META: {"id": "TSPE-SUITLIM-EX-011", "chapitre": "TSPE-SUITES-LIMITES", "type_objet": "exercice", "capacites": ["TSPE-SUITLIM-C2"], "capacites_codes": ["C2"], "methodes": ["M2"], "parcours": 2, "competences": ["raisonner"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% # P(n): n! >= 2^n for n >= 4
% import math
% for n in range(4, 15):
%     assert math.factorial(n) >= 2**n
% # n=3: 6 < 8, so fails before n=4
% assert math.factorial(3) < 2**3
% END-VERIFY
\begin{exercice}{TSPE-SUITLIM-EX-011}{2}{15}
\begin{enumerate}
  \item Verifier que $3! < 2^3$ et que $4! \geq 2^4$.
  \item Demontrer par recurrence que pour tout entier $n \geq 4$, $n! \geq 2^n$.
  \item En deduire $\lim_{n \to +\infty} n!$.
\end{enumerate}
\end{exercice}
