% META: {"id":"1SPE-DERGLOBAL-CO-015","chapitre":"1SPE-DERIVATION-GLOBAL","type_objet":"corrige","exercice_id":"1SPE-DERGLOBAL-EX-015","status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import symbols, diff, expand, solve, factor
% x = symbols('x')
% f = (x**2 - 3)*(x**2 + 1)
% fp = diff(f, x)
% assert expand(fp) == 4*x**3 - 4*x
% assert factor(fp) == 4*x*(x - 1)*(x + 1)
% sols = solve(fp, x)
% assert set(sols) == {-1, 0, 1}
% END-VERIFY
\begin{corrige}{1SPE-DERGLOBAL-EX-015}

\textbf{Question 1.} On pose $u = x^2-3$ et $v = x^2+1$, donc $u' = 2x$ et $v' = 2x$.
\[
f'(x) = 2x(x^2+1) + (x^2-3)(2x) = 2x\bigl[(x^2+1) + (x^2-3)\bigr] = 2x(2x^2-2) = 4x(x^2-1).
\]
On factorise : $f'(x) = 4x(x-1)(x+1)$.

\textbf{Question 2.} $f'(x) = 0 \iff 4x(x-1)(x+1) = 0 \iff x \in \{-1\,;\,0\,;\,1\}$.

\textbf{Question 3.} Tableau de variations de $f$ :

\begin{center}
\begin{tabular}{|c|ccccccccc|}
\hline
$x$ & $-\infty$ & & $-1$ & & $0$ & & $1$ & & $+\infty$ \\
\hline
$f'(x)$ & & $-$ & $0$ & $+$ & $0$ & $-$ & $0$ & $+$ & \\
\hline
$f(x)$ & $+\infty$ & $\searrow$ & $-4$ & $\nearrow$ & $-3$ & $\searrow$ & $-4$ & $\nearrow$ & $+\infty$ \\
\hline
\end{tabular}
\end{center}

\textit{Valeurs :} $f(-1) = (1-3)(1+1) = -4$, $f(0) = (-3)(1) = -3$, $f(1) = (1-3)(2) = -4$.
\end{corrige}
