% META: {"id":"1SPE-DERGLOBAL-CO-041","chapitre":"1SPE-DERIVATION-GLOBAL","type_objet":"corrige","exercice_id":"1SPE-DERGLOBAL-EX-041","status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import symbols, diff, solve
% x = symbols('x')
% f = -x**2 + 6*x - 5
% fp = diff(f, x)
% assert fp == -2*x + 6
% xopt = solve(fp, x)
% assert xopt == [3]
% assert f.subs(x, 3) == 4
% assert fp.subs(x, 2) > 0
% assert fp.subs(x, 4) < 0
% END-VERIFY
\begin{corrige}{1SPE-DERGLOBAL-EX-041}
\textbf{1.} On dérive terme à terme :
\[
f'(x) = -2x + 6.
\]

\textbf{2.} On résout $f'(x) = 0$ :
\[
-2x + 6 = 0 \iff x = 3.
\]
$f'(x) > 0$ pour $x < 3$ et $f'(x) < 0$ pour $x > 3$.

Tableau de variations de $f$ sur $\mathbb{R}$ :
\[
\begin{array}{|c|ccccc|}
\hline
x     & -\infty & & 3 & & +\infty \\
\hline
f'(x) &         & + & 0 & - & \\
\hline
f(x)  & -\infty & \nearrow & 4 & \searrow & -\infty \\
\hline
\end{array}
\]

\textbf{3.} $f'$ change de signe de $+$ à $-$ en $x = 3$, donc $f$ admet un \textbf{maximum} en $x = 3$.
Ce maximum vaut $f(3) = -9 + 18 - 5 = \boxed{4}$.
\end{corrige}
