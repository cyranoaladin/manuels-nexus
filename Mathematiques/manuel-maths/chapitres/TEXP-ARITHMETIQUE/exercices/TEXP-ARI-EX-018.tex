% META: {"id": "TEXP-ARI-EX-018", "chapitre": "TEXP-ARITHMETIQUE", "type_objet": "exercice", "capacites": ["TEXP-ARI-C4"], "capacites_codes": ["C4"], "methodes": ["M4"], "parcours": 1, "competences": ["calculer"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TEXP-ARITHMETIQUE/exercices/TEXP-ARI-EX-018.tex", "corrige_tex": "chapitres/TEXP-ARITHMETIQUE/corriges/TEXP-ARI-CO-018.tex", "status": "generated"}
% BEGIN-VERIFY
% import math
% assert math.gcd(7, 11) == 1
% assert 11 == 1 * 7 + 4 and 7 == 1 * 4 + 3 and 4 == 1 * 3 + 1
% assert 2 * 11 - 3 * 7 == 1
% assert 7 * (-3) + 11 * 2 == 1
% for k in range(-6, 7):
%     assert 7 * (-3 + 11 * k) + 11 * (2 - 7 * k) == 1
% trouvees = {(x, y) for x in range(-40, 41) for y in range(-40, 41) if 7 * x + 11 * y == 1}
% attendues = {(-3 + 11 * k, 2 - 7 * k) for k in range(-3, 4)}
% assert attendues <= trouvees
% END-VERIFY
\begin{exercice}{TEXP-ARI-EX-018}{1}{15}
On considère l'équation $(E)$ : $7x+11y=1$ d'inconnues entières $x$ et $y$.
\begin{enumerate}
  \item Justifier que $(E)$ admet des solutions.
  \item Déterminer une solution particulière par l'algorithme d'Euclide
        étendu.
  \item Donner l'ensemble des solutions entières de $(E)$.
\end{enumerate}
\end{exercice}
