% META: {"id": "TEXP-ARI-EX-041", "chapitre": "TEXP-ARITHMETIQUE", "type_objet": "exercice", "capacites": ["TEXP-ARI-C5"], "capacites_codes": ["C5"], "methodes": ["M4"], "parcours": 1, "competences": ["modeliser","raisonner"], "duree_min": 20, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TEXP-ARITHMETIQUE/exercices/TEXP-ARI-EX-041.tex", "corrige_tex": "chapitres/TEXP-ARITHMETIQUE/corriges/TEXP-ARI-CO-041.tex", "status": "approved"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = x**3 - 6*x**2 + 9*x + 1
% fp = diff(f, x)
% fpp = diff(f, x, 2)
% assert expand(fp) == 3*x**2 - 12*x + 9
% assert expand(fpp) == 6*x - 12
% assert solve(fp, x) == [1, 3]
% assert solve(fpp, x) == [2]
% assert f.subs(x, 1) == 5
% assert f.subs(x, 2) == 3
% assert f.subs(x, 3) == 1
% assert fpp.subs(x, 0) == -12  # concave
% assert fpp.subs(x, 4) == 12   # convexe
% END-VERIFY
\begin{exercice}{TEXP-ARI-EX-041}{3}{20}
Soit $f(x) = x^3 - 6x^2 + 9x + 1$. Realiser une etude complete : variations, convexite, points d'inflexion, tangentes remarquables, puis esquisser la courbe.

\end{exercice}
