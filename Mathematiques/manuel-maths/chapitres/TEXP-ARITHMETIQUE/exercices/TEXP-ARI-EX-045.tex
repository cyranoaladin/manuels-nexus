% META: {"id": "TEXP-ARI-EX-045", "chapitre": "TEXP-ARITHMETIQUE", "type_objet": "exercice", "capacites": ["TEXP-ARI-C9"], "capacites_codes": ["C9"], "methodes": ["M3"], "parcours": 1, "competences": ["raisonner"], "duree_min": 18, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TEXP-ARITHMETIQUE/exercices/TEXP-ARI-EX-045.tex", "corrige_tex": "chapitres/TEXP-ARITHMETIQUE/corriges/TEXP-ARI-CO-045.tex", "status": "approved"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% # tangente a e^x en 0: y=1+x (deja vu)
% # mais aussi: e^x >= 1 + x + x^2/2 (meilleure approx car convexe)
% # en fait e^x - 1 - x - x^2/2: etudier
% g = exp(x) - 1 - x - x**2/2
% gp = diff(g, x)
% gpp = diff(g, x, 2)
% assert gpp == exp(x) - 1
% assert gpp.subs(x, 0) == 0
% assert g.subs(x, 0) == 0
% assert gp.subs(x, 0) == 0
% # g'' = e^x - 1 >= 0, donc g' croissante, g'(0)=0 => g'>=0 pour x>=0
% assert gpp.subs(x, 1) == exp(1) - 1 > 0
% assert g.subs(x, 1) == exp(1) - Rational(5,2)
% END-VERIFY
\begin{exercice}{TEXP-ARI-EX-045}{1}{18}
Montrer que pour tout $x \geq 0$ : $\mathrm{e}^x \geq 1 + x + \dfrac{x^2}{2}$.

\end{exercice}
