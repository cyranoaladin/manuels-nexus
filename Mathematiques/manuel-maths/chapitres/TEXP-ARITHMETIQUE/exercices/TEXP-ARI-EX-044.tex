% META: {"id": "TEXP-ARI-EX-044", "chapitre": "TEXP-ARITHMETIQUE", "type_objet": "exercice", "capacites": ["TEXP-ARI-C8"], "capacites_codes": ["C8"], "methodes": ["M3"], "parcours": 1, "competences": ["raisonner"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TEXP-ARITHMETIQUE/exercices/TEXP-ARI-EX-044.tex", "corrige_tex": "chapitres/TEXP-ARITHMETIQUE/corriges/TEXP-ARI-CO-044.tex", "status": "approved"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = exp(x)
% # tangente en 0: y = 1 + x
% # f convexe => f >= tangente => e^x >= 1 + x
% g = exp(x) - 1 - x
% gpp = diff(g, x, 2)
% assert gpp == exp(x)  # > 0, convexe
% assert g.subs(x, 0) == 0
% assert diff(g, x).subs(x, 0) == 0
% assert g.subs(x, 2) == exp(2) - 3 > 0
% END-VERIFY
\begin{exercice}{TEXP-ARI-EX-044}{1}{15}
En utilisant la convexite de $f(x) = \mathrm{e}^x$, demontrer que $\mathrm{e}^x \geq 1 + x$ pour tout $x \in \mathbb{R}$.

\end{exercice}
