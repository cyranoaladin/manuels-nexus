% META: {"id": "TEXP-MAT-EX-038", "chapitre": "TEXP-MATRICES-MARKOV", "type_objet": "exercice", "capacites": ["TEXP-MAT-C3"], "capacites_codes": ["C3"], "methodes": ["M4"], "parcours": 2, "competences": ["raisonner"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TEXP-MATRICES-MARKOV/exercices/TEXP-MAT-EX-038.tex", "corrige_tex": "chapitres/TEXP-MATRICES-MARKOV/corriges/TEXP-MAT-CO-038.tex", "status": "approved"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% # f(x) = x^4 + x^2 >= 0 par convexite (somme de convexe)
% f = x**4 + x**2
% fpp = diff(f, x, 2)
% assert expand(fpp) == 12*x**2 + 2
% # fpp = 12*x**2 + 2 > 0 (strictement convexe)
% assert f.subs(x, 0) == 0
% assert f.subs(x, 1) == 2
% END-VERIFY
\begin{exercice}{TEXP-MAT-EX-038}{2}{15}
Soit $f(x) = x^4 + x^2$.
\begin{enumerate}
  \item Montrer que $f$ est strictement convexe sur $\mathbb{R}$.
  \item En deduire que $f$ admet un unique minimum et le determiner.
  \item Resoudre $f(x) \geq 2$.
\end{enumerate}

\end{exercice}
