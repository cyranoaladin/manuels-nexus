% META: {"id": "TEXP-MAT-CO-009", "chapitre": "TEXP-MATRICES-MARKOV", "type_objet": "corrige", "exercice_ref": "TEXP-MAT-EX-009", "capacites_codes": ["C2"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import Matrix
% P = Matrix([[4, 2, 1], [1, 3, 5]])
% T = Matrix([[6, 4]])
% assert (T * P) == Matrix([[28, 24, 26]])
% assert 6 * 4 + 4 * 1 == 28
% assert 6 * 2 + 4 * 3 == 24
% assert 6 * 1 + 4 * 5 == 26
% END-VERIFY
\begin{corrige}{TEXP-MAT-EX-009}
\textbf{1.} $P=\begin{pmatrix}4&2&1\\1&3&5\end{pmatrix}$.

\textbf{2.} $T=\begin{pmatrix}6&4\end{pmatrix}$ et
$TP=\begin{pmatrix}28&24&26\end{pmatrix}$.

\textbf{3.} Les trois coefficients sont les quantités totales produites :
$28$ pièces $\alpha$, $24$ pièces $\beta$ et $26$ pièces $\gamma$. Le produit
matriciel réalise, pour chaque produit, la somme des contributions des deux
ateliers pondérées par leurs durées.
\end{corrige}
