% META: {"id": "TEXP-CTP-ME-001", "chapitre": "TEXP-COMPLEXES-TRIGO-POLYNOMES", "type_objet": "methode", "capacites_codes": ["C1"], "methodes": ["M1"], "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "needs_review"}
% BEGIN-VERIFY
% from sympy import I, sqrt, Abs, arg, pi, exp, expand_complex
% z = -1 + I
% assert Abs(z) == sqrt(2)
% assert arg(z) == 3*pi/4
% assert expand_complex(sqrt(2)*exp(3*I*pi/4)) == z
% END-VERIFY
\begin{fichemethode}{M1}{Passer de la forme algébrique aux formes trigonométrique et exponentielle}

\quandUtiliser{%
  Utiliser cette méthode lorsque l'énoncé demande de convertir un complexe
  entre ses trois ecritures : algébrique $a + ib$, trigonométrique
  $r(\cos\theta + i\sin\theta)$, exponentielle $r\,e^{i\theta}$.
}

\pasApas{%
  \begin{enumerate}
    \item Algébrique $\to$ trigonométrique : calculer $r = |z| =
      \sqrt{a^2 + b^2}$, puis chercher $\theta$ avec
      $\cos\theta = \frac{a}{r}$ ET $\sin\theta = \frac{b}{r}$.
    \item Reconnaitre un angle usuel a partir du COUPLE de valeurs (sinon,
      valeur approchee).
    \item Écrire $z = r(\cos\theta + i\sin\theta) = r\,e^{i\theta}$ — la forme
      exponentielle n'est qu'une notation de la forme trigonométrique.
    \item Exponentielle $\to$ algébrique : developper
      $r\,e^{i\theta} = r\cos\theta + i\,r\sin\theta$ avec les valeurs
      exactes.
    \item Contrôler : $r > 0$ obligatoirement, et le point doit tomber dans le
      bon quadrant.
  \end{enumerate}
}

\exempleRedige{%
  Mettre $z = -1 + i$ sous formes trigonométrique et exponentielle.

  \medskip
  \commentaireMarge{Module d'abord.}%
  $r = \sqrt{(-1)^2 + 1^2} = \sqrt{2}$.

  \medskip
  \commentaireMarge{Deux conditions pour $\theta$ : cosinus ET sinus.}%
  On cherche $\theta$ tel que $\cos\theta = -\dfrac{1}{\sqrt{2}}$ et
  $\sin\theta = \dfrac{1}{\sqrt{2}}$ : $\theta = \dfrac{3\pi}{4}$ (deuxième
  quadrant).

  \medskip
  Donc $z = \sqrt{2}\left(\cos\dfrac{3\pi}{4} + i\sin\dfrac{3\pi}{4}\right)
  = \sqrt{2}\,e^{3i\pi/4}$.
}

\pieges{%
  \begin{itemize}
    \item \textbf{Piège 1.} Écrire une « forme trigonométrique » avec $r < 0$
      ou avec un signe MOINS entre les deux termes : le format est
      $r(\cos\theta + i\sin\theta)$, $r > 0$.
    \item \textbf{Piège 2.} Déduire $\theta$ du seul cosinus : $\frac{3\pi}{4}$
      et $-\frac{3\pi}{4}$ ont le même cosinus — le sinus tranche.
    \item \textbf{Piège 3.} Confondre $e^{i\theta}$ (module 1) et
      $r\,e^{i\theta}$ : ne pas oublier le module en revenant a la forme
      algébrique.
  \end{itemize}
}

\verifier{%
  Redevelopper la forme trouvee pour retrouver $a + ib$ exactement, et
  contrôler le quadrant du point sur une figure rapide.
}

\sEntrainer{\refExos{M1}}

\end{fichemethode}
