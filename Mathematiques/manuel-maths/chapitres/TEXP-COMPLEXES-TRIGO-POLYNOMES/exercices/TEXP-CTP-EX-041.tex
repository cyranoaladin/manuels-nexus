% META: {"id": "TEXP-CTP-EX-041", "chapitre": "TEXP-COMPLEXES-TRIGO-POLYNOMES", "type_objet": "exercice", "capacites": ["TEXP-CTP-C6"], "capacites_codes": ["C6"], "methodes": ["M4"], "parcours": 2, "competences": ["modeliser","raisonner"], "duree_min": 20, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TEXP-COMPLEXES-TRIGO-POLYNOMES/exercices/TEXP-CTP-EX-041.tex", "corrige_tex": "chapitres/TEXP-COMPLEXES-TRIGO-POLYNOMES/corriges/TEXP-CTP-CO-041.tex", "status": "approved"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = x**3 - 6*x**2 + 9*x + 1
% fp = diff(f, x)
% fpp = diff(f, x, 2)
% assert expand(fp) == 3*x**2 - 12*x + 9
% assert expand(fpp) == 6*x - 12
% assert solve(fp, x) == [1, 3]
% assert solve(fpp, x) == [2]
% assert f.subs(x, 1) == 5
% assert f.subs(x, 2) == 3
% assert f.subs(x, 3) == 1
% assert fpp.subs(x, 0) == -12  # concave
% assert fpp.subs(x, 4) == 12   # convexe
% END-VERIFY
\begin{exercice}{TEXP-CTP-EX-041}{3}{20}
Soit $f(x) = x^3 - 6x^2 + 9x + 1$. Realiser une etude complete : variations, convexite, points d'inflexion, tangentes remarquables, puis esquisser la courbe.

\end{exercice}
