% META: {"id": "TEXP-GRA-EV-A-corrige", "chapitre": "TEXP-GRAPHES", "type_objet": "corrige_evaluation", "evaluation_ref": "TEXP-GRA-EV-A", "capacites_codes": ["C1", "C3", "C4", "C5"], "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import Matrix
% MA = Matrix([
%     [0,1,1,0],
%     [1,0,1,1],
%     [1,1,0,0],
%     [0,1,0,0],
% ])
% deg = [sum(MA.row(i)) for i in range(4)]
% assert deg == [2,3,2,1]
% MA2 = MA**2
% MA3 = MA**3
% assert MA2 == Matrix([[2,1,1,1],[1,3,1,0],[1,1,2,1],[1,0,1,1]])
% assert MA3 == Matrix([[2,4,3,1],[4,2,4,3],[3,4,2,1],[1,3,1,0]])
% assert MA2[0,3] == 1
% assert MA3[0,3] == 1
% assert MA2[0,1] * MA[1,3] == 1
% END-VERIFY
\begin{corrige}{TEXP-GRA-EV-A-EX1}
\textbf{1.} Le graphe a $4$ sommets : il est d'\textbf{ordre 4}.

\textbf{2.} $\deg(P)=2$, $\deg(Q)=3$, $\deg(R)=2$, $\deg(S)=1$.

\textbf{3.} En numérotant $P,Q,R,S$ dans cet ordre :
$$M=\begin{pmatrix}0&1&1&0\\1&0&1&1\\1&1&0&0\\0&1&0&0\end{pmatrix}$$
\end{corrige}

\begin{corrige}{TEXP-GRA-EV-A-EX2}
\textbf{1.}
$$M^2=\begin{pmatrix}2&1&1&1\\1&3&1&0\\1&1&2&1\\1&0&1&1\end{pmatrix}$$
Le coefficient $(M^2)_{P,S}=1$ : il existe $1$ chaîne de longueur $2$ entre $P$ et $S$, à savoir $P\text{-}Q\text{-}S$.

\textbf{2.} $(M^{n+1})_{ij}=\displaystyle\sum_{k}(M^n)_{ik}\times m_{kj}$. Cette relation traduit le fait que toute chaîne de longueur $n+1$ de $i$ à $j$ se décompose de façon unique en une chaîne de longueur $n$ de $i$ à un sommet $k$ adjacent à $j$, suivie de l'arête $\{k,j\}$.

\textbf{3.} $S$ n'a qu'un seul voisin : $Q$. Dans la somme $(M^3)_{P,S}=\sum_k(M^2)_{P,k}\times m_{k,S}$, seul le terme $k=Q$ est non nul. Donc $(M^3)_{P,S}=(M^2)_{P,Q}\times m_{Q,S}=1\times1=1$.
\end{corrige}
