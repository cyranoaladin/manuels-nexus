% META: {"id": "1SPE-GEOREP-CR-013", "chapitre": "1SPE-GEOMETRIE-REPEREE", "type_objet": "cours", "capacites_codes": ["C4"], "sources_inspiration": [], "status": "generated"}

\section{Positions relatives de droites et de cercles}

% ============================================================
% STRATE 1 — Résultats fondamentaux
% ============================================================

\subsection{Positions relatives de deux droites}

\propriete[1]{
Soient $\mathcal{D}_1 : a_1 x + b_1 y + c_1 = 0$ et $\mathcal{D}_2 : a_2 x + b_2 y + c_2 = 0$ deux droites.
\begin{itemize}
  \item $\mathcal{D}_1$ et $\mathcal{D}_2$ sont \textbf{parallèles} $\iff$ $a_1 b_2 - a_2 b_1 = 0$ (vecteurs directeurs colinéaires).
  \item $\mathcal{D}_1$ et $\mathcal{D}_2$ sont \textbf{sécantes} $\iff$ $a_1 b_2 - a_2 b_1
\neq 0$.
  \item Si elles sont sécantes, le point d'intersection s'obtient en résolvant le système
    \[\begin{cases} a_1 x + b_1 y + c_1 = 0 \\ a_2 x + b_2 y + c_2 = 0 \end{cases}\]
  \item $\mathcal{D}_1$ et $\mathcal{D}_2$ sont \textbf{confondues} $\iff$ il existe $\lambda
\neq 0$ tel que $a_2 = \lambda a_1$, $b_2 = \lambda b_1$ et $c_2 = \lambda c_1$.
\end{itemize}
}

\exemple{
$\mathcal{D}_1 : 2x + 3y - 1 = 0$ et $\mathcal{D}_2 : 4x + 6y + 5 = 0$.

$a_1 b_2 - a_2 b_1 = 2 \times 6 - 4 \times 3 = 12 - 12 = 0$ : droites parallèles.

$\mathcal{D}_2$ s'écrit aussi $2(2x + 3y) + 5 = 0$. Si elles étaient confondues, on aurait $c_2 = 2 c_1 = -2$, or $c_2 = 5
\neq -2$. Elles sont donc strictement parallèles (non confondues).
}

\exemple{
$\mathcal{D}_1 : x - 2y + 3 = 0$ et $\mathcal{D}_2 : 3x + y - 5 = 0$.

$a_1 b_2 - a_2 b_1 = 1 \times 1 - 3 \times (-2) = 1 + 6 = 7
\neq 0$ : droites sécantes.

Système : $\begin{cases} x - 2y = -3 \\ 3x + y = 5 \end{cases}$. De la première : $x = 2y - 3$. En substituant : $3(2y - 3) + y = 5$, soit $7y = 14$, $y = 2$, $x = 1$.

Point d'intersection : $(1 ; 2)$.
}

\subsection{Distance d'un point à une droite}

\propriete[2]{
Dans un repère orthonormé, la distance du point $M_0(x_0 ; y_0)$ à la droite $\mathcal{D} : ax + by + c = 0$ est :
\[
  d(M_0, \mathcal{D}) = \frac{|a x_0 + b y_0 + c|}{\sqrt{a^2 + b^2}}.
\]
}

\exemple{
Distance du point $P(1 ; 3)$ à la droite $\mathcal{D} : 3x - 4y + 2 = 0$.

$d = \dfrac{|3 \times 1 - 4 \times 3 + 2|}{\sqrt{9 + 16}} = \dfrac{|3 - 12 + 2|}{5} = \dfrac{|-7|}{5} = \dfrac{7}{5}$.
}

\subsection{Positions relatives d'une droite et d'un cercle}

\propriete[3]{
Soit $\mathcal{C}$ un cercle de centre $\Omega$ et de rayon $r$, et $\mathcal{D}$ une droite. On note $d = d(\Omega, \mathcal{D})$ la distance du centre à la droite.
\begin{itemize}
  \item Si $d > r$ : $\mathcal{D}$ et $\mathcal{C}$ n'ont \textbf{aucun point commun} (la droite est extérieure au cercle).
  \item Si $d = r$ : $\mathcal{D}$ et $\mathcal{C}$ ont \textbf{un seul point commun} (la droite est tangente au cercle).
  \item Si $d < r$ : $\mathcal{D}$ et $\mathcal{C}$ ont \textbf{deux points communs} (la droite est sécante au cercle).
\end{itemize}
}

\exemple{
$\mathcal{C}$ de centre $\Omega(2 ; 1)$ et de rayon $r = 5$. Droite $\mathcal{D} : 3x + 4y - 1 = 0$.

$d = \dfrac{|3 \times 2 + 4 \times 1 - 1|}{\sqrt{9 + 16}} = \dfrac{|6 + 4 - 1|}{5} = \dfrac{9}{5} = 1{,}8$.

$d = 1{,}8 < 5 = r$ : la droite est sécante au cercle (deux points d'intersection).
}

% ============================================================
% STRATE 2 — Marge d'appui et erreurs fréquentes
% ============================================================

\margeAppui{
Substituer $y$ dans l'équation du cercle : le discriminant donne le nombre de solutions.
}

\erreurFrequente{
\textbf{Oubli de la valeur absolue} dans la formule de distance : $|ax_0 + by_0 + c|$ et non $ax_0 + by_0 + c$. La distance est toujours positive.
}

\erreurFrequente{
\textbf{Erreur dans $\sqrt{a^2 + b^2}$} : le dénominateur utilise les coefficients $a$ et $b$ de l'équation de la droite, pas les coordonnées du point.
}

\erreurFrequente{
\textbf{Confusion tangente/sécante.} Une droite tangente touche le cercle en un seul point ($d = r$), une droite sécante coupe le cercle en deux points ($d < r$).
}

% BEGIN-VERIFY
% from sympy import symbols, solve, sqrt, Rational, simplify, Abs
%
% x, y = symbols('x y', real=True)
%
% def determinant(d1, d2):
%     (a1, b1, _), (a2, b2, _) = d1, d2
%     return a1*b2 - a2*b1
%
% def distance(point, droite):
%     a, b, c = droite
%     return Abs(a*point[0] + b*point[1] + c) / sqrt(a**2 + b**2)
%
% # Exemple 1 : droites strictement paralleles.
% D1, D2 = (2, 3, -1), (4, 6, 5)
% assert determinant(D1, D2) == 0
% assert D2[0] == 2*D1[0] and D2[1] == 2*D1[1] and D2[2] != 2*D1[2]
% assert solve([2*x + 3*y - 1, 4*x + 6*y + 5], [x, y]) == []
%
% # Exemple 2 : droites secantes, intersection (1 ; 2).
% E1, E2 = (1, -2, 3), (3, 1, -5)
% assert determinant(E1, E2) == 7 != 0
% assert solve([x - 2*y + 3, 3*x + y - 5], [x, y]) == {x: 1, y: 2}
%
% # Distance : l'exemple du cours, P(1;3) a la droite 3x-4y+2=0.
% assert simplify(distance((1, 3), (3, -4, 2)) - Rational(7, 5)) == 0
% # L'erreur frequente nommee : sans valeur absolue, on obtiendrait -7/5.
% assert Rational(3*1 - 4*3 + 2, 5) == Rational(-7, 5)
% assert distance((1, 3), (3, -4, 2)) > 0
%
% # Position droite / cercle : l'exemple, d = 9/5 < 5 = r, donc deux points.
% centre, rayon = (2, 1), 5
% droite = (3, 4, -1)
% d = distance(centre, droite)
% assert simplify(d - Rational(9, 5)) == 0 and d < rayon
% cercle = (x - centre[0])**2 + (y - centre[1])**2 - rayon**2
% assert len(solve([cercle, 3*x + 4*y - 1], [x, y])) == 2
%
% # Les deux autres cas du critere, construits pour tomber juste.
% tangente = (3, 4, -(3*centre[0] + 4*centre[1]) - 25)      # d = 5 = r
% assert simplify(distance(centre, tangente) - 5) == 0
% assert len(solve([cercle,
%                   tangente[0]*x + tangente[1]*y + tangente[2]], [x, y])) == 1
% exterieure = (3, 4, -(3*centre[0] + 4*centre[1]) - 50)    # d = 10 > r
% assert simplify(distance(centre, exterieure) - 10) == 0
% assert solve([cercle, exterieure[0]*x + exterieure[1]*y + exterieure[2]],
%              [x, y], dict=True) == []
% END-VERIFY
