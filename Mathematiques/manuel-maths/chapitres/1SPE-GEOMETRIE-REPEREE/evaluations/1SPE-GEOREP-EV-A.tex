% META: {"id": "1SPE-GEOREP-EV-A", "chapitre": "1SPE-GEOMETRIE-REPEREE", "type_objet": "evaluation", "version": "A", "duree_min": 55, "bareme_total": 20, "capacites_codes": ["C1", "C2", "C3", "C4", "C5"], "competences": ["chercher", "modeliser", "representer", "calculer", "raisonner", "communiquer"], "mode_creation": "ex_nihilo", "sources_inspiration": [], "parametres_sympy": {"A": [1, 3], "B": [5, 1], "Omega": [3, -1], "r": 4}, "fichier_tex": "chapitres/1SPE-GEOMETRIE-REPEREE/evaluations/1SPE-GEOREP-EV-A.tex", "corrige_tex": "chapitres/1SPE-GEOMETRIE-REPEREE/evaluations/1SPE-GEOREP-EV-A-corrige.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x, y = symbols('x y')
%
% # ---- Exercice 1 : equation de droite (C1, 4 pts) ----
% # Droite par A(1,3) et B(5,1): m = (1-3)/(5-1) = -1/2
% m = Rational(-1, 2)
% assert m == Rational(-1, 2)
% # y - 3 = -1/2(x-1) => y = -x/2 + 7/2 => x + 2y - 7 = 0
% assert 1 + 2*3 - 7 == 0
% assert 5 + 2*1 - 7 == 0
%
% # ---- Exercice 2 : vecteur normal (C2, 4 pts) ----
% # D: x + 2y - 7 = 0, normal (1,2), directeur (-2,1)
% assert 1*(-2) + 2*1 == 0
% # Perpendiculaire passant par M(3,4): vecteur normal (-2,1) => 2(x-3)-1(y-4)=0 non
% # Normal de la perp = directeur de D = (-2,1) => -2(x-3)+1(y-4)=0 => -2x+6+y-4=0 => -2x+y+2=0 => 2x-y-2=0
% assert 2*3 - 4 - 2 == 0
% # Ortho check: 1*2 + 2*(-1) = 0
% assert 1*2 + 2*(-1) == 0
%
% # ---- Exercice 3 : cercle (C3, 4 pts) ----
% # Cercle centre Omega(3,-1), rayon 4: (x-3)^2+(y+1)^2=16
% assert (3-3)**2 + (-1+1)**2 == 0  # centre
% # P(7,-1): (7-3)^2+(0)^2 = 16 => sur le cercle
% assert (7-3)**2 + (-1+1)**2 == 16
% # Q(3,3): (0)^2+(4)^2 = 16 => sur le cercle
% assert (3-3)**2 + (3+1)**2 == 16
%
% # ---- Exercice 4 : position relative (C4, 4 pts) ----
% # Distance de Omega(3,-1) a D: x+2y-7=0
% d = abs(3 + 2*(-1) - 7) / sqrt(1+4)
% assert d == Rational(6, 1) / sqrt(5)
% # Simplifie: d = 6/sqrt(5) = 6*sqrt(5)/5
% # Compare a r=4: 6*sqrt(5)/5 vs 4. 6*sqrt(5)/5 = 6*2.236.../5 ≈ 2.68 < 4
% assert (Rational(6,1)/sqrt(5)) < 4
%
% # ---- Exercice 5 : probleme (C5, 4 pts) ----
% # Aire du triangle ABOmega: A(1,3), B(5,1), Omega(3,-1)
% # AB = (4,-2), AOmega = (2,-4)
% # Aire = |4*(-4)-(-2)*2|/2 = |-16+4|/2 = 12/2 = 6
% assert abs(4*(-4) - (-2)*2) == 12
% assert 12//2 == 6
% END-VERIFY

%---------------------------------------------------------------
% Durée : 55 minutes — Barème : 20 points
% Toute réponse doit être justifiée ; les résultats seuls ne sont pas acceptés.
% La qualité de la rédaction sera prise en compte.
%---------------------------------------------------------------

\begin{evaluation}{1SPE-GEOREP-EV-A}{55}

%===============================================================
% EXERCICE 1 — Équation cartésienne d'une droite (4 points)
%===============================================================

\exerciceEval{1}{4}{C1}{calculer, raisonner}

On considère les points $A(1 ; 3)$ et $B(5 ; 1)$ dans un repère orthonormé.

\begin{enumerate}
  \item Calculer les coordonnées du vecteur $\vec{AB}$.
  \item Déterminer le coefficient directeur de la droite $(AB)$.
  \item En déduire une équation cartésienne de la droite $(AB)$.
  \item Vérifier que les points $A$ et $B$ appartiennent à cette droite.
\end{enumerate}

%===============================================================
% EXERCICE 2 — Vecteur normal (4 points)
%===============================================================

\exerciceEval{2}{4}{C2}{calculer, raisonner}

Soit la droite $\mathcal{D}$ d'équation cartésienne $x + 2y - 7 = 0$.

\begin{enumerate}
  \item Donner un vecteur normal et un vecteur directeur de $\mathcal{D}$.
  \item Déterminer l'équation de la droite $\mathcal{D}'$ perpendiculaire à $\mathcal{D}$ passant par $M(3 ; 4)$.
  \item Vérifier que $\mathcal{D}$ et $\mathcal{D}'$ sont bien perpendiculaires.
\end{enumerate}

%===============================================================
% EXERCICE 3 — Équation du cercle (4 points)
%===============================================================

\exerciceEval{3}{4}{C3}{calculer, raisonner}

Soit le cercle $\mathcal{C}$ de centre $\Omega(3 ; -1)$ et de rayon $4$.

\begin{enumerate}
  \item Écrire l'équation de $\mathcal{C}$.
  \item Le point $P(7 ; -1)$ est-il sur $\mathcal{C}$ ?
  \item Le point $Q(3 ; 3)$ est-il sur $\mathcal{C}$ ?
  \item Développer l'équation de $\mathcal{C}$ et vérifier que l'on retrouve le centre et le rayon en complétant les carrés.
\end{enumerate}

%===============================================================
% EXERCICE 4 — Position relative (4 points)
%===============================================================

\exerciceEval{4}{4}{C4}{calculer, raisonner, communiquer}

On reprend la droite $(AB) : x + 2y - 7 = 0$ et le cercle $\mathcal{C}$ de centre $\Omega(3 ; -1)$ et de rayon $4$.

\begin{enumerate}
  \item Calculer la distance de $\Omega$ à la droite $(AB)$.
  \item En déduire la position relative de la droite $(AB)$ et du cercle $\mathcal{C}$.
  \item Déterminer les coordonnées exactes des points d'intersection.
\end{enumerate}

%===============================================================
% EXERCICE 5 — Problème (4 points)
%===============================================================

\exerciceEval{5}{4}{C5}{chercher, raisonner, communiquer}

Avec les mêmes points $A(1 ; 3)$, $B(5 ; 1)$ et $\Omega(3 ; -1)$ :

\begin{enumerate}
  \item Calculer l'aire du triangle $AB\Omega$.
  \item Déterminer le projeté orthogonal de $\Omega$ sur la droite $(AB)$.
  \item En déduire la hauteur du triangle $AB\Omega$ relative au côté $[AB]$.
\end{enumerate}

\end{evaluation}
