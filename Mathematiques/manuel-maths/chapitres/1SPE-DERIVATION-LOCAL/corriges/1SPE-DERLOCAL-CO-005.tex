% META: {"id":"1SPE-DERLOCAL-CO-005","chapitre":"1SPE-DERIVATION-LOCAL","type_objet":"corrige","exercice_id":"1SPE-DERLOCAL-EX-005","status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import symbols, simplify, factor
% a, b = symbols('a b')
% expr = (b**2 - a**2)/(b - a)
% assert simplify(expr - (a + b)) == 0
% END-VERIFY
\begin{corrige}{1SPE-DERLOCAL-EX-005}
\textbf{1.} On calcule :
\begin{itemize}
  \item Entre $1$ et $3$ : $\dfrac{3^2-1^2}{3-1}=\dfrac{8}{2}=4$.
  \item Entre $2$ et $4$ : $\dfrac{4^2-2^2}{4-2}=\dfrac{12}{2}=6$.
  \item Entre $0$ et $5$ : $\dfrac{5^2-0^2}{5-0}=\dfrac{25}{5}=5$.
\end{itemize}

\textbf{2.} On observe : $4=1+3$, $6=2+4$, $5=0+5$. Conjecture : le taux de variation de $f$ entre $a$ et $b$ vaut $a+b$.

\textbf{3.} Pour $a
e b$, on écrit
\[
\frac{f(b)-f(a)}{b-a}=\frac{b^2-a^2}{b-a}.
\]
On factorise le numérateur par l'identité remarquable $b^2-a^2=(b-a)(b+a)$. Comme $b
e a$, on simplifie :
\[
\frac{(b-a)(b+a)}{b-a}=b+a=a+b.
\]
La conjecture est démontrée.

\textbf{4.} La sécante à la parabole passant par les points d'abscisses $a$ et $b$ a pour coefficient directeur le taux de variation de $f$ entre $a$ et $b$, soit $a+b$.
\end{corrige}
