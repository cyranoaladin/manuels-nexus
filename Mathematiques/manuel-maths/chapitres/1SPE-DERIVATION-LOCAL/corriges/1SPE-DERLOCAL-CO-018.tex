% META: {"id":"1SPE-DERLOCAL-CO-018","chapitre":"1SPE-DERIVATION-LOCAL","type_objet":"corrige","exercice_id":"1SPE-DERLOCAL-EX-018","status":"generated"}
% BEGIN-VERIFY
% from sympy import symbols, Eq, solve
% a = symbols('a')
% sol = solve(Eq(2*a, 6), a)
% assert sol == [3]
% assert 3**2 == 9
% # Tangente en 3 : y = 6(x-3)+9 = 6x - 9
% END-VERIFY
\begin{corrige}{1SPE-DERLOCAL-EX-018}

\textbf{Question 1.} On a $f(a) = a^2$ et $f'(a) = 2a$. L'équation de la tangente $T_a$ au point d'abscisse $a$ est :
\[
y = f'(a)(x - a) + f(a) = 2a(x - a) + a^2 = 2ax - 2a^2 + a^2.
\]
D'où $T_a : y = 2ax - a^2$. Le coefficient directeur de $T_a$ est $\boxed{2a}$.

\textbf{Question 2.} La droite $\mathcal{D}$ a pour équation $y = 6x - 1$, donc son coefficient directeur est $6$.

Deux droites sont parallèles si et seulement si elles ont le même coefficient directeur. On cherche $a$ tel que :
\[
2a = 6 \iff a = 3.
\]
Il existe une unique valeur $a = 3$ pour laquelle la tangente est parallèle à $\mathcal{D}$. La réponse est donc \textbf{oui}.

\textbf{Question 3.} Pour $a = 3$ : $f(3) = 3^2 = 9$. Le point de la courbe est $\boxed{(3\,;\,9)}$.

L'équation de la tangente en ce point est :
\[
y = 2 \times 3 \times x - 3^2 = 6x - 9.
\]
Soit $\boxed{y = 6x - 9}$.

\textbf{Vérification :} le coefficient directeur de cette tangente est bien $6$, identique à celui de $\mathcal{D}$. De plus, $\mathcal{D} : y = 6x - 1$ et $T_3 : y = 6x - 9$ ont des ordonnées à l'origine distinctes ($-1 \neq -9$), donc ces droites sont bien parallèles et non confondues. \checkmark

\end{corrige}
