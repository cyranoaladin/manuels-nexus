% META: {"id":"1SPE-DERIVATION-LOCAL-QCM","chapitre":"1SPE-DERIVATION-LOCAL","type_objet":"qcm","capacites_codes":["C1","C2","C3","C4","C5"],"status":"generated"}
\section*{QCM d'auto-évaluation — Dérivation locale}
Chaque réponse erronée renvoie à la fiche méthode indiquée dans la version numérique.
\begin{enumerate}
\item [C1] Le taux de variation entre $a$ et $b$ est : A. $\frac{f(b)-f(a)}{b}$ ; B. $\frac{f(b)-f(a)}{b-a}$ ; C. $f(b)-f(a)$ ; D. $f(a)$.
\item [C2] Le nombre dérivé représente : A. la pente de la tangente ; B. une vitesse moyenne ; C. une ordonnée ; D. une abscisse.
\item [C3] La tangente en $A$ : A. passe par deux points distincts ; B. est toujours horizontale ; C. a pour pente $f'(a)$ ; D. ne passe pas par $A$.
\item [C4] Si $f(a)=3$ et $f'(a)=2$, la tangente en $a=1$ est : A. $y=3$ ; B. $y=2x$ ; C. $y=x+3$ ; D. $y=2(x-1)+3$.
\item [C5] Si $f(a)=4$, $f'(a)=3$ et $h=0{,}1$, alors $f(a+h)\approx$ : A. $3{,}6$ ; B. $4{,}3$ ; C. $0{,}3$ ; D. $7$.
\item [C1] Une sécante relie : A. deux points de la courbe ; B. un point et une pente ; C. deux tangentes ; D. deux axes.
\item [C1] Si $f$ est croissante entre $a$ et $b$, son taux de variation est : A. positif ; B. nul ; C. négatif ; D. indéterminé.
\item [C2] $f'(a)$ est calculé au voisinage : A. de $a$ ; B. de $0$ toujours ; C. de $f(a)$ ; D. de $x$ quelconque.
\item [C2] Une vitesse instantanée est : A. une pente de tangente ; B. une distance ; C. une durée ; D. une sécante fixe.
\item [C3] Pour construire une tangente connue, il faut : A. un point et sa pente ; B. deux ordonnées ; C. une aire ; D. une valeur de $h$ seule.
\item [C3] Le point de contact appartient : A. à la courbe et à la tangente ; B. seulement à la courbe ; C. seulement à l'axe ; D. à aucune droite.
\item [C4] La forme correcte est : A. $y=f'(a)(x-a)+f(a)$ ; B. $y=f'(x)x$ ; C. $y=f(a)x$ ; D. $y=x-a$.
\item [C4] Vérifier une tangente consiste notamment à remplacer : A. $x$ par $a$ ; B. $a$ par $x$ ; C. $f(a)$ par $0$ ; D. $x$ par $h$.
\item [C5] L'approximation linéaire est fiable lorsque : A. $h$ est proche de $0$ ; B. $h$ est très grand ; C. $f'(a)=0$ seulement ; D. $a=0$ seulement.
\item [C5] Le symbole approprié est : A. $\approx$ ; B. $=$ dans tous les cas ; C. $>$ ; D. $\in$.
\end{enumerate}
