% META: {"id":"1SPE-DERIVATION-LOCAL-CR-012","chapitre":"1SPE-DERIVATION-LOCAL","type_objet":"cours","capacites_codes":["C3"],"sources_inspiration":[],"status":"generated"}
\section{Lire et construire une tangente}
\definition[Tangente]{Lorsque $f$ admet un nombre dérivé en $a$, la \textbf{tangente} à sa courbe au point $A(a;f(a))$ est la droite qui passe par $A$ et dont le coefficient directeur est $f'(a)$.}
\exemple{Si $A(2;5)$ appartient à la courbe et si $f'(2)=-3$, la tangente passe par $A$ et descend de $3$ unités lorsque l'on avance d'une unité.}
\contreexemple{Une droite coupant la courbe en deux points distincts est une sécante ; elle n'est pas nécessairement la tangente au premier point.}
\propriete[Lecture sur un quadrillage]{Pour déterminer graphiquement $f'(a)$, on choisit deux points lisibles sur la \emph{droite tangente}, puis on calcule « déplacement vertical / déplacement horizontal ».}
\exemple{Une tangente passe par les intersections $(1;2)$ et $(3;3)$. Sa pente vaut $\frac{3-2}{3-1}=\frac12$.}
\contreexemple{Prendre deux points de la courbe au lieu de deux points de la tangente donne la pente d'une sécante, pas le nombre dérivé demandé.}
\begin{nxfigure}{Lecture de la pente sur un quadrillage}
\begin{tikzpicture}
\begin{axis}[nexus,
  xmin=-0.5, xmax=5.5, ymin=-0.5, ymax=5.5,
  xlabel=$x$, ylabel=$y$,
  xtick={0,1,...,5}, ytick={0,1,...,5},
  grid=both, minor grid style={nxGris!10}]
  \addplot[courbe,domain=0:5,samples=60] {0.3*(x-1)^2 + 1};
  % Tangente en x=3, pente = 0.6*(3-1) = 1.2
  \addplot[tangente,domain=0.5:5] {1.2*(x-3) + 2.2};
  \addplot[point] coordinates {(3,2.2)};
  % Lecture : de (1,−0.2) à (3,2.2), mais prenons des points lisibles
  % Points de lecture sur la tangente
  \addplot[point,nxOr] coordinates {(1,{1.2*(1-3)+2.2}) (5,{1.2*(5-3)+2.2})};
  
\node[anchor=south east,font=\footnotesize,nxBleu] at (axis cs:3,2.2) {point de contact};
  % Flèches de lecture
  \draw[-{Stealth},nxOr,line width=0.6pt] (axis cs:1,-0.2) -- node[right,font=\footnotesize] {$+4.8$} (axis cs:1,4.6);
  \draw[-{Stealth},nxOr,line width=0.6pt] (axis cs:1,-0.2) -- node[below,font=\footnotesize] {$+4$} (axis cs:5,-0.2);
  
\node[anchor=north,font=\footnotesize,nxOr] at (axis cs:3,-0.3) {pente $=\frac{4{,}8}{4}=1{,}2$};
\end{axis}
\end{tikzpicture}

\nxcaption{On lit la pente de la tangente en choisissant deux points sur la droite, pas sur la courbe.}
\end{nxfigure}

\margeAppui{Pour éviter une lecture imprécise, prolonger légèrement la tangente et choisir deux intersections du quadrillage éloignées l'une de l'autre.}
\margeAppui{Pour construire une pente $\frac pq$, avancer de $q$ carreaux et monter de $p$ carreaux ; si $p$ est négatif, descendre.}
\erreurFrequente{\textbf{Copie fautive :} lecture de l'ordonnée de $A$ comme nombre dérivé. \textbf{Correction :} l'ordonnée est $f(a)$ ; le nombre dérivé est la pente de la droite tangente.}
\erreurFrequente{\textbf{Copie fautive :} droite « à peu près parallèle » ne passant pas par $A$. \textbf{Correction :} une tangente doit passer exactement par son point de contact.}
\approfondissement{Procédé graphique complet : 1. repérer le point de contact ; 2. tracer ou identifier la tangente ; 3. choisir deux intersections nettes de cette droite avec le quadrillage ; 4. calculer le rapport des déplacements en conservant leur signe ; 5. pour la construction, placer depuis $A$ un second point par le même déplacement, puis relier les deux points.}

% BEGIN-VERIFY
% from sympy import Rational, symbols, diff
%
% # La lecture graphique de l'exemple : une tangente passant par (1;2) et (3;3).
% A, B_pt = (1, 2), (3, 3)
% pente = Rational(B_pt[1] - A[1], B_pt[0] - A[0])
% assert pente == Rational(1, 2)
%
% # Le contre-exemple : deux points de la COURBE donnent la pente d'une
% # secante, pas le nombre derive. Sur f(x) = x^2, entre 1 et 3 :
% carre = lambda t: t**2
% pente_secante = Rational(carre(3) - carre(1), 3 - 1)
% x = symbols('x', real=True)
% nombre_derive = diff(x**2, x).subs(x, 1)
% assert pente_secante == 4 and nombre_derive == 2
% assert pente_secante != nombre_derive
%
% # Une secante coupe la courbe en deux points ; la tangente en touche un.
% # Sur f(x)=x^2, la droite y = 4x - 3 rencontre la courbe en 1 et 3.
% rencontres = [t for t in (-3, -1, 0, 1, 2, 3, 4) if carre(t) == 4*t - 3]
% assert rencontres == [1, 3]
% # La tangente en 1, y = 2x - 1, ne la rencontre qu'en 1.
% tangentes = [t for t in (-3, -1, 0, 1, 2, 3, 4) if carre(t) == 2*t - 1]
% assert tangentes == [1]
%
% # L'erreur frequente nommee : l'ordonnee de A n'est pas le nombre derive.
% # En a = 2 sur la fonction carre, f(2) = 4 et f'(2) = 4 coincident par
% # hasard ; en a = 3 elles different, ce qui suffit a les distinguer.
% assert carre(3) == 9 and diff(x**2, x).subs(x, 3) == 6
% END-VERIFY
