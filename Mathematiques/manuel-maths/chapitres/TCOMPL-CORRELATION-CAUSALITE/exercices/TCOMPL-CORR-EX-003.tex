% META: {"id": "TCOMPL-CORR-EX-003", "chapitre": "TCOMPL-CORRELATION-CAUSALITE", "type_objet": "exercice", "capacites": ["TCOMPL-CORR-C3"], "capacites_codes": ["C3"], "methodes": [], "parcours": 2, "competences": ["modeliser"], "duree_min": 20, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TCOMPL-CORRELATION-CAUSALITE/exercices/TCOMPL-CORR-EX-003.tex", "corrige_tex": "chapitres/TCOMPL-CORRELATION-CAUSALITE/corriges/TCOMPL-CORR-CO-003.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import log, symbols, diff, solve, N, exp
% xs = [1, 2, 3, 4]
% ys = [3, 12, 27, 48]
% Ys = [log(y) for y in ys]
% a, b = symbols('a b')
% S = sum((Y - (a*log(x) + b))**2 for x, Y in zip(xs, Ys))
% sols = solve([diff(S, a), diff(S, b)], [a, b])
% assert abs(N(sols[a]) - 2) < 1e-9
% assert abs(N(exp(sols[b])) - 3) < 1e-9
% END-VERIFY
\begin{exercice}{TCOMPL-CORR-EX-003}{2}{20}
On soupçonne qu'une série de données $(x_i,y_i)$ suit une relation de type $y=A\,x^{b}$ (loi puissance), avec $(1,3),(2,12),(3,27),(4,48)$.
\begin{enumerate}
  \item Quel changement de variable, en posant $X=\ln(x)$ et $Y=\ln(y)$, permet de linéariser une relation de type $y=Ax^b$ ?
  \item À la calculatrice, déterminer la droite de régression de $Y$ en fonction de $X$, puis en déduire $A$ et $b$.
\end{enumerate}
\end{exercice}
