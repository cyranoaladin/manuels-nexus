% META: {"id": "TCOMPL-CORR-EX-004", "chapitre": "TCOMPL-CORRELATION-CAUSALITE", "type_objet": "exercice", "capacites": ["TCOMPL-CORR-C5"], "capacites_codes": ["C5"], "methodes": [], "parcours": 1, "competences": ["raisonner"], "duree_min": 10, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TCOMPL-CORRELATION-CAUSALITE/exercices/TCOMPL-CORR-EX-004.tex", "corrige_tex": "chapitres/TCOMPL-CORRELATION-CAUSALITE/corriges/TCOMPL-CORR-CO-004.tex", "status": "generated"}
\begin{exercice}{TCOMPL-CORR-EX-004}{1}{10}
Dans une ville, le nombre de pompiers mobilisés lors d'un incendie et les dégâts matériels causés par cet incendie sont fortement corrélés (plus il y a de pompiers, plus les dégâts sont importants).
\begin{enumerate}
  \item Peut-on en conclure que les pompiers \textbf{causent} les dégâts ?
  \item Proposer une explication plus plausible à cette corrélation, en identifiant une variable cachée.
\end{enumerate}
\end{exercice}

% BEGIN-VERIFY
% from statistics import correlation
%
% # La situation de l'enonce, simulee : la TAILLE de l'incendie cause a la fois
% # le nombre de pompiers et les degats.
% taille = [1, 2, 3, 5, 8, 13, 20, 30]
% pompiers = [2*t + 3 for t in taille]
% degats = [15*t + 10 for t in taille]
%
% # Q1 : les deux variables observees sont bien fortement correlees...
% assert correlation(pompiers, degats) > 0.99
% # ... et chacune l'est tout autant avec la taille.
% assert correlation(pompiers, taille) > 0.99
% assert correlation(degats, taille) > 0.99
%
% # Q2 : a taille d'incendie FIXEE, envoyer plus de pompiers va de pair avec
% # MOINS de degats. La causalite suggeree par la question 1 est donc refutee
% # par les donnees elles-memes.
% pompiers_fixe = [4, 6, 8, 10, 12]
% degats_fixe = [90, 80, 70, 60, 50]
% assert correlation(pompiers_fixe, degats_fixe) < -0.99
% END-VERIFY
