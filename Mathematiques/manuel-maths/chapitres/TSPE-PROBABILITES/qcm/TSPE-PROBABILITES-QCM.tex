% META: {"id": "TSPE-PROBABILITES-QCM", "chapitre": "TSPE-PROBABILITES", "type_objet": "qcm", "capacites_codes": ["C1","C2","C5","C7","C8","C9","C10"], "status": "generated"}

\section*{QCM d'auto-evaluation — Probabilités}

\begin{center}
\textit{Pour chaque question, une seule reponse est exacte. Entourez la lettre correspondante.}
\end{center}

\bigskip

\subsection*{C2/C5 — Schéma de Bernoulli, loi binomiale}

\begin{enumerate}

\item \textbf{[Q1]} Si $X \sim \mathcal{B}(n,p)$, alors $P(X=k)$ vaut :
  \begin{enumerate}[label=\Alph*.]
    \item $p^k(1-p)^{n-k}$.
    \item $\dbinom{n}{k}p^k(1-p)^{n-k}$.
    \item $\dbinom{n}{k}p^k$.
    \item $np^k$.
  \end{enumerate}

\bigskip

\item \textbf{[Q2]} Un schema de Bernoulli suppose que les epreuves sont :
  \begin{enumerate}[label=\Alph*.]
    \item dependantes et identiques.
    \item independantes et identiques.
    \item independantes et differentes.
    \item toutes de probabilite $1/2$.
  \end{enumerate}

\bigskip

\subsection*{C7/C8/C9 — Espérance, variance}

\item \textbf{[Q3]} La linearite de l'esperance $E(X+Y)=E(X)+E(Y)$ est valable :
  \begin{enumerate}[label=\Alph*.]
    \item uniquement si $X,Y$ sont independantes.
    \item toujours, sans condition d'independance.
    \item uniquement si $X=Y$.
    \item jamais.
  \end{enumerate}

\bigskip

\item \textbf{[Q4]} L'additivite de la variance $V(X+Y)=V(X)+V(Y)$ necessite :
  \begin{enumerate}[label=\Alph*.]
    \item que $X$ et $Y$ soient independantes.
    \item que $X=Y$.
    \item rien de particulier.
    \item que $E(X)=E(Y)$.
  \end{enumerate}

\bigskip

\item \textbf{[Q5]} Pour $X \sim \mathcal{B}(n,p)$, $V(X)$ vaut :
  \begin{enumerate}[label=\Alph*.]
    \item $np$.
    \item $np(1-p)$.
    \item $n(1-p)$.
    \item $p(1-p)$.
  \end{enumerate}

\bigskip

\subsection*{C10 — Concentration}

\item \textbf{[Q6]} L'inegalite de Bienayme-Tchebychev majore :
  \begin{enumerate}[label=\Alph*.]
    \item $P(|Y-\mu|\geq\delta)$.
    \item $E(Y)$.
    \item $V(Y)$.
    \item $P(Y=\mu)$.
  \end{enumerate}

\end{enumerate}
