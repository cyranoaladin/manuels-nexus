% META: {"id": "TSPE-PROBABILITES-COURS-07", "chapitre": "TSPE-PROBABILITES", "type_objet": "cours", "capacites_codes": ["C7"], "sources_inspiration": [], "mode_creation": "ex_nihilo", "status": "approved", "capacites": ["TSPE-PROBA-C7"], "statut": "structure"}

\section{Concentration, inégalité de Bienaymé-Tchebychev}

% ============================================================
% STRATE 1 — Inegalite et application (echantillonnage)
% ============================================================

\theoreme[Inégalité de Bienaymé-Tchebychev, admise]{
Soit $Y$ une variable aleatoire d'esperance $\mu$ et de variance $V$. Pour tout reel $\delta>0$ :
\[
  P(|Y-\mu| \geq \delta) \leq \frac{V}{\delta^2}.
\]
}

\margeAppui{
Cette inegalite quantifie l'idee de \textbf{concentration} : plus la variance est petite, plus $Y$ est « concentree » autour de son esperance.
}

\propriete[Application à l'échantillonnage]{
Soit $X_n$ le nombre de succes dans un schema de Bernoulli de parametres $n,p$, et $M_n = \dfrac{X_n}{n}$ la frequence observee. Alors $E(M_n)=p$ et $V(M_n) = \dfrac{p(1-p)}{n}$, et pour tout $\delta>0$ :
\[
  P\big(|M_n-p| \geq \delta\big) \leq \frac{p(1-p)}{n\,\delta^2} \leq \frac{1}{4n\delta^2}.
\]
}

\margeAppui{
On utilise $p(1-p) \leq \dfrac{1}{4}$ pour tout $p \in [0,1]$ (maximum atteint en $p=1/2$), ce qui donne une majoration valable quel que soit $p$ (utile si $p$ est inconnu).
}

\exemple{
On souhaite determiner une taille d'echantillon $n$ garantissant $P(|M_n-p|\geq 0{,}02) \leq 0{,}05$, sans connaitre $p$.

On veut $\dfrac{1}{4n\times(0{,}02)^2} \leq 0{,}05$, soit $n \geq \dfrac{1}{4\times(0{,}02)^2\times0{,}05} = \dfrac{1}{0{,}00008} = 12\,500$.

% BEGIN-VERIFY
% from sympy import Rational
% n_min = 1 / (4 * Rational(2,100)**2 * Rational(5,100))
% assert n_min == 12500
% END-VERIFY

Un echantillon de taille $n=12\,500$ suffit donc a garantir cette precision avec ce risque.
}

% ============================================================
% STRATE 2 — Erreurs frequentes
% ============================================================

\erreurFrequente{
\textbf{Oublier la majoration $p(1-p)\leq 1/4$ lorsque $p$ est inconnu.} Sans cette majoration, il faudrait connaitre $p$ pour calculer $V(M_n)=p(1-p)/n$. La borne $1/(4n\delta^2)$ permet de s'affranchir de cette connaissance, au prix d'une majoration moins fine.
}
