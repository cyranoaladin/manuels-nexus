% META: {"id": "1SPE-PRODSCAL-EV-B-corrige", "chapitre": "1SPE-PRODUIT-SCALAIRE", "type_objet": "corrige_evaluation", "capacites_codes": ["C1", "C2", "C3", "C4", "C5"], "status": "generated", "evaluation_ref": "1SPE-PRODSCAL-EV-B"}

\section*{Corrige — Évaluation B — Produit scalaire}

%---------------------------------------------------------------
\subsection*{Exercice 1 — 5 points}
%---------------------------------------------------------------

\baremeIndicatif{Q1 : 1 pt ; Q2 : 1 pt ; Q3 : 1,5 pt ; Q4 : 1,5 pt}

\textbf{Question 1.}
$\vec{u} \cdot \vec{v} = 4 \times (-1) + 2 \times 3 = -4 + 6 = 2$.

\textbf{Question 2.}
$\|\vec{u}\| = \sqrt{16 + 4} = \sqrt{20} = 2\sqrt{5}$ et $\|\vec{v}\| = \sqrt{1 + 9} = \sqrt{10}$.

\textbf{Question 3.}
$\|\vec{u} - \vec{v}\|^2 = \|\vec{u}\|^2 - 2\,\vec{u} \cdot \vec{v} + \|\vec{v}\|^2 = 20 - 4 + 10 = 26$.

Donc $\|\vec{u} - \vec{v}\| = \sqrt{26}$.

Vérification : $\vec{u} - \vec{v} = \begin{pmatrix} 5 \\ -1 \end{pmatrix}$, $\|\vec{u} - \vec{v}\|^2 = 25 + 1 = 26$. \checkmark

\textbf{Question 4.}
$(\vec{u} + \vec{v}) \cdot (\vec{u} - \vec{v}) = \|\vec{u}\|^2 - \|\vec{v}\|^2 = 20 - 10 = 10$.

Vérification : $\vec{u} + \vec{v} = \begin{pmatrix} 3 \\ 5 \end{pmatrix}$, $\vec{u} - \vec{v} = \begin{pmatrix} 5 \\ -1 \end{pmatrix}$. $15 - 5 = 10$. \checkmark

%---------------------------------------------------------------
\subsection*{Exercice 2 — 5 points}
%---------------------------------------------------------------

\baremeIndicatif{Q1 : 2 pts ; Q2 : 2 pts ; Q3 : 1 pt}

\textbf{Question 1.}
$\overrightarrow{AB}\begin{pmatrix} 3 \\ -2 \end{pmatrix}$ et $\overrightarrow{AC}\begin{pmatrix} 2 \\ 3 \end{pmatrix}$.

$\overrightarrow{AB} \cdot \overrightarrow{AC} = 6 - 6 = 0$.

Le triangle est \textbf{rectangle en $A$}.

\textbf{Question 2.}
$\overrightarrow{BA}\begin{pmatrix} -3 \\ 2 \end{pmatrix}$, $\overrightarrow{BC}\begin{pmatrix} -1 \\ 5 \end{pmatrix}$.

$\overrightarrow{BA} \cdot \overrightarrow{BC} = 3 + 10 = 13$, $\|\overrightarrow{BA}\| = \sqrt{13}$, $\|\overrightarrow{BC}\| = \sqrt{26}$.

$\cos\widehat{ABC} = \dfrac{13}{\sqrt{13} \times \sqrt{26}} = \dfrac{13}{13\sqrt{2}} = \dfrac{\sqrt{2}}{2}$.

$\widehat{ABC} = \dfrac{\pi}{4}$.

\textbf{Question 3.}
$\widehat{ACB} = \pi - \dfrac{\pi}{2} - \dfrac{\pi}{4} = \dfrac{\pi}{4}$. Triangle rectangle isocele en $A$.

%---------------------------------------------------------------
\subsection*{Exercice 3 — 5 points}
%---------------------------------------------------------------

\baremeIndicatif{Q1 : 1,5 pt ; Q2 : 2 pts ; Q3 : 1,5 pt}

\textbf{Question 1.}
$I = (4; 0)$, $\overrightarrow{AB}\begin{pmatrix} 8 \\ 0 \end{pmatrix}$. Mediatrice : $8(x - 4) = 0$, soit $x = 4$.

\textbf{Question 2.}
$\overrightarrow{BC}\begin{pmatrix} -5 \\ 6 \end{pmatrix}$. $H = B + t\,\overrightarrow{BC} = (8 - 5t; 6t)$.

$\overrightarrow{AH} \cdot \overrightarrow{BC} = 0$ : $(8-5t)(-5) + (6t)(6) = 0$, soit $-40 + 25t + 36t = 0$, $61t = 40$, $t = \dfrac{40}{61}$.

$H = \left(\dfrac{288}{61}; \dfrac{240}{61}\right)$.

\textbf{Question 3.}
$\mathcal{A} = \dfrac{1}{2} |x_B \cdot y_C - x_C \cdot y_B| = \dfrac{1}{2} |48 - 0| = 24$.

%---------------------------------------------------------------
\subsection*{Exercice 4 — 5 points}
%---------------------------------------------------------------

\baremeIndicatif{Q1 : 2 pts ; Q2 : 1,5 pt ; Q3 : 1,5 pt}

\textbf{Question 1.}
$BC^2 = 36 + 100 - 120\cos\!\left(\dfrac{\pi}{4}\right) = 136 - 60\sqrt{2}$.

$BC = \sqrt{136 - 60\sqrt{2}} \approx 7{,}15$.

\textbf{Question 2.}
$\cos\widehat{ABC} = \dfrac{BC^2 + AB^2 - AC^2}{2 \cdot BC \cdot AB} = \dfrac{136 - 60\sqrt{2} + 36 - 100}{2 \cdot BC \cdot 6} = \dfrac{72 - 60\sqrt{2}}{12\,BC}$.

Numeriquement : $\cos\widehat{ABC} \approx \dfrac{72 - 84{,}85}{12 \times 7{,}15} \approx \dfrac{-12{,}85}{85{,}8} \approx -0{,}150$.

$\widehat{ABC} \approx 98{,}6°$.

\textbf{Question 3.}
$\mathcal{A} = \dfrac{1}{2} \times 6 \times 10 \times \sin\!\left(\dfrac{\pi}{4}\right) = 30 \times \dfrac{\sqrt{2}}{2} = 15\sqrt{2} \approx 21{,}2$.

% BEGIN-VERIFY
% from sympy import Matrix, sqrt, Rational, simplify, cos, sin, pi, symbols, solve, nsimplify
% # ---- Exercice 1 : u(4;2), v(-1;3) ----
% u = Matrix([4, 2]); v = Matrix([-1, 3])
% assert u.dot(v) == 2
% assert simplify(u.norm() - 2*sqrt(5)) == 0
% assert simplify(v.norm() - sqrt(10)) == 0
% assert (u - v).dot(u - v) == 26
% assert (u + v).dot(u - v) == 10
% assert (u + v).dot(u - v) == u.dot(u) - v.dot(v)
% # ---- Exercice 2 : A(0;1), B(3;-1), C(2;4) ----
% A = Matrix([0, 1]); B = Matrix([3, -1]); C = Matrix([2, 4])
% AB = B - A; AC = C - A
% assert AB == Matrix([3, -2]) and AC == Matrix([2, 3])
% assert AB.dot(AC) == 0
% BA = A - B; BC = C - B
% assert BA.dot(BC) == 13
% assert simplify(BA.dot(BC)/(BA.norm()*BC.norm()) - sqrt(2)/2) == 0
% # ---- Exercice 3 : A(0;0), B(8;0), C(3;6) ----
% A = Matrix([0, 0]); B = Matrix([8, 0]); C = Matrix([3, 6])
% I = (A + B)/2
% assert I == Matrix([4, 0])
% t = symbols('t')
% H = B + t*(C - B)
% racine = solve((H - A).dot(C - B), t)
% assert racine == [Rational(40, 61)]
% H = H.subs(t, Rational(40, 61))
% assert H == Matrix([Rational(288, 61), Rational(240, 61)])
% aire = abs((B - A)[0]*(C - A)[1] - (C - A)[0]*(B - A)[1])/2
% assert aire == 24
% # ---- Exercice 4 : AB=6, AC=10, angle A = pi/4 ----
% AB_, AC_ = 6, 10
% BC2 = simplify(AB_**2 + AC_**2 - 2*AB_*AC_*cos(pi/4))
% assert simplify(BC2 - (136 - 60*sqrt(2))) == 0
% assert round(float(sqrt(BC2)), 2) == 7.15
% cosB = simplify((BC2 + AB_**2 - AC_**2)/(2*sqrt(BC2)*AB_))
% assert round(float(cosB), 3) == -0.150
% assert simplify(Rational(1, 2)*AB_*AC_*sin(pi/4) - 15*sqrt(2)) == 0
% END-VERIFY
