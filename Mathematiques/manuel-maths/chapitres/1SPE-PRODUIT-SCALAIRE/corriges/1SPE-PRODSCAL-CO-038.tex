% META: {"id":"1SPE-PRODSCAL-CO-038","chapitre":"1SPE-PRODUIT-SCALAIRE","type_objet":"corrige","exercice_id":"1SPE-PRODSCAL-EX-038","status":"generated"}
% BEGIN-VERIFY
% from sympy import *
% # Circumscribed circle: MA^2 = MB^2 and MA^2 = MC^2
% # A(0,0), B(4,0), C(1,3)
% x, y = symbols('x y')
% MA2 = x**2 + y**2
% MB2 = (x-4)**2 + y**2
% MC2 = (x-1)**2 + (y-3)**2
% eq1 = expand(MA2 - MB2)  # 8x - 16 = 0 => x = 2
% eq2 = expand(MA2 - MC2)  # 2x + 6y - 10 = 0 => x + 3y = 5
% sol = solve([eq1, eq2], [x, y])
% assert sol == {x: 2, y: 1}
% END-VERIFY
\begin{corrige}{1SPE-PRODSCAL-EX-038}
\begin{enumerate}
\item $I_{AB} = (2; 0)$, $\overrightarrow{AB}\begin{pmatrix} 4 \\ 0 \end{pmatrix}$. Mediatrice : $4(x-2) = 0$, soit $x = 2$.
\item $I_{AC} = (1/2; 3/2)$, $\overrightarrow{AC}\begin{pmatrix} 1 \\ 3 \end{pmatrix}$. Mediatrice : $(x-1/2) + 3(y-3/2) = 0$, soit $x + 3y = 5$.
\item $x = 2$ et $2 + 3y = 5$, donc $y = 1$. Le centre est $\Omega(2; 1)$.
\end{enumerate}
\end{corrige}
