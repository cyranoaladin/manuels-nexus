% META: {"id": "TSPE-PRIMITIVES-EQDIFF-QCM", "chapitre": "TSPE-PRIMITIVES-EQDIFF", "type_objet": "qcm", "genere_depuis": "chapitres/TSPE-PRIMITIVES-EQDIFF/qcm/TSPE-PRIMITIVES-EQDIFF-QCM.json", "status": "generated"}
% Fichier genere par scripts/build_qcm_tex.py — ne pas editer a la main.

\section*{\textcolor{chapcolor}{\MakeUppercase{QCM d'auto-évaluation — Primitives et équations différentielles}}}

\begin{center}
\textit{Pour chaque question, une seule réponse est exacte.}
\end{center}

\begin{enumerate}

\item[] \textbf{Capacité C1}

\item \textbf{[Q1]} Une primitive de $f(x) = \mathrm{e}^{3x}$ est :
  \begin{enumerate}[label=\Alph*.]
    \item $3\mathrm{e}^{3x}$.
    \item $\mathrm{e}^{3x}$.
    \item $\dfrac{1}{3}\mathrm{e}^{3x}$.
    \item $\mathrm{e}^{3x+1}$.
  \end{enumerate}

\item[] \textbf{Capacité C4}

\item \textbf{[Q2]} Si $F$ et $G$ sont deux primitives de la même fonction $f$ continue sur un intervalle $I$, alors :
  \begin{enumerate}[label=\Alph*.]
    \item $F=G$.
    \item $F-G$ est une fonction affine de pente non nulle.
    \item $F-G$ est constante.
    \item $F \times G$ est constante.
  \end{enumerate}

\item[] \textbf{Capacité C5}

\item \textbf{[Q3]} Les solutions de $y'=5y$ sur $\mathbb{R}$ sont :
  \begin{enumerate}[label=\Alph*.]
    \item $y(x) = 5x + C$.
    \item $y(x) = C\mathrm{e}^{5x}$.
    \item $y(x) = C\mathrm{e}^{-5x}$.
    \item $y(x) = 5\mathrm{e}^{Cx}$.
  \end{enumerate}

\item[] \textbf{Capacité C2}

\item \textbf{[Q4]} Pour $y' = 3y - 12$, la solution particulière constante est :
  \begin{enumerate}[label=\Alph*.]
    \item $y_0 = 4$.
    \item $y_0 = -4$.
    \item $y_0 = 12$.
    \item $y_0 = -36$.
  \end{enumerate}

\item[] \textbf{Capacité C3}

\item \textbf{[Q5]} Pour utiliser une solution particulière $y_p$ donnee de $y'=ay+f$, il faut d'abord :
  \begin{enumerate}[label=\Alph*.]
    \item calculer $y_p(0)$.
    \item vérifier que $y_p$ satisfait bien l'équation.
    \item dériver $f$.
    \item résoudre $y_p=0$.
  \end{enumerate}

\end{enumerate}

% NEXUS-QCM-TEACHER-ONLY-BEGIN
\ifnxVersionProfesseur
\clearpage
\section*{Clé de correction — réservée au professeur}

\begin{center}
\begin{tabular}{lll}
\hline
Question & Capacité & Réponse exacte \\
\hline
Q1 & C1 & \textbf{C} \\
Q2 & C4 & \textbf{C} \\
Q3 & C5 & \textbf{B} \\
Q4 & C2 & \textbf{A} \\
Q5 & C3 & \textbf{B} \\
\hline
\end{tabular}
\end{center}

\subsection*{Diagnostic des réponses erronées}

\begin{itemize}
  \item \textbf{Q1}
  \begin{itemize}
    \item \textbf{A} — La dérivée de $3\mathrm e^{3x}$ vaut $9\mathrm e^{3x}$ ; ce choix multiplie par $3$ au lieu de compenser la dérivée de $3x$.
    \item \textbf{B} — La dérivée de $\mathrm e^{3x}$ vaut $3\mathrm e^{3x}$ ; il faut donc le facteur $1/3$ pour retrouver exactement $\mathrm e^{3x}$.
    \item \textbf{D} — Comme $\mathrm e^{3x+1}=\mathrm e\,\mathrm e^{3x}$, sa dérivée vaut $3\mathrm e\,\mathrm e^{3x}$ et non $\mathrm e^{3x}$.
  \end{itemize}
  \item \textbf{Q2}
  \begin{itemize}
    \item \textbf{A} — Deux primitives d'une même fonction ne sont pas necessairement égales ; elles peuvent differer d'une constante. \emph{Renvoi : C1, ensemble des primitives.}
    \item \textbf{B} — Puisque $(F-G)'=f-f=0$, la difference est constante : elle ne peut donc pas être affine de pente non nulle. \emph{Renvoi : C1, ensemble des primitives.}
    \item \textbf{D} — Aucune propriété generale ne rend le produit de deux primitives constant. \emph{Renvoi : C1, ensemble des primitives.}
  \end{itemize}
  \item \textbf{Q3}
  \begin{itemize}
    \item \textbf{A} — Pour $y=5x+C$, on a $y'=5$ alors que $5y=25x+5C$ ; l'égalité différentielle n'est donc pas satisfaite en general.
    \item \textbf{C} — La dérivée de $C\mathrm e^{-5x}$ vaut $-5y$ ; cette famille resout $y'=-5y$, et non l'équation $y'=5y$.
    \item \textbf{D} — Pour $y=5\mathrm e^{Cx}$, on obtient $y'=Cy$ ; cette expression ne satisfait $y'=5y$ que pour $C=5$ et ne donne pas toute la famille.
  \end{itemize}
  \item \textbf{Q4}
  \begin{itemize}
    \item \textbf{B} — Pour la constante $y_0=-4$, le membre de gauche vaut $0$ mais $3y_0-12=-24$ ; l'équation n'est pas verifiee.
    \item \textbf{C} — Pour $y_0=12$, on a $3y_0-12=24$ au lieu de $0$ ; cette constante n'est donc pas une solution particulière.
    \item \textbf{D} — Pour $y_0=-36$, le second membre vaut $3(-36)-12=-120$, tandis que la dérivée de la constante vaut $0$.
  \end{itemize}
  \item \textbf{Q5}
  \begin{itemize}
    \item \textbf{A} — La valeur $y_p(0)$ peut servir ensuite avec une condition initiale, mais elle ne prouve pas que $y_p'=ay_p+f$ sur l'intervalle.
    \item \textbf{C} — Dériver $f$ n'est pas la vérification demandee ; il faut calculer $y_p'$ puis comparer cette dérivée a $ay_p+f$.
    \item \textbf{D} — Les zeros de $y_p$ ne disent pas si la fonction satisfait l'équation différentielle ; il faut substituer $y_p$ et $y_p'$ dans celle-ci.
  \end{itemize}
\end{itemize}
\fi
% NEXUS-QCM-TEACHER-ONLY-END
