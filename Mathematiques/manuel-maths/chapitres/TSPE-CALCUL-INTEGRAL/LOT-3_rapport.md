# LOT 3 — Cours TSPE-CALCUL-INTEGRAL
7 fichiers de cours. Demonstration exigible C7 (encadrement par rectangles,
theoreme des gendarmes) et C8 (IPP via derivee d'un produit integree)
redigees et verifiees. Un bug LaTeX trouve dans contrat.yaml lors de la
premiere compilation : le libelle_eleve de C7 contenait des symboles
mathematiques (integrale, indices) hors bloc $...$, provoquant un echec
"Missing $ inserted" — corrige (regle deja documentee dans les conventions
du projet, reappliquee ici). make verify : 0 FAIL apres correctif.
