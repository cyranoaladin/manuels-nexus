% META: {"id": "TSPE-LIMFCT-CR-012", "chapitre": "TSPE-LIMITES-FONCTIONS", "type_objet": "cours", "capacites_codes": ["C3"], "sources_inspiration": [], "status": "approved"}

\section{Croissance comparee de $x^n$ et $\mathrm{e}^x$}

% ============================================================
% STRATE 1 — Théorème et démonstration exigible
% ============================================================

\subsection{Résultat fondamental}

\theoreme{
Pour tout entier naturel $n$ :
\[
  \lim_{x \to +\infty} \frac{\mathrm{e}^x}{x^n} = +\infty.
\]
Autrement dit, l'exponentielle croit plus vite que toute puissance de $x$ en $+\infty$.
}

\subsection{Démonstration exigible}

\demonstration{
\textbf{Étape 1 : cas $n = 1$, c'est-a-dire $\lim_{x \to +\infty} \dfrac{\mathrm{e}^x}{x} = +\infty$.}

\medskip

Pour tout $x \geq 0$, on a l'inégalité $\mathrm{e}^x \geq 1 + x$ (admise, ou obtenue par étude de $g(x) = \mathrm{e}^x - 1 - x$ qui vérifie $g'(x) = \mathrm{e}^x - 1 \geq 0$ pour $x \geq 0$, $g(0) = 0$, donc $g(x) \geq 0$).

Appliquons cette inégalité a $x/2$ : pour tout $x \geq 0$,
\[
  \mathrm{e}^{x/2} \geq 1 + \frac{x}{2}.
\]

Donc :
\[
  \mathrm{e}^x = \left(\mathrm{e}^{x/2}\right)^2 \geq \left(1 + \frac{x}{2}\right)^2 \geq \frac{x^2}{4}.
\]

Pour $x > 0$, on divise par $x$ :
\[
  \frac{\mathrm{e}^x}{x} \geq \frac{x}{4}.
\]

Or $\lim_{x \to +\infty} \dfrac{x}{4} = +\infty$, donc par comparaison :
\[
  \lim_{x \to +\infty} \frac{\mathrm{e}^x}{x} = +\infty.
\]

\medskip

\textbf{Étape 2 : cas general, c'est-a-dire $\lim_{x \to +\infty} \dfrac{\mathrm{e}^x}{x^n} = +\infty$ pour tout $n \geq 1$.}

\medskip

On pose $X = x/n$, soit $x = nX$. Quand $x \to +\infty$, $X \to +\infty$.
\[
  \frac{\mathrm{e}^x}{x^n} = \frac{\mathrm{e}^{nX}}{(nX)^n} = \frac{(\mathrm{e}^X)^n}{n^n \, X^n} = \frac{1}{n^n}\left(\frac{\mathrm{e}^X}{X}\right)^n.
\]

D'après l'étape 1, $\dfrac{\mathrm{e}^X}{X} \to +\infty$ quand $X \to +\infty$.

Donc $\left(\dfrac{\mathrm{e}^X}{X}\right)^n \to +\infty$, et $\dfrac{\mathrm{e}^x}{x^n} \to +\infty$.
}

\subsection{Consequences}

\propriete{
Pour tout entier naturel $n$ :
\[
  \lim_{x \to +\infty} \frac{x^n}{\mathrm{e}^x} = 0 \qquad \text{et} \qquad \lim_{x \to -\infty} x^n \, \mathrm{e}^x = 0.
\]
}

\textit{Justification de la seconde limite.} On pose $X = -x$, soit $x = -X$. Quand $x \to -\infty$, $X \to +\infty$.
\[
  x^n \, \mathrm{e}^x = (-X)^n \, \mathrm{e}^{-X} = (-1)^n \frac{X^n}{\mathrm{e}^X} \to 0
\]
d'après la première limite.

\exemple{
$\lim_{x \to +\infty} x^{100}\, \mathrm{e}^{-x} = \lim_{x \to +\infty} \dfrac{x^{100}}{\mathrm{e}^x} = 0$.
}

\exemple{
$\lim_{x \to +\infty} (x^3 - \mathrm{e}^x)$. On factorise par $\mathrm{e}^x$ :
\[
  x^3 - \mathrm{e}^x = \mathrm{e}^x\left(\frac{x^3}{\mathrm{e}^x} - 1\right).
\]
$\dfrac{x^3}{\mathrm{e}^x} \to 0$ donc le contenu de la parenthese $\to -1$, et $\mathrm{e}^x \to +\infty$. La limite est $-\infty$.
}

\exemple{
$\lim_{x \to -\infty} x^2 \, \mathrm{e}^x = 0$ par croissances comparees.
}

% ============================================================
% STRATE 2 — Marge d'appui et erreurs frequentes
% ============================================================

\margeAppui{
\textbf{Hierarchie des croissances en $+\infty$ (n $\geq$ 2), par ordre croissant :} $\ln x$, puis $x^{1/n}$, puis $x$, puis $x^n$, puis $\mathrm{e}^x$. Chaque fonction croit infiniment plus vite que la précédente. Pour le programme de terminale, on retient surtout $x^n \ll \mathrm{e}^x$.
}

\margeAppui{
\textbf{Memo formes indeterminees avec exponentielle :} $x^n \mathrm{e}^{-x} \to 0$ en $+\infty$ (l'exponentielle l'emporte) ; $x^n \mathrm{e}^x \to 0$ en $-\infty$ (par changement de variable) ; factoriser par $\mathrm{e}^x$ pour ramener a une limite connue.
}

\erreurFrequente{
\textbf{Appliquer les croissances comparees en $-\infty$ sans changement de variable.}

La limite $\lim_{x \to +\infty} \dfrac{\mathrm{e}^x}{x^n} = +\infty$ est un résultat en $+\infty$. En $-\infty$, on pose $X = -x$ ou on utilise la consequence $\lim_{x \to -\infty} x^n\,\mathrm{e}^x = 0$.
}

\erreurFrequente{
\textbf{Écrire $\mathrm{e}^x - x^n = \mathrm{e}^x$ « car l'exponentielle domine ».}

Les croissances comparees donnent le comportement du \emph{rapport}, pas de la difference. Pour la difference $\mathrm{e}^x - x^n$, il faut factoriser par $\mathrm{e}^x$ et utiliser $\dfrac{x^n}{\mathrm{e}^x} \to 0$.
}

% BEGIN-VERIFY
% from sympy import symbols, exp, limit, oo, Rational, diff, simplify
%
% x = symbols('x', positive=True)
%
% # Le theoreme, pour plusieurs exposants.
% for n in range(0, 6):
%     assert limit(exp(x)/x**n, x, oo) == oo
%     assert limit(x**n/exp(x), x, oo) == 0
%
% # L'inegalite auxiliaire e^x >= 1 + x pour x >= 0, et sa preuve : g'(x) >= 0
% # et g(0) = 0.
% g = exp(x) - 1 - x
% assert simplify(diff(g, x) - (exp(x) - 1)) == 0
% assert g.subs(x, 0) == 0
% for essai in (Rational(1, 10), 1, 3, 10):
%     assert (exp(essai) - 1).evalf() >= 0
%     assert g.subs(x, essai).evalf() >= 0
%
% # Etape 1 du cours : e^x / x tend vers l'infini.
% assert limit(exp(x)/x, x, oo) == oo
% # Verification numerique de la domination, sur des valeurs croissantes.
% valeurs = [(t, float(exp(t)/t**3)) for t in (5, 10, 20, 40)]
% assert [v for _, v in valeurs] == sorted(v for _, v in valeurs)
% assert valeurs[-1][1] > 10**6
% END-VERIFY
