% META: {"id": "TSPE-LIMFCT-CO-027", "chapitre": "TSPE-LIMITES-FONCTIONS", "type_objet": "corrige", "exercice_ref": "TSPE-LIMFCT-EX-027", "capacites_codes": ["C2"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% assert limit((x + 1)/((x - 1)*(x + 3)), x, oo) == 0
% END-VERIFY
\begin{corrige}{TSPE-LIMFCT-EX-027}

\textbf{1.} $D_f = \mathbb{R} \setminus \{-3, 1\}$.

\textbf{2.} $\lim_{\pm\infty} f = 0$ : AH $y = 0$.

En $1$ : num $\to 2 > 0$, $(x-1) \to 0$, $(x+3) \to 4 > 0$. $\lim_{1^+} = +\infty$, $\lim_{1^-} = -\infty$. AV $x = 1$.

En $-3$ : num $\to -2 < 0$, $(x+3) \to 0$, $(x-1) \to -4 < 0$. $\lim_{-3^+} = +\infty$, $\lim_{-3^-} = -\infty$. AV $x = -3$.

\end{corrige}
