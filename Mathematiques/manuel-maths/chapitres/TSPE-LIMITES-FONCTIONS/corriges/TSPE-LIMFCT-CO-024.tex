% META: {"id": "TSPE-LIMFCT-CO-024", "chapitre": "TSPE-LIMITES-FONCTIONS", "type_objet": "corrige", "exercice_ref": "TSPE-LIMFCT-EX-024", "capacites_codes": ["C2"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% assert limit((2*x - 1)/(x + 1), x, oo) == 2
% assert simplify((2*x-1)/(x+1) - (2 - 3/(x+1))) == 0
% END-VERIFY
\begin{corrige}{TSPE-LIMFCT-EX-024}

\textbf{1.} $\lim_{\pm\infty} f = 2$ : AH $y = 2$. $\lim_{-1^+} = -\infty$, $\lim_{-1^-} = +\infty$ : AV $x = -1$.

\textbf{2.} $\dfrac{2x-1}{x+1} = \dfrac{2(x+1)-3}{x+1} = 2 - \dfrac{3}{x+1}$.

\textbf{3.} $f(x)-2 = -\dfrac{3}{x+1}$. Si $x > -1$ : $f(x)-2 < 0$, courbe sous l'AH. Si $x < -1$ : $f(x)-2 > 0$, courbe au-dessus.

\end{corrige}
