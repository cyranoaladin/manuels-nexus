% META: {"id": "TSPE-LIMFCT-CO-033", "chapitre": "TSPE-LIMITES-FONCTIONS", "type_objet": "corrige", "exercice_ref": "TSPE-LIMFCT-EX-033", "capacites_codes": ["C2"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% q, r = div(2*x**2 + x - 1, x + 2, x)
% assert q == 2*x - 3
% assert r == 5
% END-VERIFY
\begin{corrige}{TSPE-LIMFCT-EX-033}

\textbf{1.} Valeur interdite : $x = -2$. Numerateur en $-2$ : $8-2-1 = 5 > 0$. $(x+2) \to 0$. AV $x = -2$.

\textbf{2.} $2x^2+x-1 = (x+2)(2x-3) + 5$. $f(x) = 2x - 3 + \dfrac{5}{x+2}$. Asymptote oblique : $y = 2x - 3$.

\textbf{3.} $f'(x) = 2 - \dfrac{5}{(x+2)^2} = \dfrac{2(x+2)^2-5}{(x+2)^2}$. $f'(x) = 0 \iff (x+2)^2 = 5/2 \iff x = -2 \pm \sqrt{5/2}$.

\end{corrige}
