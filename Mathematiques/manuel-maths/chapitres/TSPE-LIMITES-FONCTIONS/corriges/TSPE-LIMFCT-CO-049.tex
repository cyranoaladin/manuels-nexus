% META: {"id": "TSPE-LIMFCT-CO-049", "chapitre": "TSPE-LIMITES-FONCTIONS", "type_objet": "corrige", "exercice_ref": "TSPE-LIMFCT-EX-049", "capacites_codes": ["C3"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% assert limit((x**2 - 2*x + 3)*exp(-x), x, oo) == 0
% # discriminant de -x^2+4x-5 = 16-20 = -4 < 0
% assert 16 - 20 == -4
% END-VERIFY
\begin{corrige}{TSPE-LIMFCT-EX-049}

\textbf{1.} $\lim_{+\infty} = 0$ (croissances comparees : $x^2/\mathrm{e}^x \to 0$). AH $y = 0$. $\lim_{-\infty} = +\infty$.

\textbf{2.} $f'(x) = (2x-2)\mathrm{e}^{-x} + (x^2-2x+3)(-\mathrm{e}^{-x}) = (2x-2-x^2+2x-3)\mathrm{e}^{-x} = (-x^2+4x-5)\mathrm{e}^{-x}$.

\textbf{3.} $\Delta = 16-20 = -4 < 0$, coefficient de $x^2$ negatif. Donc $-x^2+4x-5 < 0$ pour tout $x$. $f'(x) < 0$ pour tout $x$.

\textbf{4.} $f$ strictement decroissante sur $\mathbb{R}$, de $+\infty$ a $0$.

\end{corrige}
