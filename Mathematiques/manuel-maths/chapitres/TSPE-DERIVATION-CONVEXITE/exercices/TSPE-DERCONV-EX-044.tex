% META: {"id": "TSPE-DERCONV-EX-044", "chapitre": "TSPE-DERIVATION-CONVEXITE", "type_objet": "exercice", "capacites": ["TSPE-DERCONV-C6"], "capacites_codes": ["C6"], "methodes": ["M3"], "parcours": 1, "competences": ["raisonner"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/exercices/TSPE-DERCONV-EX-044.tex", "corrige_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/corriges/TSPE-DERCONV-CO-044.tex", "status": "approved"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = exp(x)
% # tangente en 0: y = 1 + x
% # f convexe => f >= tangente => e^x >= 1 + x
% g = exp(x) - 1 - x
% gpp = diff(g, x, 2)
% assert gpp == exp(x)  # > 0, convexe
% assert g.subs(x, 0) == 0
% assert diff(g, x).subs(x, 0) == 0
% assert g.subs(x, 2) == exp(2) - 3 > 0
% END-VERIFY
\begin{exercice}{TSPE-DERCONV-EX-044}{1}{15}
En utilisant la convexite de $f(x) = \mathrm{e}^x$, demontrer que $\mathrm{e}^x \geq 1 + x$ pour tout $x \in \mathbb{R}$.

\end{exercice}
