% META: {"id": "TSPE-DERCONV-EX-043", "chapitre": "TSPE-DERIVATION-CONVEXITE", "type_objet": "exercice", "capacites": ["TSPE-DERCONV-C6"], "capacites_codes": ["C6"], "methodes": ["M3"], "parcours": 1, "competences": ["raisonner"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/exercices/TSPE-DERCONV-EX-043.tex", "corrige_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/corriges/TSPE-DERCONV-CO-043.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = x**2
% fp = diff(f, x)
% assert fp.subs(x, 1) == 2
% # tangente en 1: y = 2(x-1)+1 = 2x-1
% # f(x) - (2x-1) = x^2 - 2x + 1 = (x-1)^2 >= 0
% g = f - (2*x - 1)
% assert expand(g - (x-1)**2) == 0
% assert g.subs(x, 1) == 0
% assert g.subs(x, 0) == 1
% assert g.subs(x, 2) == 1
% END-VERIFY
\begin{exercice}{TSPE-DERCONV-EX-043}{1}{15}
Soit $f(x) = x^2$.
\begin{enumerate}
  \item Determiner la tangente a la courbe en $x = 1$.
  \item Montrer que la courbe est au-dessus de cette tangente.
\end{enumerate}

\end{exercice}
