% META: {"id": "TSPE-DERCONV-EX-040", "chapitre": "TSPE-DERIVATION-CONVEXITE", "type_objet": "exercice", "capacites": ["TSPE-DERCONV-C5"], "capacites_codes": ["C5"], "methodes": ["M4"], "parcours": 2, "competences": ["raisonner"], "duree_min": 18, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/exercices/TSPE-DERCONV-EX-040.tex", "corrige_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/corriges/TSPE-DERCONV-CO-040.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% # f(x) = sqrt(1+x), tangente en 0: f(0)=1, f'(0)=1/2, y=1+x/2
% f = sqrt(1 + x)
% fp = diff(f, x)
% assert fp.subs(x, 0) == Rational(1, 2)
% # f concave: f'' < 0
% fpp = diff(f, x, 2)
% assert fpp == -1/(4*(1+x)**Rational(3,2))
% assert fpp.subs(x, 0) == -Rational(1,4)  # concave
% # approximation: sqrt(1.1) ~ 1.05
% approx = 1 + Rational(1,20)  # 1 + 0.1/2
% assert approx == Rational(21, 20)
% exact = f.subs(x, Rational(1,10))
% assert exact == sqrt(Rational(11,10))
% # concave => courbe sous la tangente => approx >= exact
% assert approx > float(exact)
% END-VERIFY
\begin{exercice}{TSPE-DERCONV-EX-040}{2}{18}
Soit $f(x) = \sqrt{1+x}$.
\begin{enumerate}
  \item Determiner la tangente en $0$ et l'utiliser comme approximation de $\sqrt{1{,}1}$.
  \item L'approximation est-elle par exces ou par defaut ? Justifier.
\end{enumerate}

\end{exercice}
