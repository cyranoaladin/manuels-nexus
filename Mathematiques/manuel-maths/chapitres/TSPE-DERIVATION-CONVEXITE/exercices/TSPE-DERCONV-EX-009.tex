% META: {"id": "TSPE-DERCONV-EX-009", "chapitre": "TSPE-DERIVATION-CONVEXITE", "type_objet": "exercice", "capacites": ["TSPE-DERCONV-C1"], "capacites_codes": ["C1"], "methodes": ["M1","M5"], "parcours": 3, "competences": ["modeliser","raisonner"], "duree_min": 20, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/exercices/TSPE-DERCONV-EX-009.tex", "corrige_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/corriges/TSPE-DERCONV-CO-009.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = x*sqrt(4 - x**2)
% fp = diff(f, x)
% crit = solve(fp, x)
% assert sqrt(2) in crit
% fmax = f.subs(x, sqrt(2))
% assert simplify(fmax) == 2
% assert f.subs(x, 0) == 0
% assert f.subs(x, 2) == 0
% END-VERIFY
\begin{exercice}{TSPE-DERCONV-EX-009}{3}{20}
On considere la fonction $f$ definie sur $[0,\, 2]$ par $f(x) = x\sqrt{4 - x^2}$.
\begin{enumerate}
  \item Justifier que $f$ est bien definie sur $[0,\, 2]$.
  \item Calculer $f'(x)$ et determiner les variations de $f$.
  \item En deduire le maximum de $f$ sur $[0,\, 2]$ et la valeur de $x$ pour laquelle il est atteint.
\end{enumerate}

\end{exercice}
