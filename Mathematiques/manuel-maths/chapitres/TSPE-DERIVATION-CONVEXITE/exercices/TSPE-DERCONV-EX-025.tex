% META: {"id": "TSPE-DERCONV-EX-025", "chapitre": "TSPE-DERIVATION-CONVEXITE", "type_objet": "exercice", "capacites": ["TSPE-DERCONV-C3"], "capacites_codes": ["C3"], "methodes": ["M3"], "parcours": 2, "competences": ["raisonner"], "duree_min": 20, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/exercices/TSPE-DERCONV-EX-025.tex", "corrige_tex": "chapitres/TSPE-DERIVATION-CONVEXITE/corriges/TSPE-DERCONV-CO-025.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% a, b = symbols('a b', positive=True)
% # ln concave => ln((a+b)/2) >= (ln a + ln b)/2 = ln(sqrt(ab))
% lhs = ln((a+b)/2)
% rhs = (ln(a) + ln(b))/2
% assert simplify(rhs - ln(sqrt(a*b))) == 0
% # numerique
% assert simplify((lhs - rhs).subs({a: 1, b: 4}) - (ln(Rational(5,2)) - ln(2))) == 0
% assert lhs.subs({a: 1, b: 4}) == ln(Rational(5,2))
% assert simplify(rhs.subs({a: 1, b: 4}) - ln(2)) == 0
% assert ln(Rational(5,2)) > ln(2)
% END-VERIFY
\begin{exercice}{TSPE-DERCONV-EX-025}{2}{20}
En utilisant la concavite de la fonction $\ln$, demontrer l'inegalite arithmetico-geometrique :
\[ \frac{a+b}{2} \geq \sqrt{ab} \quad (a > 0,\; b > 0). \]

\end{exercice}
