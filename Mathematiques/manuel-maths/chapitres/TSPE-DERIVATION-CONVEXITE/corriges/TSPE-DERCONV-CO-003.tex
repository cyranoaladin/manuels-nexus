% META: {"id": "TSPE-DERCONV-CO-003", "chapitre": "TSPE-DERIVATION-CONVEXITE", "type_objet": "corrige", "exercice_ref": "TSPE-DERCONV-EX-003", "capacites_codes": ["C1"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = (2*x + 1)**5
% fp = diff(f, x)
% assert fp == 10*(2*x+1)**4
% assert fp.subs(x, 0) == 10
% assert fp.subs(x, Rational(-1,2)) == 0
% END-VERIFY
\begin{corrige}{TSPE-DERCONV-EX-003}

On pose $u(x) = 2x+1$. Alors $u'(x) = 2$.

\commentaireMarge{Identifier $u(x) = 2x+1$ puis appliquer $(u^n)' = n\,u'\,u^{n-1}$.}

La formule $(u^n)' = n\,u'\,u^{n-1}$ donne :
\[ f'(x) = 5 \times 2 \times (2x+1)^4 = 10(2x+1)^4. \]

On verifie : $f'(0) = 10$ et $f'\!\left(-\tfrac{1}{2}\right) = 0$.

\end{corrige}
