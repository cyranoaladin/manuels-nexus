% META: {"id": "1SPE-VARALEA-CO-026", "chapitre": "1SPE-VARIABLES-ALEATOIRES", "type_objet": "corrige", "exercice_ref": "1SPE-VARALEA-EX-026", "capacites_codes": [], "extension_codes": ["X1"], "programme_alignment": "OPTIONAL_EXTENSION", "extension_label": "Approfondissement — Vers la Terminale", "sources_inspiration": [], "status": "generated"}
\begin{center}\textbf{Approfondissement — Vers la Terminale}\end{center}
% BEGIN-VERIFY
% from sympy import binomial, Rational, nsimplify
% # X ~ B(10, 1/20) : tirages independants, meme probabilite
% p, n = Rational(1, 20), 10
% P0 = binomial(n, 0) * p**0 * (1 - p)**n
% P1 = binomial(n, 1) * p**1 * (1 - p)**(n - 1)
% assert P0 == Rational(19, 20)**10
% assert P1 == 10 * Rational(1, 20) * Rational(19, 20)**9
% # 2. et 3. valeurs annoncees au corrige, arrondies au millieme
% assert abs(float(P0) - 0.599) < 5e-4
% assert abs(float(P1) - 0.315) < 5e-4
% # 4. au plus une piece defectueuse
% Ple1 = P0 + P1
% assert abs(float(Ple1) - 0.914) < 5e-4
% # controle de coherence : la loi somme bien a 1
% total = sum(binomial(n, k) * p**k * (1 - p)**(n - k) for k in range(n + 1))
% assert total == 1
% END-VERIFY
\begin{corrige}{1SPE-VARALEA-EX-026}

\textbf{1.} La production est supposée assez grande pour que les tirages soient indépendants. Chaque pièce est défectueuse avec $p = 0{,}05$. Donc $X \sim \mathcal{B}(10, 0{,}05)$.

\medskip
\textbf{2.} $P(X=0) = (0{,}95)^{10} \approx 0{,}599$.

\medskip
\textbf{3.} $P(X=1) = \binom{10}{1}(0{,}05)(0{,}95)^9 = 10 \times 0{,}05 \times (0{,}95)^9 \approx 0{,}315$.

\medskip
\textbf{4.} $P(X \leq 1) \approx 0{,}599 + 0{,}315 = 0{,}914$. Il y a environ $91{,}4\,\%$ de chances d'avoir au plus une pièce défectueuse dans l'échantillon.

\end{corrige}
