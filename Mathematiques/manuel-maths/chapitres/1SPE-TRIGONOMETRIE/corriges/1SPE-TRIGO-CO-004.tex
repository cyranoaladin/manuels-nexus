% META: {"id": "1SPE-TRIGO-CO-004", "chapitre": "1SPE-TRIGONOMETRIE", "type_objet": "corrige", "exercice_ref": "1SPE-TRIGO-EX-004", "capacites_codes": ["C1"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import *
% assert cos(pi/6) == sqrt(3)/2
% assert sin(pi/6) == Rational(1,2)
% assert cos(2*pi/3) == Rational(-1,2)
% assert sin(2*pi/3) == sqrt(3)/2
% assert cos(-pi/4) == sqrt(2)/2
% assert sin(-pi/4) == -sqrt(2)/2
% assert cos(5*pi/4) == -sqrt(2)/2
% assert sin(5*pi/4) == -sqrt(2)/2
% END-VERIFY
\begin{corrige}{1SPE-TRIGO-EX-004}

\commentaireMarge{Le point associe a $t$ a pour coordonnees $(\cos t;\sin t)$.}

\textbf{1.} $M_1$ associe a $\dfrac{\pi}{6}$ : $M_1 = \left(\cos\dfrac{\pi}{6}\,;\,\sin\dfrac{\pi}{6}\right) = \left(\dfrac{\sqrt{3}}{2}\,;\,\dfrac{1}{2}\right)$.

\textbf{2.} $M_2$ associe a $\dfrac{2\pi}{3} = \pi - \dfrac{\pi}{3}$ : $M_2 = \left(-\cos\dfrac{\pi}{3}\,;\,\sin\dfrac{\pi}{3}\right) = \left(-\dfrac{1}{2}\,;\,\dfrac{\sqrt{3}}{2}\right)$.

\textbf{3.} $M_3$ associe a $-\dfrac{\pi}{4}$ : $M_3 = \left(\cos\dfrac{\pi}{4}\,;\,-\sin\dfrac{\pi}{4}\right) = \left(\dfrac{\sqrt{2}}{2}\,;\,-\dfrac{\sqrt{2}}{2}\right)$.

\textbf{4.} $M_4$ associe a $\dfrac{5\pi}{4} = \pi + \dfrac{\pi}{4}$ : $M_4 = \left(-\cos\dfrac{\pi}{4}\,;\,-\sin\dfrac{\pi}{4}\right) = \left(-\dfrac{\sqrt{2}}{2}\,;\,-\dfrac{\sqrt{2}}{2}\right)$.

\end{corrige}
