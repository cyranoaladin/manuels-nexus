% META: {"id":"1SPE-TRIGO-TD-CONTEXTUALISE","chapitre":"1SPE-TRIGONOMETRIE","type_objet":"td","capacites_codes":["C1","C2"],"status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}

%---------------------------------------------------------------
% TD CONTEXTUALISE — Hauteur sur une grande roue
%---------------------------------------------------------------

\section*{TD contextualise — La grande roue}

\subsection*{Mise en situation}

Une grande roue de fete foraine a un rayon de $25$~m et son centre est situe a $27$~m du sol. Un passager monte dans une nacelle au point le plus bas de la roue. On note $\theta$ l'angle (en radians) parcouru depuis ce point le plus bas, dans le sens trigonométrique.

La hauteur $h(\theta)$ du passager par rapport au sol est modélisée par :
\[
  h(\theta) = 27 - 25\cos\theta.
\]

\bigskip

\subsection*{Partie A — Premieres observations}

\begin{exercice}{1SPE-TRIGO-TD-CTX-A}{1}{10}
\begin{enumerate}
  \item Calculer $h(0)$. Quelle est la hauteur du passager au depart ?
  \item Calculer $h(\pi)$. Quelle est la hauteur du passager au point le plus haut ?
  \item Calculer $h\!\left(\dfrac{\pi}{3}\right)$. Donner la valeur exacte puis une valeur approchee au dixieme.
  \item Calculer $h\!\left(\dfrac{3\pi}{2}\right)$ et interpréter cette valeur.
\end{enumerate}
\end{exercice}

% BEGIN-VERIFY
% from sympy import *
% theta = symbols('theta', real=True)
% h = 27 - 25*cos(theta)
% assert h.subs(theta, 0) == 2
% assert h.subs(theta, pi) == 52
% assert simplify(h.subs(theta, pi/3) - Rational(29, 2)) == 0
% assert h.subs(theta, 3*pi/2) == 27
% END-VERIFY

\subsection*{Partie B — Lire des positions sur le cercle}

\begin{exercice}{1SPE-TRIGO-TD-CTX-B}{2}{15}
\begin{enumerate}
  \item Pour $\theta=\dfrac{\pi}{2}$, $\pi$, $\dfrac{3\pi}{2}$ puis $2\pi$,
    placer la nacelle sur un cercle et lire la valeur de $\cos\theta$. En déduire
    sa hauteur dans chaque position.
  \item La nacelle a parcouru un angle de $\dfrac{2\pi}{3}$. Quelle fraction de
    tour cela représente-t-il ? Calculer alors sa hauteur.
  \item La grande roue fait un tour complet en $3$~minutes. Déterminer l'angle
    parcouru, puis la hauteur de la nacelle, après $0{,}75$~min, $1{,}5$~min,
    $2{,}25$~min et $3$~min.
\end{enumerate}
\end{exercice}

% BEGIN-VERIFY
% from sympy import *
% theta = symbols('theta', real=True)
% for angle, height in [(pi/2, 27), (pi, 52), (3*pi/2, 27), (2*pi, 2)]:
%     assert h.subs(theta, angle) == height
% assert Rational(2, 3)*pi/(2*pi) == Rational(1, 3)
% assert h.subs(theta, 2*pi/3) == Rational(79, 2)
% t = symbols('t', real=True)
% theta_t = 2*pi*t/3
% h_t = 27 - 25*cos(theta_t)
% expected = [(Rational(3,4), pi/2, 27), (Rational(3,2), pi, 52),
%             (Rational(9,4), 3*pi/2, 27), (3, 2*pi, 2)]
% for instant, angle, height in expected:
%     assert simplify(theta_t.subs(t, instant) - angle) == 0
%     assert h_t.subs(t, instant) == height
% assert simplify(h_t.subs(t, 3) - 2) == 0
% END-VERIFY
