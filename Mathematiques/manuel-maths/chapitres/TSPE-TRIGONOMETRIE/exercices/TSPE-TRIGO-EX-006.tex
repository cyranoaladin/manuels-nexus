% META: {"id": "TSPE-TRIGO-EX-006", "chapitre": "TSPE-TRIGONOMETRIE", "type_objet": "exercice", "capacites": ["TSPE-TRIGO-C1", "TSPE-TRIGO-C2"], "capacites_codes": ["C1", "C2"], "methodes": ["M1", "M2"], "parcours": 3, "competences": ["raisonner", "calculer", "chercher"], "duree_min": 20, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TSPE-TRIGONOMETRIE/exercices/TSPE-TRIGO-EX-006.tex", "corrige_tex": "chapitres/TSPE-TRIGONOMETRIE/corriges/TSPE-TRIGO-CO-006.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import symbols, sin, cos, diff, pi, sqrt, simplify, solve, Eq
% x = symbols('x')
% assert simplify(cos(3*pi/4) + sqrt(2)/2) == 0
% f = cos(x) - sin(x)
% fp = diff(f, x)
% assert fp == -sin(x) - cos(x)
% crit = solve(Eq(fp, 0), x)
% assert -pi/4 in crit or 3*pi/4 in crit
% assert f.subs(x, 3*pi/4) == -sqrt(2)
% END-VERIFY
\begin{exercice}{TSPE-TRIGO-EX-006}{3}{20}
Soit $f(x) = \cos x - \sin x$ sur $[-\pi, \pi]$.
\begin{enumerate}
  \item Calculer $f'(x)$, et resoudre $f'(x) = 0$ sur $[-\pi,\pi]$ (on cherchera les valeurs de $x$ pour lesquelles $\cos x = -\sin x$, c'est-a-dire $\tan x = -1$ ; on admettra que les solutions sont $x = -\dfrac{\pi}{4}$ et $x = \dfrac{3\pi}{4}$).
  \item Calculer $f\!\left(-\dfrac{\pi}{4}\right)$ et $f\!\left(\dfrac{3\pi}{4}\right)$.
  \item En admettant que $f$ est croissante sur $\left[-\dfrac{\pi}{4}, \dfrac{3\pi}{4}\right]$ et decroissante en dehors (sur $\left[-\pi,-\dfrac{\pi}{4}\right]$ et $\left[\dfrac{3\pi}{4},\pi\right]$), dresser le tableau de variations de $f$ sur $[-\pi,\pi]$ et donner le minimum et le maximum de $f$.
\end{enumerate}
\end{exercice}
