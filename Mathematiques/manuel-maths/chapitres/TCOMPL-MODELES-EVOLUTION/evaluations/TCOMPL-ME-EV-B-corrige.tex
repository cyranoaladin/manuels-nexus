% META: {"id": "TCOMPL-ME-EV-B-corrige", "chapitre": "TCOMPL-MODELES-EVOLUTION", "type_objet": "corrige_evaluation", "evaluation_ref": "TCOMPL-ME-EV-B", "capacites_codes": ["C3", "C5", "C6"], "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import symbols, exp, diff, Rational, limit, oo, solve, Eq
% x = symbols('x')
% assert solve(Eq(Rational(1,3)*x + 4, x), x) == [6]
% u0 = 1
% u1 = Rational(1,3)*u0 + 4
% u2 = Rational(1,3)*u1 + 4
% assert u1 == Rational(13, 3)
% assert u2 == Rational(49, 9)
%
% t = symbols('t')
% y = -80*exp(-Rational(1,3)*t) + 100
% yp = diff(y, t)
% assert (yp - (-Rational(1,3)*y + Rational(100,3))).simplify() == 0
% assert y.subs(t, 0) == 20
% assert limit(y, t, oo) == 100
%
% # Encadrement admis puis theoreme des gendarmes.
% n = symbols('n', positive=True, integer=True)
% ecart = 5 * Rational(1, 3)**n
% assert limit(ecart, n, oo) == 0
% assert limit(6 - ecart, n, oo) == 6
% assert limit(6 + ecart, n, oo) == 6
% # L'encadrement admis est bien verifie par les premiers termes.
% suite = [Rational(1)]
% for _ in range(6):
%     suite.append(Rational(1, 3) * suite[-1] + 4)
% for rang, terme in enumerate(suite):
%     assert abs(terme - 6) <= 5 * Rational(1, 3)**rang
% END-VERIFY
\begin{corrige}{TCOMPL-ME-EV-B-EX1}
\textbf{1.} $\dfrac{1}{3}x+4 = x \iff 4 = x - \dfrac{1}{3}x = \dfrac{2}{3}x \iff x=6$. Le point fixe est $x=6$.

\textbf{2.} $u_1 = \dfrac{1}{3}\times1+4 = \dfrac{13}{3} \approx 4{,}33$. $u_2 = \dfrac{1}{3}\times\dfrac{13}{3}+4 = \dfrac{49}{9} \approx 5{,}44$. Les termes se rapprochent progressivement de $6$, confirmant la conjecture graphique.
\end{corrige}

\begin{corrige}{TCOMPL-ME-EV-B-EX2}
\textbf{1.} $y_p$ vérifie $-\dfrac{1}{3}y_p + \dfrac{100}{3} = 0$, soit $y_p = 100$.

\textbf{2.} Solution générale : $y(t) = C\,\mathrm{e}^{-t/3} + 100$. Avec $y(0)=20$ : $C+100=20$, soit $C=-80$ :
\[
  y(t) = -80\,\mathrm{e}^{-t/3} + 100.
\]

\textbf{3.} Quand $t\to+\infty$, $\mathrm{e}^{-t/3}\to0$, donc $y(t)\to100$ : la quantité limite de médicament dans le sang est de $100$ mg.
\end{corrige}

\begin{corrige}{TCOMPL-ME-EV-B-EX3}
\textbf{1.} L'inégalité $\left|u_n-6\right|\leq5\left(\frac13\right)^n$ équivaut à
\[
  6-5\left(\frac13\right)^n \;\leq\; u_n \;\leq\; 6+5\left(\frac13\right)^n .
\]

\textbf{2.} Comme $0<\frac13<1$, la suite géométrique $\left(\frac13\right)^n$ tend vers $0$ ; les deux suites encadrantes tendent donc toutes deux vers $6$. D'après le \textbf{théorème des gendarmes}, $(u_n)$ converge et $\lim\limits_{n\to+\infty}u_n=6$. On retrouve le point fixe de l'exercice $1$, ce que la construction graphique laissait conjecturer.
\end{corrige}
