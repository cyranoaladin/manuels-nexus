% META: {"id": "TCOMPL-BAYES-CO-008", "chapitre": "TCOMPL-INFERENCE-BAYESIENNE", "type_objet": "corrige", "exercice_ref": "TCOMPL-BAYES-EX-008", "capacites_codes": ["C4"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import Rational, N
% vp, fn, fp, vn = 18, 2, 39, 941
% assert vp + fn + fp + vn == 1000
% sensibilite = Rational(vp, vp + fn)
% specificite = Rational(vn, vn + fp)
% vpp = Rational(vp, vp + fp)
% vpn = Rational(vn, vn + fn)
% assert sensibilite == Rational(9, 10)
% assert abs(float(N(specificite)) - 0.9602) < 0.001
% assert abs(float(N(vpp)) - 0.3158) < 0.001
% assert abs(float(N(vpn)) - 0.9979) < 0.001
% END-VERIFY
\begin{corrige}{TCOMPL-BAYES-EX-008}
\textbf{1.} Sensibilité $=\dfrac{18}{20}=0{,}9$ ; spécificité
$=\dfrac{941}{980}\approx0{,}960$.

\textbf{2.} Valeur prédictive positive $=\dfrac{18}{57}\approx0{,}316$ ;
valeur prédictive négative $=\dfrac{941}{943}\approx0{,}998$.

\textbf{3.} Sensibilité $=P_M(T^+)$, spécificité
$=P_{\overline M}(T^-)$, valeur prédictive positive $=P_{T^+}(M)$, valeur
prédictive négative $=P_{T^-}(\overline M)$. Les deux premières conditionnent
par l'état de santé, les deux dernières par le résultat du test.
\end{corrige}
