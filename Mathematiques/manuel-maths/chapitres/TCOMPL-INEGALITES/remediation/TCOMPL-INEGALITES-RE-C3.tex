% META: {"id": "TCOMPL-INEGALITES-RE-C3", "chapitre": "TCOMPL-INEGALITES", "type_objet": "remediation", "capacites_codes": ["C3"], "status": "generated"}

%---------------------------------------------------------------
% FICHE DE REMEDIATION — C3 : oubli du facteur 2 dans la formule de Gini
%---------------------------------------------------------------

\begin{exercice}{TCOMPL-INEG-RE-C3-EX1}{1}{10}
Pour $L(x)=x^2$, un élève calcule $\displaystyle\int_0^1 L(x)\,\mathrm{d}x = \dfrac{1}{3}$, puis conclut directement $G = 1-\dfrac{1}{3} = \dfrac{2}{3}$. Vérifier si ce résultat est cohérent avec le fait que $L(x)=x^2$ n'est pas très éloignée de la bissectrice (comparer avec $G=0$ pour l'égalité parfaite et $G$ proche de $1$ pour une forte inégalité). Corriger le calcul.
\end{exercice}

% BEGIN-VERIFY
% from sympy import symbols, integrate, Rational
% x = symbols('x')
% L = x**2
% I = integrate(L, (x, 0, 1))
% assert I == Rational(1, 3)
% G_correct = 1 - 2*I
% assert G_correct == Rational(1, 3)
% G_faux = 1 - I
% assert G_faux == Rational(2, 3)
% assert G_correct < G_faux
% END-VERIFY

\begin{corrige}{TCOMPL-INEG-RE-C3-EX1}
La formule correcte est $G = 1 - 2\displaystyle\int_0^1 L(x)\,\mathrm{d}x$ (le facteur $2$ a été oublié). Le bon calcul donne $G = 1-2\times\dfrac{1}{3} = \dfrac{1}{3} \approx 0{,}33$, et non $\dfrac{2}{3}\approx0{,}67$ obtenu par erreur. Le résultat erroné $\dfrac{2}{3}$ correspondrait à une répartition très inégalitaire, ce qui est incohérent pour un modèle aussi proche de la bissectrice que $L(x)=x^2$ : cette incohérence aurait dû alerter l'élève sur une erreur de calcul.
\end{corrige}
