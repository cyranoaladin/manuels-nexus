% META: {"id": "1SPE-PROBCOND-ME-001", "chapitre": "1SPE-PROBA-COND", "type_objet": "methode", "capacites_codes": ["C1"], "methodes": ["M1"], "sources_inspiration": [], "status": "generated"}
\begin{fichemethode}{M1}{Calculer une probabilité conditionnelle}

\quandUtiliser{%
  Utiliser cette méthode lorsque l'énoncé demande de calculer << la probabilité de $B$ sachant $A$ >>,
  ou lorsqu'on connait $P(A \cap B)$ et $P(A)$ et qu'on cherche $P_A(B)$.
}

\pasApas{%
  \begin{enumerate}
    \item J'identifie les événements $A$ et $B$ a partir de l'énoncé.
    \item Je déterminé $P(A)$ et $P(A \cap B)$ (par lecture directe, arbre ou tableau).
    \item Je vérifie que $P(A) > 0$ (condition d'existence).
    \item J'applique la formule : $P_A(B) = \dfrac{P(A \cap B)}{P(A)}$.
    \item Je simplifie la fraction si possible.
  \end{enumerate}
}

\exempleRedige{%
  Dans un groupe de $200$ eleves, $120$ sont des filles. Parmi les filles, $48$ portent des lunettes.
  On tire un élève au hasard. Calculer la probabilité de porter des lunettes sachant que l'élève est une fille.

  \medskip
  \commentaireMarge{Identifier les événements.}%
  $A$ : << l'élève est une fille >>, $B$ : << l'élève porte des lunettes >>.

  \commentaireMarge{Lire les probabilités.}%
  $P(A) = \dfrac{120}{200} = \dfrac{3}{5}$, \quad $P(A \cap B) = \dfrac{48}{200} = \dfrac{6}{25}$.

  \commentaireMarge{Appliquer la formule.}%
  \[
    P_A(B) = \frac{P(A \cap B)}{P(A)} = \frac{\frac{6}{25}}{\frac{3}{5}} = \frac{6}{25} \times \frac{5}{3} = \frac{2}{5}.
  \]

  \medskip
  Parmi les filles, $\dfrac{2}{5} = 40\,\%$ portent des lunettes.
}

\pieges{%
  \begin{itemize}
    \item \textbf{Piège 1.} Confondre $P_A(B)$ et $P_B(A)$ : l'ordre compte !
    \item \textbf{Piège 2.} Confondre $P(A \cap B)$ et $P(A \cup B)$ dans la formule.
    \item \textbf{Piège 3.} Oublier de vérifier que $P(A) > 0$.
  \end{itemize}
}

\verifier{%
  Vérifier que $0 \leq P_A(B) \leq 1$. Si le résultat depasse $1$ ou est négatif, il y a une erreur.
  On peut aussi vérifier que $P_A(B) + P_A(\overline{B}) = 1$.
}

\sEntrainer{\refExos{M1}}

\end{fichemethode}
