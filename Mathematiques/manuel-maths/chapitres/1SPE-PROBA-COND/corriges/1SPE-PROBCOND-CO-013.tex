% META: {"id": "1SPE-PROBCOND-CO-013", "chapitre": "1SPE-PROBA-COND", "type_objet": "corrige", "exercice_ref": "1SPE-PROBCOND-EX-013", "capacites_codes": ["C2"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import Rational
% # Les trois couleurs forment la partition ; chacune porte sa probabilite
% # conditionnelle de gain. C'est la proportion qui somme a 1, pas le gain.
% branches = [(Rational(1, 2), Rational(1, 5)),
%             (Rational(1, 3), Rational(2, 5)),
%             (Rational(1, 6), Rational(3, 5))]
% assert sum(proportion for proportion, _ in branches) == 1
% # 2. formule des probabilites totales
% contributions = [proportion * gain for proportion, gain in branches]
% assert contributions == [Rational(1, 10), Rational(2, 15), Rational(1, 10)]
% P_G = sum(contributions)
% assert P_G == Rational(10, 30) == Rational(1, 3)
% # controle : le gain moyen est encadre par le plus petit et le plus grand
% assert Rational(1, 5) <= P_G <= Rational(3, 5)
% END-VERIFY
\begin{corrige}{1SPE-PROBCOND-EX-013}

\textbf{1.} Arbre a trois branches principales (R, B, N) puis chacune se divise en gagner/perdre.

\medskip
\textbf{2.} $P(G) = \dfrac{1}{2} \times \dfrac{1}{5} + \dfrac{1}{3} \times \dfrac{2}{5} + \dfrac{1}{6} \times \dfrac{3}{5} = \dfrac{1}{10} + \dfrac{2}{15} + \dfrac{1}{10} = \dfrac{3}{30} + \dfrac{4}{30} + \dfrac{3}{30} = \dfrac{10}{30} = \dfrac{1}{3}$.

\end{corrige}
