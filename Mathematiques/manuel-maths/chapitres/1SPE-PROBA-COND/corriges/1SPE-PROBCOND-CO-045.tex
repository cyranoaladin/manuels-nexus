% META: {"id": "1SPE-PROBCOND-CO-045", "chapitre": "1SPE-PROBA-COND", "type_objet": "corrige", "exercice_ref": "1SPE-PROBCOND-EX-045", "capacites_codes": ["C5"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import Rational
% P_P1 = Rational(2, 5)
% P_P2_P1 = Rational(7, 10)
% P_P2_P1bar = Rational(3, 10)
% # Chaque feuille sous SON evenement.
% assert P_P1 * P_P2_P1 == Rational(14, 50)
% assert P_P1 * (1 - P_P2_P1) == Rational(6, 50)
% assert (1 - P_P1) * P_P2_P1bar == Rational(9, 50)
% assert (1 - P_P1) * (1 - P_P2_P1bar) == Rational(21, 50)
% assert Rational(14,50)+Rational(6,50)+Rational(9,50)+Rational(21,50) == 1
% assert Rational(23, 50) == Rational(2,5)*Rational(7,10)+Rational(3,5)*Rational(3,10)
% END-VERIFY
\begin{corrige}{1SPE-PROBCOND-EX-045}

\textbf{1.} On note $P_1$ « il pleut aujourd'hui » et $P_2$ « il pleut demain ». L'arbre pondéré de la situation est le suivant.

% Enveloppe R6 : le gate de compilation isolee (scripts/check_latex.py) monte
% l'objet sans la charte. Le corrige rend donc son composant disponible
% lui-meme ; sans effet des que la charte l'a deja charge (manuel assemble).
\makeatletter\@ifundefined{nxarbreproba}{%
  \IfFileExists{gabarits/common/nexus-arbres.sty}%
    {\input{gabarits/common/nexus-arbres.sty}}%
    {\input{../../gabarits/common/nexus-arbres.sty}}}{}\makeatother
\begin{nxarbreproba}
  \nxarbreracine{o}
  \nxarbrenoeud{a}{o}{$P_1$}{$\dfrac{2}{5}$}
  \nxarbrefeuille{ap}{a}{$P_2$}{$\dfrac{7}{10}$}{$P_2 \cap P_1$}{$\dfrac{14}{50}$}
  \nxarbrefeuille{an}{a}{$\overline{P_2}$}{$\dfrac{3}{10}$}{$\overline{P_2} \cap P_1$}{$\dfrac{6}{50}$}
  \nxarbrenoeud{b}{o}{$\overline{P_1}$}{$\dfrac{3}{5}$}
  \nxarbrefeuille{bp}{b}{$P_2$}{$\dfrac{3}{10}$}{$P_2 \cap \overline{P_1}$}{$\dfrac{9}{50}$}
  \nxarbrefeuille{bn}{b}{$\overline{P_2}$}{$\dfrac{7}{10}$}{$\overline{P_2} \cap \overline{P_1}$}{$\dfrac{21}{50}$}
\end{nxarbreproba}

Lecture de l'arbre :

\begin{itemize}
  \item Premier niveau : $P_1$ de probabilité $\dfrac{2}{5}$, et $\overline{P_1}$ de probabilité $\dfrac{3}{5}$.
  \item Sous $P_1$ : $P_2$ de probabilité $\dfrac{7}{10}$, et $\overline{P_2}$ de probabilité $\dfrac{3}{10}$.
  \item Sous $\overline{P_1}$ : $P_2$ de probabilité $\dfrac{3}{10}$, et $\overline{P_2}$ de probabilité $\dfrac{7}{10}$.
\end{itemize}

\medskip

\textbf{2.} $P(\text{pluie demain}) = \frac{2}{5}\times\frac{7}{10}+\frac{3}{5}\times\frac{3}{10} = \frac{14}{50}+\frac{9}{50} = \frac{23}{50} = 46\,\%$.

\end{corrige}
