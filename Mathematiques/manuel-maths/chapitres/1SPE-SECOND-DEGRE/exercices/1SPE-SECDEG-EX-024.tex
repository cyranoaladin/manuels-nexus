% META: {"id": "1SPE-SECDEG-EX-024", "chapitre": "1SPE-SECOND-DEGRE", "type_objet": "exercice", "capacites": ["1SPE-SECOND-DEGRE-C3", "1SPE-SECOND-DEGRE-C4"], "capacites_codes": ["C3", "C4"], "methodes": ["M3", "M4"], "parcours": 2, "competences": ["calculer", "raisonner"], "duree_min": 20, "mode_creation": "ex_nihilo", "sources_inspiration": [], "parametres_sympy": {}, "fichier_tex": "chapitres/1SPE-SECOND-DEGRE/exercices/1SPE-SECDEG-EX-024.tex", "corrige_tex": "chapitres/1SPE-SECOND-DEGRE/corriges/1SPE-SECDEG-CO-024.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import symbols, solve, simplify, Rational, sqrt
% x = symbols('x')
% # Equation produit : (x^2 - 4)(x - 3) = 0
% # x^2 - 4 = 0 => x = 2 ou x = -2
% # x - 3 = 0 => x = 3
% f = (x**2 - 4)*(x - 3)
% sols = sorted(solve(f, x))
% assert sols == [-2, 2, 3]
% # g(x) = x^2 - 4 = (x-2)(x+2), signe etude
% g = x**2 - 4
% assert g.subs(x, 0) < 0
% assert g.subs(x, 3) > 0
% # h(x) = 2x^2 + 3x - 5, delta = 9+40 = 49
% h = 2*x**2 + 3*x - 5
% delta_h = 9 + 40
% assert delta_h == 49
% sols_h = sorted(solve(h, x))
% assert sols_h == [Rational(-5, 2), 1]
% assert simplify(h - (2*x + 5)*(x - 1)) == 0
% END-VERIFY
\begin{exercice}{1SPE-SECDEG-EX-024}{2}{20}
\begin{enumerate}
  \item \textbf{(C3)} Résoudre dans $\mathbb{R}$ l'équation $(x^2 - 4)(x - 3) = 0$.

  \textit{Utiliser la règle du produit nul après avoir factorisé $x^2 - 4$.}

  \item \textbf{(C4)} Dresser le tableau de signes de $g(x) = x^2 - 4$ sur $\mathbb{R}$.

  \item \textbf{(C3)} Pour $h(x) = 2x^2 + 3x - 5$ :
  \begin{enumerate}
    \item Calculer $\Delta$ et déterminer les racines de $h$.
    \item Factoriser $h(x)$.
    \item Dresser le tableau de signes de $h$.
  \end{enumerate}

  \item \textbf{(C4)} En déduire les solutions de l'inéquation $2x^2 + 3x - 5 \geqslant 0$.
\end{enumerate}
\end{exercice}
