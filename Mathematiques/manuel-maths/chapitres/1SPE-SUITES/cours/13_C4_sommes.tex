% META: {"id": "1SPE-SUITES-CR-013", "chapitre": "1SPE-SUITES", "type_objet": "cours", "capacites_codes": ["C4"], "sources_inspiration": [], "status": "generated"}

%---------------------------------------------------------------
% STRATE 1 — Savoirs fondamentaux
%---------------------------------------------------------------

\theoreme[1 — Somme des $n$ premiers entiers]{
Soit $n \in \mathbb{N}^*$. On a :
\[
  1 + 2 + \cdots + n = \frac{n(n+1)}{2}.
\]
}

\theoreme[2 — Somme géométrique]{
Soit $n \in \mathbb{N}$ et $q \in \mathbb{R}$.
\begin{itemize}
  \item Si $q
\neq 1$ :
        \[
          1 + q + q^2 + \cdots + q^n = \frac{1 - q^{n+1}}{1 - q}.
        \]
  \item Si $q = 1$ :
        \[
          1 + 1 + \cdots + 1 \;(\text{$n+1$ fois}) = n + 1.
        \]
\end{itemize}
}

\propriete[1 — Somme des termes d'une suite arithmétique]{
Soit $(u_n)$ une suite arithmétique de premier terme $u_0$ et de raison $r$.
La somme des $(n+1)$ premiers termes (de $u_0$ à $u_n$) vaut :
\[
  u_0 + u_1 + \cdots + u_n
  = (n+1) \times \frac{u_0 + u_n}{2}.
\]
\textit{Formule mnémotechnique : nombre de termes $\times$ moyenne du premier et du dernier terme.}
}

\propriete[2 — Somme des termes d'une suite géométrique]{
Soit $(u_n)$ une suite géométrique de premier terme $u_0$ et de raison $q$.
\begin{itemize}
  \item Si $q
\neq 1$ :
        \[
          u_0 + u_1 + \cdots + u_n
          = u_0 \times \frac{1 - q^{n+1}}{1 - q}.
        \]
  \item Si $q = 1$ : $u_0 + u_1 + \cdots + u_n = (n+1)\,u_0$.
\end{itemize}
}

\exemple{
\textbf{Suite arithmétique.} La suite $(u_n)$ définie par $u_0 = 3$ et $r = 2$ donne
$u_0 = 3$, $u_5 = 13$.
Somme : $u_0 + \cdots + u_5 = 6 \times \dfrac{3 + 13}{2} = 48$.
}

\exemple{
\textbf{Suite géométrique.} La suite $(v_n)$ définie par $v_0 = 2$ et $q = 3$ donne :
$v_0 + v_1 + \cdots + v_4 = 2 \times \dfrac{1 - 3^5}{1 - 3} = 2 \times \dfrac{-242}{-2} = 242$.
}

\exemple{
La suite $(u_n)$ définie par $u_n=(-1)^n$ est géométrique de premier terme
$u_0=1$ et de raison $q=-1$, car $u_{n+1}=-u_n$. La formule géométrique
s'applique donc bien avec $q=-1$.

En revanche, la suite $v_n=n^2$ n'est ni arithmétique ni géométrique et aucune
des deux formules ci-dessus ne s'applique directement.
}

%---------------------------------------------------------------
% STRATE 2 — Appui et erreurs fréquentes
%---------------------------------------------------------------

\erreurFrequente{
\textbf{Erreur sur le nombre de termes.}
De $u_0$ à $u_n$, il y a \textbf{$n+1$ termes}, pas $n$.
Exemple : de $u_0$ à $u_4$, il y a $5$ termes ($0, 1, 2, 3, 4$).

\medskip
\textbf{Confusion $q = 1$.}
Si $q = 1$, la formule $\dfrac{1-q^{n+1}}{1-q}$ est une division par zéro : elle ne s'applique pas.
Il faut utiliser le cas $q = 1$ séparément : la somme vaut simplement $n+1$.

\medskip
\textbf{Astuce mnémotechnique pour les suites arithmétiques.}
\og Nombre de termes $\times$ demi-somme des extrêmes \fg{} — penser à une moyenne pondérée.
Pour les suites géométriques, retenir $u_0 \times \dfrac{1-q^{n+1}}{1-q}$ : le numérateur fait apparaître le terme d'indice $n+1$.
}

\margeAppui{Compter les termes : $u_0, u_1, \ldots, u_n$ $\Rightarrow$ $n+1$ termes.}

\margeAppui{$q = 1$ : somme $= (n+1)u_0$.}

%---------------------------------------------------------------
% STRATE 3 — Approfondissement (démonstrations exigibles)
%---------------------------------------------------------------

\approfondissement{
\textbf{Démonstrations exigibles.}

\medskip
\textbf{(a) Somme des $n$ premiers entiers — méthode de Gauss.}

Posons $S = 1 + 2 + \cdots + (n-1) + n$.

On écrit la même somme à l'envers :
\[
  S = n + (n-1) + \cdots + 2 + 1.
\]

En additionnant membre à membre ces deux lignes, chaque colonne donne $1 + n = n + 1$, et il y a $n$ colonnes :
\[
  2S = \underbrace{(n+1) + (n+1) + \cdots + (n+1)}_{n \text{ fois}} = n(n+1).
\]

Donc $S = \dfrac{n(n+1)}{2}$. \hfill$\square$

\medskip
\textbf{(b) Somme géométrique — multiplication par $q$ et télescopage.}

Supposons $q
\neq 1$. Posons $S = 1 + q + q^2 + \cdots + q^n$.

On multiplie par $q$ :
\[
  qS = q + q^2 + q^3 + \cdots + q^{n+1}.
\]

On soustrait :
\[
  S - qS = 1 - q^{n+1},
\]
car tous les termes intermédiaires se simplifient (effet télescopique).

Donc $S(1 - q) = 1 - q^{n+1}$, soit $S = \dfrac{1-q^{n+1}}{1-q}$ puisque $q
\neq 1$. \hfill$\square$
}

% BEGIN-VERIFY
% from sympy import symbols, summation, simplify, Rational
%
% n, k = symbols('n k', positive=True)
%
% # Theoreme 1 : somme des n premiers entiers, verifiee formellement.
% assert simplify(summation(k, (k, 1, n)) - n*(n + 1)/2) == 0
% # Et par comptage direct sur des rangs concrets.
% for N in range(1, 40):
%     assert sum(range(1, N + 1)) == N*(N + 1)//2
%
% # Theoreme 2 : somme geometrique, q != 1. Les raisons sont rationnelles pour
% # que la comparaison reste exacte : un flottant ferait echouer l'egalite
% # pour une raison d'arrondi, pas de mathematiques.
% for raison in (Rational(2), Rational(3), Rational(1, 2), Rational(-1),
%                Rational(-3, 2)):
%     for N in range(0, 12):
%         directe = sum(raison**i for i in range(N + 1))
%         formule = (1 - raison**(N + 1)) / (1 - raison)
%         assert simplify(directe - formule) == 0, (raison, N)
%
% # Cas q = 1 : la formule generale diviserait par zero ; la somme vaut n+1.
% for N in range(0, 8):
%     assert sum(Rational(1)**i for i in range(N + 1)) == N + 1
%
% # Exemple arithmetique du cours : u_0 = 3, r = 2, somme des six premiers.
% termes = [3 + 2*i for i in range(6)]
% assert termes[-1] == 13
% assert sum(termes) == 48 == 6 * Rational(3 + 13, 2)
%
% # Exemple geometrique : v_0 = 2, q = 3, cinq termes.
% geo = [2 * 3**i for i in range(5)]
% assert sum(geo) == 242 == 2 * Rational(1 - 3**5, 1 - 3)
%
% # L'exemple limite : (-1)^n est geometrique de raison -1, et la formule
% # s'applique.
% alternee = [(-1)**i for i in range(7)]
% assert all(alternee[i+1] == -alternee[i] for i in range(6))
% assert sum(alternee) == Rational(1 - (-1)**7, 1 - (-1))
%
% # n^2 n'est ni arithmetique ni geometrique : aucune des deux formules ne vaut.
% carres = [i**2 for i in range(1, 6)]
% assert len({carres[i+1] - carres[i] for i in range(4)}) > 1
% assert len({Rational(carres[i+1], carres[i]) for i in range(4)}) > 1
% END-VERIFY
