% META: {"id": "1SPE-SUITES-CR-015", "chapitre": "1SPE-SUITES", "type_objet": "cours", "capacites_codes": ["C6"], "sources_inspiration": [], "status": "generated"}

%---------------------------------------------------------------
% STRATE 1 — Savoirs fondamentaux
%---------------------------------------------------------------

\propriete[1 — Suite arithmétique comme modèle linéaire discret]{
Une suite arithmétique de raison $r$ modélise une évolution où la \textbf{variation absolue} est constante :
\[
  u_{n+1} = u_n + r \quad \text{pour tout } n \in \mathbb{N}.
\]
C'est l'analogue discret d'une fonction affine $f(x) = ax + b$ : la suite croît (ou décroît) du même montant $r$ à chaque pas.
}

\propriete[2 — Suite géométrique comme modèle exponentiel discret]{
Une suite géométrique de raison $q$ modélise une évolution où le \textbf{taux de variation} est constant :
\[
  u_{n+1} = q \cdot u_n \quad \text{pour tout } n \in \mathbb{N}.
\]
C'est l'analogue discret d'une fonction exponentielle.
Le coefficient multiplicateur $q$ est lié au taux d'évolution $t$ (exprimé en valeur décimale) par :
\[
  q = 1 + t.
\]
}

\exemple{
\textbf{Modèle arithmétique.} Un abonnement coûte $10$~€ à l'inscription puis $5$~€ par mois.
Le coût total après $n$ mois est $u_n = 10 + 5n$, qui est une suite arithmétique de raison $5$ et de premier terme $u_0 = 10$.
}

\exemple{
\textbf{Modèle géométrique.} Un capital de $1\,000$~€ est placé à un taux annuel de $3\,\%$.
Chaque année, le capital est multiplié par $q = 1{,}03$.
Après $n$ années : $u_n = 1\,000 \times 1{,}03^n$.
}

\exemple{
\textbf{Lien taux / coefficient multiplicateur.} Une population augmente de $2\,\%$ par an.
Taux $t = 0{,}02$, donc $q = 1 + 0{,}02 = 1{,}02$.
Une diminution de $5\,\%$ donne $q = 1 - 0{,}05 = 0{,}95$.
}

%---------------------------------------------------------------
% STRATE 2 — Appui et erreurs fréquentes
%---------------------------------------------------------------

\erreurFrequente{
\textbf{Confondre évolution absolue et évolution relative.}
\og La valeur augmente de $20$ \fg{} $\rightarrow$ modèle arithmétique (variation absolue constante).
\og La valeur augmente de $20\,\%$ \fg{} $\rightarrow$ modèle géométrique (variation relative constante).

\medskip
\textbf{Erreur sur le coefficient multiplicateur.}
Une augmentation de $5\,\%$ donne $q = 1{,}05$, pas $q = 0{,}05$ (qui serait une quasi-suppression).
Une diminution de $5\,\%$ donne $q = 0{,}95$, pas $q = 1{,}05$ ni $q = -0{,}05$.

\medskip
\textbf{Tableau récapitulatif.}

\begin{center}
\begin{tabular}{|p{4.2cm}|c|c|}
\hline
 & Suite arithmétique & Suite géométrique \\
\hline
Relation & $u_{n+1} = u_n + r$ & $u_{n+1} = q \cdot u_n$ \\
Terme général & $u_n = u_0 + nr$ & $u_n = u_0 \cdot q^n$ \\
Modèle & linéaire discret & exponentiel discret \\
Évolution & absolue constante ($r$) & relative constante ($q$) \\
Taux et $q$ & — & $q = 1 + t$ \\
\hline
\end{tabular}
\end{center}

Pour $q=0$, le terme initial reste donné séparément et l'expression
$u_n=u_0q^n$ est utilisée seulement pour $n\geq1$.
}

\margeAppui{Variation absolue constante $\Rightarrow$ arithmétique.}

\margeAppui{Variation relative constante $\Rightarrow$ géométrique.}

\margeAppui{Augmentation de $p\,\%$ : $q = 1 + \frac{p}{100}$.}

%---------------------------------------------------------------
% STRATE 3 — Approfondissement
%---------------------------------------------------------------

\approfondissement{
\textbf{Vérifier la validité d'un modèle.}

\medskip
Les modèles arithmétiques et géométriques supposent une évolution parfaitement
régulière, ce qui est rarement vérifié en pratique pendant une durée
arbitrairement longue. Avant d'utiliser un modèle, il faut donc préciser son
\textbf{domaine de validité} et confronter ses résultats au contexte.

\medskip
Un tableau de valeurs permet notamment de repérer des prévisions
invraisemblables :
\begin{itemize}
  \item pour une quantité initiale positive et un coefficient $q>1$, les
  valeurs d'un modèle géométrique peuvent devenir incompatibles avec des
  ressources limitées ;
  \item un modèle arithmétique de raison négative peut finir par produire une
  valeur négative, impossible pour une population ou une masse.
\end{itemize}

\medskip
Une prévision numérique ne vaut donc que dans les hypothèses et sur la période
où le modèle reste pertinent.
}

% BEGIN-VERIFY
% from sympy import Rational
%
% # Modele arithmetique : variation ABSOLUE constante.
% def arithmetique(u0, r, n):
%     return u0 + n*r
% valeurs = [arithmetique(1000, 50, n) for n in range(6)]
% assert valeurs == [1000, 1050, 1100, 1150, 1200, 1250]
% assert {valeurs[i+1] - valeurs[i] for i in range(5)} == {50}
% # Le TAUX, lui, n'est pas constant : c'est ce qui distingue les deux modeles.
% taux = {Rational(valeurs[i+1] - valeurs[i], valeurs[i]) for i in range(5)}
% assert len(taux) > 1
%
% # Modele geometrique : taux de variation constant, q = 1 + t.
% def geometrique(u0, q, n):
%     return u0 * q**n
% t = Rational(5, 100)
% q = 1 + t
% assert q == Rational(21, 20)
% croissance = [geometrique(1000, q, n) for n in range(6)]
% assert {Rational(croissance[i+1] - croissance[i], croissance[i])
%         for i in range(5)} == {t}
% # Et la variation absolue, elle, augmente.
% absolues = [croissance[i+1] - croissance[i] for i in range(5)]
% assert absolues == sorted(absolues) and absolues[0] < absolues[-1]
%
% # Une baisse de 20 % correspond a q = 0,8 — et non a q = -0,2.
% baisse = 1 + Rational(-20, 100)
% assert baisse == Rational(4, 5)
% apres_deux_baisses = geometrique(100, baisse, 2)
% assert apres_deux_baisses == 64
% # Deux baisses de 20 % ne font pas une baisse de 40 %.
% assert apres_deux_baisses != 100 * (1 - Rational(40, 100))
% END-VERIFY
