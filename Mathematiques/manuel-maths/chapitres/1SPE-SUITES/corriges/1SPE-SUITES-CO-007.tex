% META: {"id":"1SPE-SUITES-CO-007","chapitre":"1SPE-SUITES","type_objet":"corrige","exercice_ref":"1SPE-SUITES-EX-007","capacites":["1SPE-SUITES-C2","1SPE-SUITES-C5"],"capacites_codes":["C2","C5"],"sources_inspiration":[],"status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import *
% n = symbols('n', integer=True, nonnegative=True)
% u0 = 100
% r = -3
% u = u0 + n*r
% assert u.subs(n, 0) == 100
% assert u.subs(n, 1) == 97
% assert u.subs(n, 2) == 94
% assert u.subs(n, 20) == 40
% # r = -3 < 0 => suite decroissante
% assert r < 0
% END-VERIFY
\begin{corrige}{1SPE-SUITES-EX-007}

\commentaireMarge{Terme général : $u_n = u_0 + nr$.}

\textbf{Question 1.} La suite $(u_n)$ est arithmétique de premier terme $u_0 = 100$ et de raison $r = -3$. Sa formule du terme général est :
\[
u_n = u_0 + n \times r = 100 + n \times (-3) = 100 - 3n.
\]

\commentaireMarge{On substitue les valeurs de $n$ dans la formule.}

\textbf{Question 2.} On calcule $u_1$ et $u_2$ :
\[
u_1 = 100 - 3 \times 1 = 100 - 3 = 97.
\]
\[
u_2 = 100 - 3 \times 2 = 100 - 6 = 94.
\]

Donc $u_1 = 97$ et $u_2 = 94$.

\textbf{Question 3.} On applique la formule pour $n = 20$ :
\[
u_{20} = 100 - 3 \times 20 = 100 - 60 = 40.
\]

Donc $u_{20} = 40$.

\commentaireMarge{Le signe de $r$ donne le sens de variation.}

\textbf{Question 4.} La raison de la suite est $r = -3 < 0$.

Pour toute suite arithmétique, la différence $u_{n+1} - u_n = r$ est constante. Ici, $r = -3 < 0$, donc $u_{n+1} < u_n$ pour tout $n \in \mathbb{N}$.

La suite $(u_n)$ est donc \textbf{strictement décroissante}.

\end{corrige}
