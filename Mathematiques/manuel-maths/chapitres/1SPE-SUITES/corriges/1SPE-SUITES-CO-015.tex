% META: {"id":"1SPE-SUITES-CO-015","chapitre":"1SPE-SUITES","type_objet":"corrige","exercice_ref":"1SPE-SUITES-EX-015","capacites":["1SPE-SUITES-C3","1SPE-SUITES-C5"],"capacites_codes":["C3","C5"],"sources_inspiration":[],"status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import Rational, symbols, simplify
% n = symbols('n', integer=True, nonnegative=True)
% u = 5 * Rational(3, 4)**n
% ratio = u.subs(n, n+1) / u
% assert simplify(ratio - Rational(3, 4)) == 0
% # u_n > 0 pour tout n : u0=5>0, q=3/4>0
% assert u.subs(n, 0) == 5
% assert u.subs(n, 1) == Rational(15, 4)
% # 0 < q = 3/4 < 1 et u_n > 0 => suite strictement decroissante
% assert Rational(3, 4) < 1
% assert Rational(3, 4) > 0
% END-VERIFY
\begin{corrige}{1SPE-SUITES-EX-015}

\commentaireMarge{Avant de former un quotient, on prouve que son dénominateur est strictement positif.}

\textbf{Question 1.} On vérifie que tous les termes $u_n$ sont strictement positifs.

Par récurrence : $u_0 = 5 > 0$, et si $u_n > 0$ alors $u_{n+1} = \dfrac{3}{4} u_n > 0$ (produit de deux nombres positifs). Donc $u_n > 0$ pour tout $n \in \mathbb{N}$.

\textbf{Question 2.} Puisque $u_n > 0$, le quotient $\dfrac{u_{n+1}}{u_n}$ est bien défini. La relation $u_{n+1} = \dfrac{3}{4}u_n$ donne :
\[
\frac{u_{n+1}}{u_n} = \frac{\dfrac{3}{4} u_n}{u_n} = \frac{3}{4}.
\]

\commentaireMarge{Théorème : si $(u_n)$ est une suite géométrique à termes strictement positifs, alors : $q > 1 \Rightarrow$ suite croissante ; $0 < q < 1 \Rightarrow$ suite décroissante.}

\textbf{Question 3.} Puisque tous les termes sont strictement positifs et que :
\[
\frac{u_{n+1}}{u_n} = \frac{3}{4} < 1,
\]
on a $u_{n+1} < u_n$ pour tout $n \in \mathbb{N}$.

La suite $(u_n)$ est donc \textbf{strictement décroissante}.

\end{corrige}
