% META: {"id": "TEXP-CAG-EX-006", "chapitre": "TEXP-COMPLEXES-ALGEBRE-GEOMETRIE", "type_objet": "exercice", "capacites": ["TEXP-CAG-C1"], "capacites_codes": ["C1"], "methodes": ["M1"], "parcours": 2, "competences": ["calculer", "raisonner"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TEXP-COMPLEXES-ALGEBRE-GEOMETRIE/exercices/TEXP-CAG-EX-006.tex", "corrige_tex": "chapitres/TEXP-COMPLEXES-ALGEBRE-GEOMETRIE/corriges/TEXP-CAG-CO-006.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import I, expand, re, im
% assert I ** 2 == -1 and I ** 3 == -I and I ** 4 == 1
% assert 2026 % 4 == 2 and I ** 2026 == -1
% assert expand((2 + I) ** 3) == 2 + 11 * I
% assert re(expand((2 + I) ** 3)) == 2 and im(expand((2 + I) ** 3)) == 11
% assert expand((1 + I) ** 2) == 2 * I
% assert expand((1 + I) ** 10) == 32 * I
% assert expand((2 * I) ** 5) == 32 * I
% END-VERIFY
\begin{exercice}{TEXP-CAG-EX-006}{2}{15}
\begin{enumerate}
  \item Calculer $\mathrm{i}^{2}$, $\mathrm{i}^{3}$, $\mathrm{i}^{4}$ puis
        $\mathrm{i}^{2026}$.
  \item Déterminer la partie réelle et la partie imaginaire de
        $(2+\mathrm{i})^3$.
  \item Montrer que $(1+\mathrm{i})^2=2\mathrm{i}$, et en déduire
        $(1+\mathrm{i})^{10}$.
\end{enumerate}
\end{exercice}
