% META: {"id": "TCOMPL-AIR-EX-036", "chapitre": "TCOMPL-CALCULS-AIRES", "type_objet": "exercice", "capacites": ["TCOMPL-AIR-C6"], "capacites_codes": ["C6"], "methodes": ["M4"], "parcours": 2, "competences": ["communiquer","raisonner"], "duree_min": 10, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TCOMPL-CALCULS-AIRES/exercices/TCOMPL-AIR-EX-036.tex", "corrige_tex": "chapitres/TCOMPL-CALCULS-AIRES/corriges/TCOMPL-AIR-CO-036.tex", "status": "approved"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = exp(x)
% fp = diff(f, x)
% assert fp.subs(x, 0) == 1
% # tangente en 0: y = x + 1
% # f(x) - (x+1) = e^x - x - 1 >= 0 (convexite)
% g = exp(x) - x - 1
% assert g.subs(x, 0) == 0
% assert diff(g, x).subs(x, 0) == 0
% assert diff(g, x, 2) == exp(x)  # > 0, convexe
% assert g.subs(x, 1) == exp(1) - 2 > 0
% assert g.subs(x, -1) == exp(-1) > 0
% END-VERIFY
\begin{exercice}{TCOMPL-AIR-EX-036}{1}{10}
Soit $f(x) = \mathrm{e}^x$.
\begin{enumerate}
  \item Determiner l'equation de la tangente a la courbe en $x = 0$.
  \item La courbe est-elle au-dessus ou au-dessous de cette tangente ?
\end{enumerate}

\end{exercice}
