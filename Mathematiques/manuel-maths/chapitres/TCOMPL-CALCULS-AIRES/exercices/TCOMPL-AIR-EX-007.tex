% META: {"id": "TCOMPL-AIR-EX-007", "chapitre": "TCOMPL-CALCULS-AIRES", "type_objet": "exercice", "capacites": ["TCOMPL-AIR-C3"], "capacites_codes": ["C3"], "methodes": ["M3"], "parcours": 2, "competences": ["calculer"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TCOMPL-CALCULS-AIRES/exercices/TCOMPL-AIR-EX-007.tex", "corrige_tex": "chapitres/TCOMPL-CALCULS-AIRES/corriges/TCOMPL-AIR-CO-007.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import symbols, integrate, exp, log, Rational, simplify
% x = symbols('x')
% assert integrate(2 * x + 1, (x, 1, 3)) == 10
% assert integrate(x - x ** 2, (x, 0, 1)) == Rational(1, 6)
% assert integrate(exp(x), (x, 0, log(2))) == 1
% assert simplify(integrate(exp(x), (x, 0, log(2))) / log(2) - 1 / log(2)) == 0
% END-VERIFY
\begin{exercice}{TCOMPL-AIR-EX-007}{2}{15}
\begin{enumerate}
  \item Calculer $\displaystyle\int_1^3 (2x+1)\,\mathrm{d}x$.
  \item Calculer l'aire du domaine compris entre les courbes de
        $f(x)=x^2$ et $g(x)=x$ sur $[0\,;\,1]$.
  \item Calculer la valeur moyenne de $h(x)=\mathrm{e}^{x}$ sur
        $[0\,;\,\ln 2]$.
\end{enumerate}
\end{exercice}
