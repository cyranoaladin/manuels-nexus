% META: {"id":"1SPE-EXPO-CO-010","chapitre":"1SPE-EXPONENTIELLE","type_objet":"corrige","exercice_id":"1SPE-EXPO-EX-010","status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = x*exp(x) - exp(x) + 1
% fp = f.diff(x)
% assert simplify(f.subs(x, 0)) == 0, "f(0) = 0"
% assert simplify(fp) == x*exp(x), "f'(x) = x*exp(x)"
% assert simplify(fp.subs(x, 0)) == 0, "f'(0) = 0"
% END-VERIFY
\begin{corrige}{1SPE-EXPO-EX-010}
\begin{enumerate}
\item $f(0) = 0 \cdot \mathrm{e}^0 - \mathrm{e}^0 + 1 = 0 - 1 + 1 = 0$.

\item On dérive en utilisant la formule du produit pour $x\mathrm{e}^x$ :
\[f'(x) = (x\mathrm{e}^x)' - (\mathrm{e}^x)' = (\mathrm{e}^x + x\mathrm{e}^x) - \mathrm{e}^x = x\mathrm{e}^x.\]

\item $f'(0) = 0 \cdot \mathrm{e}^0 = 0$.

\item \textbf{Signe de $f'(x) = x\mathrm{e}^x$.} Comme $\mathrm{e}^x > 0$ pour tout $x$, le signe de $f'(x)$ est celui de $x$ :
\begin{itemize}
\item $f'(x) < 0$ si $x < 0$ : $f$ est strictement décroissante sur $]-\infty ; 0]$.
\item $f'(x) > 0$ si $x > 0$ : $f$ est strictement croissante sur $[0 ; +\infty[$.
\end{itemize}

\item Le tableau de variations montre que $f$ admet un \textbf{minimum global} en $x = 0$, avec $f(0) = 0$.

Par conséquent, $f(x) \geqslant 0$ pour tout réel $x$.

\item Puisque $f(x) \geqslant 0$, on a $x\mathrm{e}^x - \mathrm{e}^x + 1 \geqslant 0$, soit :
\[x\mathrm{e}^x \geqslant \mathrm{e}^x - 1 \quad \text{pour tout réel } x.\]
\end{enumerate}
\end{corrige}
