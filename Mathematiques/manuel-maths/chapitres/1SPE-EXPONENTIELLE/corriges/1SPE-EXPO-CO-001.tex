% META: {"id":"1SPE-EXPO-CO-001","chapitre":"1SPE-EXPONENTIELLE","type_objet":"corrige","exercice_id":"1SPE-EXPO-EX-001","status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import *
% x = symbols('x')
% f = exp(x)
% assert f.diff(x) == f, "exp'(x) = exp(x)"
% assert f.subs(x, 0) == 1, "exp(0) = 1"
% assert exp(1) == E, "exp(1) = e"
% END-VERIFY
\begin{corrige}{1SPE-EXPO-EX-001}
\begin{enumerate}
\item La fonction exponentielle a pour dérivée elle-même, donc :
\[f'(x) = \mathrm{e}^x.\]
On constate que $f'(x) = f(x)$ pour tout réel $x$.

\item Pour tout réel $x$, on a $f'(x) = \mathrm{e}^x = f(x)$.
La fonction $f$ vérifie donc l'équation $f' = f$.

\item On calcule :
\[f(0) = \mathrm{e}^0 = 1.\]

\item D'après le cours, la fonction exponentielle est \textbf{l'unique} fonction $f$ dérivable sur $\mathbb{R}$ telle que $f' = f$ et $f(0) = 1$. La fonction $x \mapsto \mathrm{e}^x$ vérifie ces deux conditions, c'est donc bien cette unique fonction.

\item $\exp(1) = \mathrm{e} \approx 2{,}718$. Ce nombre est noté $\mathrm{e}$ et s'appelle le \textbf{nombre d'Euler}.
\end{enumerate}
\end{corrige}
