% META: {"id":"1SPE-EXPO-CO-013","chapitre":"1SPE-EXPONENTIELLE","type_objet":"corrige","exercice_id":"1SPE-EXPO-EX-013","status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import symbols, exp, simplify
% x = symbols('x')
% assert simplify(exp(x+1)*exp(x-1) - exp(2*x)) == 0
% assert simplify(exp(2*x+3)/exp(x+1) - exp(x+2)) == 0
% assert simplify((exp(x))**3/exp(2*x) - exp(x)) == 0
% assert simplify(exp(x+2)*exp(-(x-2)) - exp(4)) == 0
% END-VERIFY
\begin{corrige}{1SPE-EXPO-EX-013}
On utilise les propriétés : $\mathrm{e}^a \times \mathrm{e}^b = \mathrm{e}^{a+b}$ et $\dfrac{\mathrm{e}^a}{\mathrm{e}^b} = \mathrm{e}^{a-b}$.

\textbf{1.} $\mathrm{e}^{x+1} \times \mathrm{e}^{x-1} = \mathrm{e}^{(x+1)+(x-1)} = \mathrm{e}^{2x}$.

\textbf{2.} $\dfrac{\mathrm{e}^{2x+3}}{\mathrm{e}^{x+1}} = \mathrm{e}^{(2x+3)-(x+1)} = \mathrm{e}^{x+2}$.

\textbf{3.} $\dfrac{\left(\mathrm{e}^x\right)^3}{\mathrm{e}^{2x}} = \dfrac{\mathrm{e}^{3x}}{\mathrm{e}^{2x}} = \mathrm{e}^{3x-2x} = \mathrm{e}^x$.

\textbf{4.} $\mathrm{e}^{x+2} \times \mathrm{e}^{-(x-2)} = \mathrm{e}^{(x+2)+(-x+2)} = \mathrm{e}^{4}$.
\end{corrige}
