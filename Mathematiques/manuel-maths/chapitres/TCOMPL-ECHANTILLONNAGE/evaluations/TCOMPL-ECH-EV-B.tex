% META: {"id": "TCOMPL-ECH-EV-B", "chapitre": "TCOMPL-ECHANTILLONNAGE", "type_objet": "evaluation", "version": "B", "duree_min": 45, "bareme_total": 20, "capacites_codes": ["C4", "C5", "C7"], "competences": ["calculer", "raisonner"], "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from math import comb
% def P_egal(n, p, k):
%     return comb(n, k) * p**k * (1 - p)**(n - k)
% n, p = 200, 0.2
% a, cumul = 0, 0
% while cumul < 0.025:
%     cumul += P_egal(n, p, a)
%     a += 1
% a -= 1
% b, cumul2 = 0, 0
% while cumul2 < 0.975:
%     cumul2 += P_egal(n, p, b)
%     b += 1
% b -= 1
% assert a == 29
% assert b == 51
% END-VERIFY

%---------------------------------------------------------------
% Duree : 45 minutes — Bareme : 20 points
%---------------------------------------------------------------

\begin{evaluation}{TCOMPL-ECH-EV-B}{45}

\begin{exercice}{TCOMPL-ECH-EV-B-EX1}{2}{30}
\textbf{Exercice 1} (10 points) --- \textit{Capacité C4}

\medskip

Un fabricant affirme que $20\%$ de ses produits sont défectueux. On prélève un échantillon de $200$ produits, et $X$ (nombre de défectueux) est supposé suivre $\mathcal{B}(200\,;\ 0{,}2)$.
\begin{enumerate}
  \item (5 pts) À la calculatrice ou en Python, déterminer un intervalle de fluctuation $I=[a,b]$ au seuil $95\%$.
  \item (5 pts) Si l'échantillon prélevé contient $58$ produits défectueux, que peut-on en conclure sur l'affirmation du fabricant ?
\end{enumerate}
\end{exercice}

\begin{exercice}{TCOMPL-ECH-EV-B-EX2}{2}{20}
\textbf{Exercice 2} (6 points) --- \textit{Capacité C5}

\medskip

Écrire, en Python, une fonction \texttt{simuler\_moyennes(N, n, p)} qui simule $N$ échantillons de taille $n$ d'une variable de Bernoulli de paramètre $p$, et renvoie la liste des $N$ moyennes obtenues.
\end{exercice}

\begin{exercice}{TCOMPL-ECH-EV-B-EX3}{2}{10}
\textbf{Exercice 3} (4 points) --- \textit{Capacité C7}

\medskip

Une urne contient huit jetons indiscernables au toucher, numérotés de $1$ à $8$. On tire un jeton au hasard et on note $X$ son numéro.
\begin{enumerate}
  \item (1 pt) Justifier que $X$ suit la loi uniforme sur $\{1\,;2\,;\dots;8\}$ et donner $P(X=k)$ pour tout $k$ de $1$ à $8$.
  \item (2 pts) Calculer $E(X)$ en détaillant la somme $\sum_{k=1}^{8} k\,P(X=k)$.
  \item (1 pt) Retrouver ce résultat à l'aide de la formule $\dfrac{n+1}{2}$, et expliquer pourquoi l'espérance n'est pas une valeur prise par $X$.
\end{enumerate}
\end{exercice}

\end{evaluation}
