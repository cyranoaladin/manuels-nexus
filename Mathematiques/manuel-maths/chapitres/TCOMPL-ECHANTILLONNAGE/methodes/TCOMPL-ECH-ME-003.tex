% META: {"id":"TCOMPL-ECH-ME-003","chapitre":"TCOMPL-ECHANTILLONNAGE","type_objet":"methode","capacites_codes":["C3"],"methodes":["M3"],"mode_creation":"ex_nihilo","sources_inspiration":[],"status":"approved","origin":"needs_review","status_history":["needs_review","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% from sympy import Rational, binomial
% p = Rational(3, 10)
% P3 = binomial(10, 3) * p**3 * (1-p)**7
% assert abs(float(P3) - 0.2668) < 0.0005
% Ple2 = sum(binomial(10, k) * p**k * (1-p)**(10-k) for k in range(3))
% assert abs(float(Ple2) - 0.3828) < 0.0005
% END-VERIFY
\begin{fichemethode}{M3}{Calculer P(X=k) et P(X<=k) pour une loi binomiale}

\quandUtiliser{%
  Utiliser cette méthode lorsque $X$ suit une loi binomiale $\mathcal{B}(n\,;\,p)$
  et que l'énoncé demande $P(X = k)$, $P(X \leq k)$, ou une probabilité du type
  $P(X \geq k)$, a la calculatrice ou en Python.
}

\pasApas{%
  \begin{enumerate}
    \item Justifier d'abord que $X \sim \mathcal{B}(n\,;\,p)$ (fiche M1) et
      identifier $n$, $p$ et $k$.
    \item Pour une valeur exacte :
      $P(X = k) = \dbinom{n}{k} p^k (1-p)^{n-k}$.
    \item Pour une probabilité cumulee : $P(X \leq k) = \sum_{i=0}^{k} P(X = i)$
      — a la calculatrice (binomFRep/binomcdf) ou en Python.
    \item Transformer les autres formes : $P(X \geq k) = 1 - P(X \leq k-1)$ ;
      $P(a \leq X \leq b) = P(X \leq b) - P(X \leq a-1)$.
    \item En Python :
      \texttt{from math import comb} puis
      \texttt{sum(comb(n,i)*p**i*(1-p)**(n-i) for i in range(k+1))}.
  \end{enumerate}
}

\exempleRedige{%
  $X \sim \mathcal{B}(10\,;\,0{,}3)$. Calculer $P(X = 3)$ puis $P(X \leq 2)$.

  \medskip
  \commentaireMarge{$\binom{10}{3} = 120$.}%
  $P(X = 3) = \dbinom{10}{3} \times 0{,}3^3 \times 0{,}7^{7}
  = 120 \times 0{,}027 \times 0{,}7^7 \approx 0{,}267$.

  \medskip
  $P(X \leq 2) = P(X=0) + P(X=1) + P(X=2)
  = 0{,}7^{10} + 10 \times 0{,}3 \times 0{,}7^9 + 45 \times 0{,}09 \times 0{,}7^8
  \approx 0{,}383$.
}

\pieges{%
  \begin{itemize}
    \item \textbf{Piège 1.} Écrire $P(X \geq k) = 1 - P(X \leq k)$ : il faut
      $1 - P(X \leq k-1)$ (la valeur $k$ doit rester comptee).
    \item \textbf{Piège 2.} Echanger $p$ et $1-p$, ou $k$ et $n-k$, dans la formule.
    \item \textbf{Piège 3.} Arrondir trop tot : garder les valeurs exactes jusqu'au
      résultat final, puis arrondir a $10^{-3}$ pres.
  \end{itemize}
}

\verifier{%
  Contrôler que le résultat est dans $[0\,;\,1]$, que
  $\sum_{k=0}^{n} P(X=k) = 1$, et croiser calculatrice et Python sur une valeur.
}

\sEntrainer{\refExos{M3}}

\end{fichemethode}
