% META: {"id": "TCOMPL-ATT-CO-003", "chapitre": "TCOMPL-TEMPS-ATTENTE", "type_objet": "corrige", "exercice_ref": "TCOMPL-ATT-EX-003", "capacites_codes": ["C5"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import symbols, exp, integrate, oo, simplify, Rational, N
% x = symbols('x', positive=True)
% lam = Rational(1, 4)
% f = lam * exp(-lam*x)
% I = integrate(f, (x, 0, oo))
% assert simplify(I - 1) == 0
% P = integrate(f, (x, 0, 2))
% assert abs(N(P) - 0.3935) < 0.0001
% END-VERIFY
\begin{corrige}{TCOMPL-ATT-EX-003}
\textbf{1.} $f(x)=0{,}25\,\mathrm{e}^{-0{,}25x} \geq 0$ pour tout $x\geq0$, et :
\[
  \int_0^{+\infty} 0{,}25\,\mathrm{e}^{-0{,}25x}\,\mathrm{d}x = \Big[-\mathrm{e}^{-0{,}25x}\Big]_0^{+\infty} = 0-(-1) = 1.
\]
$f$ est donc bien une densité de probabilité.

\textbf{2.} $P(X\leq2) = 1-\mathrm{e}^{-0{,}25\times2} = 1-\mathrm{e}^{-0{,}5} \approx 0{,}3935$.
\end{corrige}
