% META: {"id": "TCOMPL-ATT-ME-001", "chapitre": "TCOMPL-TEMPS-ATTENTE", "type_objet": "methode", "capacites_codes": ["C1"], "methodes": ["M1"], "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "needs_review"}
% BEGIN-VERIFY
% from sympy import symbols, Rational, summation, oo
% k = symbols('k', integer=True, positive=True)
% p = Rational(1,6); q = 1 - p
% assert summation(q**(k-1)*p, (k, 1, oo)) == 1
% assert summation(k*q**(k-1)*p, (k, 1, oo)) == 6
% END-VERIFY
\begin{fichemethode}{M1}{Reconnaitre et definir une loi géométrique}

\quandUtiliser{%
  Utiliser cette méthode lorsqu'une expérience de Bernoulli (succès de
  probabilité $p$) est REPETEE de facon indépendante et que la variable
  etudiee est le RANG du premier succès : il s'agit de justifier que $X$ suit
  la loi géométrique de paramètre $p$ et d'en donner les propriétés.
}

\pasApas{%
  \begin{enumerate}
    \item Vérifier les trois conditions du modèle : epreuves de Bernoulli
      identiques, indépendantes, et $X$ = rang du premier succès.
    \item Enoncer la loi : pour tout entier $k \geq 1$,
      $P(X = k) = (1-p)^{k-1}\,p$ ($k-1$ echecs puis un succès).
    \item Retenir la queue de distribution : $P(X > n) = (1-p)^n$ (les $n$
      premieres epreuves sont toutes des echecs).
    \item Donner l'espérance : $E(X) = \dfrac{1}{p}$ — le rang moyen du premier
      succès.
    \item Enoncer l'absence de memoire : pour tous entiers $m, n \geq 0$,
      $P_{(X > m)}(X > m+n) = P(X > n)$ — le passe ne change pas l'attente
      restante (exploitation : fiche M3).
  \end{enumerate}
}

\exempleRedige{%
  On lance un de equilibre jusqu'a obtenir un 6 ; $X$ est le rang du premier 6.
  Justifier la loi de $X$ et donner $E(X)$.

  \medskip
  \commentaireMarge{Trois conditions a citer explicitement.}%
  Chaque lancer est une epreuve de Bernoulli de paramètre $p = \dfrac{1}{6}$
  (succès : « obtenir 6 »), les lancers sont indépendants et identiques, et $X$
  est le rang du premier succès : $X$ suit la loi géométrique de paramètre
  $\dfrac{1}{6}$.

  \medskip
  Donc $P(X = k) = \left(\dfrac{5}{6}\right)^{k-1}\dfrac{1}{6}$ pour $k \geq 1$,
  et $E(X) = \dfrac{1}{p} = 6$ : en moyenne, le premier 6 arrive au sixieme
  lancer.
}

\pieges{%
  \begin{itemize}
    \item \textbf{Piège 1.} Confondre avec la loi binomiale : la binomiale
      compte les succès en $n$ epreuves FIXEES, la géométrique attend le
      PREMIER succès (nombre d'epreuves non borne).
    \item \textbf{Piège 2.} Écrire $P(X = k) = p^{k-1}(1-p)$ : l'exposant
      $k-1$ porte sur l'ECHEC $(1-p)$.
    \item \textbf{Piège 3.} Oublier de justifier l'indépendance des epreuves
      avant de conclure sur la loi.
  \end{itemize}
}

\verifier{%
  Contrôler que les $P(X = k)$ somment a 1 (serie géométrique), que
  $E(X) = \frac{1}{p}$ est coherent (plus $p$ est petit, plus l'attente est
  longue), et tester $P(X > 0) = 1$.
}

\sEntrainer{\refExos{M1}}

\end{fichemethode}
