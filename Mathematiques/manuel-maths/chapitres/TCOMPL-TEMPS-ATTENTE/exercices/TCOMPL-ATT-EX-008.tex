% META: {"id": "TCOMPL-ATT-EX-008", "chapitre": "TCOMPL-TEMPS-ATTENTE", "type_objet": "exercice", "capacites": ["TCOMPL-ATT-C6"], "capacites_codes": ["C6"], "methodes": ["M6"], "parcours": 2, "competences": ["calculer", "raisonner"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TCOMPL-TEMPS-ATTENTE/exercices/TCOMPL-ATT-EX-008.tex", "corrige_tex": "chapitres/TCOMPL-TEMPS-ATTENTE/corriges/TCOMPL-ATT-CO-008.tex", "status": "generated"}
% BEGIN-VERIFY
% from sympy import symbols, exp, integrate, oo, simplify, Rational
% t = symbols('t', positive=True)
% lam = symbols('lambda', positive=True)
% esperance = integrate(t * lam * exp(-lam * t), (t, 0, oo))
% assert simplify(esperance - 1 / lam) == 0
% assert integrate(exp(-lam * t), (t, 0, oo)) == 1 / lam
% assert simplify(esperance.subs(lam, Rational(1, 2)) - 2) == 0
% END-VERIFY
\begin{exercice}{TCOMPL-ATT-EX-008}{2}{15}
Soit $T$ exponentielle de paramètre $\lambda>0$.
\begin{enumerate}
  \item Écrire $E(T)$ sous forme d'intégrale.
  \item Calculer cette intégrale par une intégration par parties.
  \item Vérifier pour $\lambda=0{,}5$ et interpréter.
\end{enumerate}
\end{exercice}
