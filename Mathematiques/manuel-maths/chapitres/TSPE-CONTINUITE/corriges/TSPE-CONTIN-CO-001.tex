% META: {"id": "TSPE-CONTIN-CO-001", "chapitre": "TSPE-CONTINUITE", "type_objet": "corrige", "exercice_ref": "TSPE-CONTIN-EX-001", "capacites_codes": ["C1"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import symbols
% x = symbols('x')
% f = x**3 - 3*x + 1
% assert f.subs(x, 0) == 1
% assert f.subs(x, 2) == 3
% assert 1 <= 2 <= 3
% END-VERIFY
\begin{corrige}{TSPE-CONTIN-EX-001}

\textbf{Q1.} $f$ est une fonction polynome, donc continue sur $\mathbb{R}$, en particulier sur $[0,2]$.

\textbf{Q2.} $f(0) = 1$ et $f(2) = 8 - 6 + 1 = 3$.

\textbf{Q3.} $f$ est continue sur $[0,2]$ et $2$ est compris entre $f(0)=1$ et $f(2)=3$. Par le theoreme des valeurs intermediaires, l'equation $f(x)=2$ admet au moins une solution dans $[0,2]$.

\end{corrige}
