% META: {"id": "TSPE-CONTIN-CO-004", "chapitre": "TSPE-CONTINUITE", "type_objet": "corrige", "exercice_ref": "TSPE-CONTIN-EX-004", "capacites_codes": ["C1"], "sources_inspiration": [], "status": "generated"}
% BEGIN-VERIFY
% from sympy import symbols, ln
% x = symbols('x')
% f = ln(x) + x - 3
% assert f.subs(x, 1) == -2
% assert round(float(f.subs(x, 4)), 2) == 2.39
% END-VERIFY
\begin{corrige}{TSPE-CONTIN-EX-004}

\textbf{Q1.} $f'(x) = \dfrac{1}{x} + 1$. Pour $x \in [1,4]$, $\dfrac{1}{x} > 0$, donc $f'(x) > 1 > 0$ : $f$ est strictement croissante sur $[1,4]$.

\textbf{Q2.} $f(1) = \ln(1) + 1 - 3 = 0 - 2 = -2$. $f(4) = \ln(4) + 4 - 3 = \ln(4)+1 \approx 2{,}39$.

\textbf{Q3.} $f$ est continue et strictement croissante sur $[1,4]$, et $0$ est compris entre $f(1)=-2$ et $f(4)\approx 2{,}39$. Par le TVI (cas strictement monotone), il existe un unique $x_0 \in [1,4]$ tel que $f(x_0)=0$.

\end{corrige}
