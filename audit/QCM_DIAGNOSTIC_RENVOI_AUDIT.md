# Renvois des diagnostics QCM : chaque cible existe-t-elle ?

- Population : `1587` renvois
- `BROKEN_REMEDIATION_REFERENCES` : `0`
- `QCM_DIAGNOSTIC_MISMATCH` : `0`
- `CROSS_CAPACITY_RENVOI_JUSTIFIED` : `11`

## Renvois croisés, jugés un par un

- `1SPE-EXPONENTIELLE/Q15` option `B` — capacité `C5`, renvoi `C3, signe ; C5, décroissance` : Renvoi composé : C3 pour le signe de l'exponentielle, C5 pour la décroissance modélisée. Le distracteur mêle les deux, et le renvoi les nomme toutes les deux.
- `1SPE-VARIABLES-ALEATOIRES/Q16` option `D` — capacité `C6`, renvoi `C7, N échantillons` : La question porte sur la simulation d'une variable (C6) ; le distracteur confond la taille d'un échantillon avec le NOMBRE d'échantillons, notion de C7.
- `TSPE-GEOMETRIE-ESPACE/Q8` option `C` — capacité `C4`, renvoi `C7, produit scalaire et orthogonalité` : La question porte sur les bases (C4) ; le distracteur invoque l'orthogonalité, qui relève du produit scalaire (C7). La confusion porte sur C7, pas sur C4.
- `TSPE-GEOMETRIE-ESPACE/Q12` option `B` — capacité `C9`, renvoi `C7, produit scalaire et orthogonalité` : La question calcule un angle (C9) ; le distracteur se trompe dans le produit scalaire lui-même (C7), qui est l'outil du calcul.
- `TSPE-GEOMETRIE-ESPACE/Q16` option `C` — capacité `C16`, renvoi `C13, vecteur normal et équation cartésienne` : La question demande de DÉMONTRER l'équation cartésienne (C16) ; le distracteur confond le vecteur normal avec les coordonnées du point, confusion qui se traite en C13, où l'équation est établie.
- `TSPE-LIMITES-FONCTIONS/Q5` option `C` — capacité `C1`, renvoi `C3, résultat fondamental` : La question calcule une limite (C1), mais le distracteur ignore la croissance comparée, qui est l'objet de C3. Renvoyer à C1 laisserait l'élève devant le même blocage.
- `TSPE-LIMITES-FONCTIONS/Q9` option `C` — capacité `C2`, renvoi `C1, limites de l'exponentielle` : La question porte sur l'interprétation d'une asymptote (C2) ; le distracteur se trompe sur les limites de l'exponentielle, enseignées en C1. C'est là qu'il faut retourner.
- `TSPE-PRIMITIVES-EQDIFF/Q2` option `B` — capacité `C4`, renvoi `C1, ensemble des primitives` : La question porte sur la démonstration (C4) ; le distracteur se trompe sur l'ensemble des primitives, décrit en C1.
- `TSPE-PRIMITIVES-EQDIFF/Q2` option `C` — capacité `C4`, renvoi `C1, ensemble des primitives` : Même question, même confusion sur l'ensemble des primitives : le renvoi conduit en C1, où cet ensemble est caractérisé.
- `TSPE-PRIMITIVES-EQDIFF/Q2` option `D` — capacité `C4`, renvoi `C1, ensemble des primitives` : Même question, troisième distracteur : il se trompe encore sur la forme de l'ensemble des primitives, caractérisé en C1. Le renvoi y conduit, et non vers la démonstration qui fait l'objet de la question.
- `TSPE-SUITES-LIMITES/Q7` option `B` — capacité `C4`, renvoi `C1, définition` : La question porte sur le théorème de la limite monotone (C4) ; le distracteur confond convergence et croissance, notions définies en C1.
