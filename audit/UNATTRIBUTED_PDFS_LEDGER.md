# Unattributed PDFs Ledger

Audit of 22 `unattributed_pdfs` anomalies.

All 22 PDFs are historical test fixture artifacts located under `tests/fixtures/` and `audit/fixtures/`.

- `producer`: `historical_build`
- `canonical`: `False`
- `fixture`: `True`
- `action`: `Déclaration explicite comme fixtures de test dans l inventaire`
- **`UNATTRIBUTED_PDFS`**: **`0` après attribution de rôle**
