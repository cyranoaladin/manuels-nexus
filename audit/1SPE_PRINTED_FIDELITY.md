# Fidelite de ce qui est imprime — 1SPE

<!-- generated by scripts/build_1spe_printed_fidelity.py -->

> Une page peut etre parfaitement composee — encre dans le format, aucun debordement, aucune collision — et raconter print(f”...”) ou afficher « ewpage » en toutes lettres. La geometrie regarde ou l'encre se pose ; il fallait lire ce qu'elle dit.

> Chaque programme publie est retrouve dans la couche texte du PDF et compare caractere par caractere a sa source. Comparer plutot que suspecter fait voir aussi bien un guillemet courbe qu'une ligature fusionnee ou un programme compose dans la mauvaise fonte.

> L'edition eleve vide l'environnement `corrige` : elle l'ecrit dans son propre maitre. Un programme de corrige n'y est donc pas attendu, et s'il y apparaissait ce serait le defaut.

> La fonte du code sert aussi aux numeros de ligne et aux annotations de marge de l'edition professeur, plus petites, qui s'intercalent dans l'ordre de lecture. La comparaison ne retient que ce qui est compose a la taille d'un programme, taille lue dans la classe. Le controle des guillemets, lui, regarde toute la fonte du code.

> La couche texte d'un PDF ne restitue pas fidelement les espaces de tete. La comparaison porte sur tout le reste, espaces retires.

> Les queues de commande surveillees viennent des sequences de controle que le corpus emploie reellement, pas d'une liste noire ecrite a la main. Celles qui sont aussi des mots du corpus sont surveillees dans les sources et non sur la page, ou rien ne permettrait de les distinguer.

> Le guillemet courbe n'est cherche que dans ce qui est compose avec la fonte du code. Le corps du texte garde les siens.

## Metriques

| Metrique | Valeur |
|---|---:|
| `CODE_BLOCKS` | 72 |
| `CODE_BLOCK_FAITHFUL` | 67 |
| `CODE_BLOCK_NOT_FAITHFUL` | 0 |
| `CODE_BLOCK_WITHHELD_AS_DECLARED` | 5 |
| `WITHHELD_CODE_BLOCK_PRINTED` | 0 |
| `SMART_QUOTE_IN_CODE_TOKEN` | 0 |
| `VISIBLE_MALFORMED_LATEX_FRAGMENT` | 0 |
| `MALFORMED_FRAGMENT_IN_SOURCE` | 0 |
| `UNKNOWN` | 0 |

## Variante `eleve` — 355 pages

28 programmes sur 33 retrouves a l'identique dans la couche texte ; 5 retenus comme la variante le declare (environnements vides : corrige).

## Variante `professeur` — 627 pages

39 programmes sur 39 retrouves a l'identique dans la couche texte ; 0 retenus comme la variante le declare (environnements vides : aucun).
