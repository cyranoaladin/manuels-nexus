# Charte v3.2 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corriger les bloquants de la charte et produire un spécimen v3.2 vérifié visuellement.

**Architecture:** Les styles TeX restent centralisés dans la classe et les extensions. Deux gates Python stateless inspectent le texte en retirant précisément les zones de code. Le spécimen est une couverture d'intégration de dix pages.

**Tech Stack:** LuaLaTeX, tcolorbox/TikZ, Python 3.12, pytest, ImageMagick/Poppler.

---

## Chunk 1: Gates de corpus

### Task 1: Accent du contenu

**Files:**
- Create: `scripts/gates_corpus/check_accents_contenu.py`
- Modify: `tests/test_gates_corpus.py`, `Makefile`

- [ ] Écrire les tests RED : mot non accentué hors code = erreur, le même identifiant dans `python`, `console` et `lstinline` = autorisé.
- [ ] Exécuter les tests et constater l'échec d'import.
- [ ] Implémenter la liste pédagogique et l'extraction des zones imprimées de `chapitres/` et `gabarits/specimen.tex`.
- [ ] Exécuter les tests puis `make accents`.

### Task 2: ASCII du code

**Files:**
- Create: `scripts/gates_corpus/check_ascii_code.py`
- Modify: `tests/test_gates_corpus.py`, `Makefile`

- [ ] Écrire les tests RED pour les six caractères interdits dans `python`, `console`, `lstinline` et `codereference`, et un caractère typographique dans le texte courant autorisé.
- [ ] Exécuter les tests et constater l'échec d'import.
- [ ] Implémenter le scan et raccorder les deux gates à `lot-gates`.
- [ ] Exécuter les tests et `make lot-gates`.

## Chunk 2: Charte et spécimen

### Task 3: Styles et identité

**Files:**
- Modify: `gabarits/nexus-manuel.cls`, `gabarits/nexus-code.tex`, `gabarits/nexus-signatures.tex`, `gabarits/nexus-figures.tex`, `docs/06_charte_graphique.md`

- [ ] Centraliser `nxboxtitle` et les huit titres à couleur explicite.
- [ ] Construire le motif mémoire Types construits et le rendre depuis l'ouverture.
- [ ] Réintroduire icônes du contrat, signature intérieure et onglet.
- [ ] Écrire les règles de figures et appliquer les styles.

### Task 4: Spécimen, preuves et livraison

**Files:**
- Create: `gabarits/specimen.tex`, `SPECIMEN_A_VALIDER.md`
- Modify: contenu pilote affecté, `PILOTE_A_VALIDER.md`, `DIRECTIVES_EN_COURS.md`

- [ ] Créer les dix pages témoins avec les corrections éditoriales demandées.
- [ ] Compiler et corriger les overfull.
- [ ] Exporter à 150 dpi, inspecter les PNG et relever les preuves dans le rapport.
- [ ] Exécuter les gates complets, committer `[CHARTE][v3.2]` et arrêter à la validation conjointe.
