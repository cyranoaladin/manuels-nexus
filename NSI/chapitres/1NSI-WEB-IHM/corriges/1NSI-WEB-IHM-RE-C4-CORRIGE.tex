% META: {"id": "1NSI-WEB-IHM-RE-C4-CORRIGE", "chapitre": "1NSI-WEB-IHM", "type_objet": "corrige", "exercice_ref": "1NSI-WEB-IHM-RE-C4", "capacites": ["P-IHM-03A"], "capacites_codes": ["C4"], "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "needs_review"}
\begin{corrige}{1NSI-WEB-IHM-RE-C4}
\begin{enumerate}
  \item \textbf{(b)} envoi de la requête — client ; \textbf{(c)} lecture de la base — serveur ;
        \textbf{(d)} affichage du HTML reçu — client ; \textbf{(a)} tri de la liste affichée —
        client. Le serveur ne peut agir qu'entre la requête et la réponse ; le tri, lui, a lieu
        après, sur la page déjà reçue.
  \item \textbf{Non.} Le visiteur ne voit que le HTML produit. Le programme qui a interrogé la
        base s'est exécuté sur le serveur et n'est jamais transmis.
  \item Parce que la base n'est pas accessible depuis le client. La lui confier supposerait de
        lui ouvrir l'accès à toutes les données, y compris celles des autres adhérents.
\end{enumerate}
\end{corrige}
