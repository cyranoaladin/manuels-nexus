% META: {"id": "TNSI-BASES-DE-DONNEES-RE-C03H", "chapitre": "TNSI-BASES-DE-DONNEES", "type_objet": "remediation", "capacites_codes": ["C12"], "status": "generated"}

%---------------------------------------------------------------
% FICHE DE REMEDIATION — C03H : DELETE sans WHERE
%---------------------------------------------------------------

\begin{exercice}{TNSI-BDD-RE-C03H-EX1}{1}{10}
Une table \texttt{Client} contient $50$ clients. On exécute la requête \texttt{DELETE FROM Client;} (sans clause \texttt{WHERE}). Combien de clients reste-t-il dans la table après cette requête ? La table elle-même existe-t-elle toujours ?
\end{exercice}

% BEGIN-VERIFY
% import sqlite3
% conn = sqlite3.connect(":memory:")
% conn.execute("CREATE TABLE Client(id INTEGER PRIMARY KEY, nom TEXT)")
% conn.executemany("INSERT INTO Client(nom) VALUES (?)", [(f"Client{i}",) for i in range(50)])
% conn.commit()
% assert conn.execute("SELECT COUNT(*) FROM Client").fetchone()[0] == 50
% conn.execute("DELETE FROM Client")
% conn.commit()
% assert conn.execute("SELECT COUNT(*) FROM Client").fetchone()[0] == 0
% tables = conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='Client'").fetchall()
% assert tables == [("Client",)]
% END-VERIFY

\begin{corrige}{TNSI-BDD-RE-C03H-EX1}
Il ne reste \textbf{aucun} client : sans clause \texttt{WHERE}, \texttt{DELETE} supprime \textbf{tous} les tuples de la table. La table \texttt{Client} elle-même continue d'exister (son schéma n'est pas touché), mais elle est vide : \texttt{SELECT COUNT(*) FROM Client;} renverrait $0$.
\end{corrige}
