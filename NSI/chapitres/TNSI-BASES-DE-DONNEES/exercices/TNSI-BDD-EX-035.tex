% META: {"id": "TNSI-BDD-EX-035", "chapitre": "TNSI-BASES-DE-DONNEES", "type_objet": "exercice", "capacites": ["TNSI-BASES-DE-DONNEES-C9"], "capacites_codes": ["C9"], "methodes": ["M1"], "parcours": 1, "competences": ["programmer"], "duree_min": 8, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TNSI-BASES-DE-DONNEES/exercices/TNSI-BDD-EX-035.tex", "corrige_tex": "chapitres/TNSI-BASES-DE-DONNEES/corriges/TNSI-BDD-CO-035.tex", "status": "approved"}
% BEGIN-VERIFY
% import sqlite3
% conn = sqlite3.connect(":memory:")
% conn.execute("CREATE TABLE Eleve(id INTEGER PRIMARY KEY, nom TEXT)")
% conn.execute("CREATE TABLE Note(id INTEGER PRIMARY KEY, id_eleve INTEGER, matiere TEXT, valeur REAL)")
% conn.executemany("INSERT INTO Eleve VALUES (?,?)", [(1, "Nabil"), (2, "Salma")])
% conn.executemany("INSERT INTO Note VALUES (?,?,?,?)", [
%     (1, 1, "NSI", 15.0), (2, 2, "NSI", 18.0), (3, 1, "Maths", 12.0),
% ])
% conn.commit()
% cur = conn.execute("""SELECT Eleve.nom, Note.matiere, Note.valeur
%     FROM Note JOIN Eleve ON Note.id_eleve = Eleve.id
%     ORDER BY Note.valeur DESC""")
% assert cur.fetchall() == [("Salma", "NSI", 18.0), ("Nabil", "NSI", 15.0), ("Nabil", "Maths", 12.0)]
% END-VERIFY
\begin{exercice}{TNSI-BDD-EX-003}{2}{20}
On dispose de deux tables :
\begin{sql}
Eleve(id, nom)
Note(id, id_eleve, matiere, valeur)
\end{sql}
où \texttt{Note.id\_eleve} est une clé étrangère vers \texttt{Eleve.id}. Écrire une requête SQL affichant, pour chaque note, le nom de l'élève, la matière et la valeur de la note, triées par valeur \textbf{décroissante}.
\end{exercice}
