# LOT 6 — Evaluations TNSI-BASES-DE-DONNEES
EVAL-A (modelisation, SELECT/WHERE/INSERT) / EVAL-B (SGBD, JOIN/ORDER BY/
UPDATE/DELETE) : 45 min chacune, enonces distincts, corriges dedies, toutes
les requetes SQL verifiees par execution reelle (sqlite3) y compris
l'enchainement UPDATE puis DELETE de EVAL-B (verifie independamment que le
DELETE final ne laisse qu'un seul employe, evitant l'erreur de conception
initiale ou le DELETE n'aurait supprime personne).
