% META: {"id": "1NSI-AGT-RE-C5", "chapitre": "1NSI-ALGO-PARCOURS-TRIS", "type_objet": "remediation", "capacites": ["P-ALGO-02B"], "status": "generated"}

%---------------------------------------------------------------
% FICHE DE REMEDIATION — C5 : indice_min reinitialise dans la mauvaise boucle
%---------------------------------------------------------------

\begin{exercice}{1NSI-AGT-RE-C5-EX1}{1}{10}
Un élève écrit le tri par sélection suivant, mais place l'initialisation de
\lstinline{indice_min} à l'\textbf{intérieur} de la boucle \lstinline{for} interne au lieu
de l'externe :
\begin{python}
def tri_selection_bugue(tableau):
    tableau = tableau[:]
    n = len(tableau)
    for i in range(n - 1):
        for j in range(i + 1, n):
            indice_min = i  # reinitialise a CHAQUE tour de la boucle interne
            if tableau[j] < tableau[indice_min]:
                indice_min = j
        tableau[i], tableau[indice_min] = tableau[indice_min], tableau[i]
    return tableau
\end{python}
\begin{enumerate}
  \item Tester cette fonction sur \lstinline{[9, 1, 5, 3, 7]}. Le résultat est-il correctement trié ?
  \item Expliquer précisément pourquoi replacer \lstinline{indice_min = i} à l'intérieur de la boucle \lstinline{for j} empêche l'algorithme de retenir le minimum \textbf{global} de la partie restante.
  \item Corriger le code en plaçant l'initialisation au bon endroit.
\end{enumerate}
\end{exercice}

% BEGIN-VERIFY
% def tri_selection_bugue(tableau):
%     tableau = tableau[:]
%     n = len(tableau)
%     for i in range(n - 1):
%         for j in range(i + 1, n):
%             indice_min = i
%             if tableau[j] < tableau[indice_min]:
%                 indice_min = j
%         tableau[i], tableau[indice_min] = tableau[indice_min], tableau[i]
%     return tableau
% assert tri_selection_bugue([9, 1, 5, 3, 7]) == [7, 1, 5, 3, 9]
% def tri_selection(tableau):
%     tableau = tableau[:]
%     n = len(tableau)
%     for i in range(n - 1):
%         indice_min = i
%         for j in range(i + 1, n):
%             if tableau[j] < tableau[indice_min]:
%                 indice_min = j
%         tableau[i], tableau[indice_min] = tableau[indice_min], tableau[i]
%     return tableau
% assert tri_selection([9, 1, 5, 3, 7]) == [1, 3, 5, 7, 9]
% END-VERIFY

\begin{corrige}{1NSI-AGT-RE-C5-EX1}
\textbf{1.} \lstinline{tri_selection_bugue([9, 1, 5, 3, 7])} renvoie
\lstinline{[7, 1, 5, 3, 9]} : le tableau n'est \textbf{pas} correctement trié (la version
correcte donnerait \lstinline{[1, 3, 5, 7, 9]}).

\textbf{2.} En réinitialisant \lstinline{indice_min = i} à \textbf{chaque} tour de la
boucle interne, la comparaison \lstinline{tableau[j] < tableau[indice_min]} ne compare
jamais \lstinline{tableau[j]} qu'à \lstinline{tableau[i]} (la valeur d'origine), jamais au
minimum \textbf{déjà trouvé} lors des tours précédents de la boucle interne. Seule la toute
\textbf{dernière} comparaison effectuée (avec \lstinline{j = n-1}) détermine la valeur
finale de \lstinline{indice_min} utilisée pour l'échange : le minimum global de la partie
restante n'est donc, en général, pas retrouvé.

\textbf{3.} Il faut initialiser \lstinline{indice_min = i} \textbf{avant} la boucle
\lstinline{for j} (donc à l'intérieur de la boucle \lstinline{for i}, mais avant la boucle
interne), pour que la comparaison se fasse contre le \textbf{meilleur candidat trouvé
jusque-là}, et non contre \lstinline{tableau[i]} à chaque tour.
\end{corrige}
