% META: {"id": "1NSI-ARCHOS-CO-001", "chapitre": "1NSI-ARCHITECTURE-OS", "type_objet": "corrige", "exercice_ref": "1NSI-ARCHOS-EX-001", "capacites": ["P-ARCH-01A"], "sources_inspiration": [], "status": "manual_review"}
\begin{corrige}{1NSI-ARCHOS-EX-001}
\textbf{1.} Le \textbf{processeur} (unité centrale) exécute les instructions. La
\textbf{mémoire} stocke à la fois les instructions et les données.

\textbf{2.} Oui : avec $8$ cœurs, l'ordinateur est \textbf{multiprocesseur} et peut exécuter
plusieurs instructions en parallèle, un cœur par instruction (au sens large : plusieurs
programmes ou plusieurs parties d'un même programme peuvent progresser simultanément).

\textbf{3.} Instructions et données sont stockées sous la \textbf{même forme} (des suites de
bits) dans le même espace mémoire, sans marqueur qui les distinguerait intrinsèquement.
C'est la \textbf{phase du cycle d'exécution} (le processeur lit-il cette adresse pour
charger l'instruction à exécuter, ou pour charger une donnée dans un registre ?) qui
détermine l'interprétation de ces bits, pas leur contenu lui-même.
\end{corrige}

% BEGIN-VERIFY
% # Q3 : le meme octet se lit de trois facons, selon la phase du cycle.
% octet = 0b01100001
% assert octet == 97 == 0x61
% assert chr(octet) == "a"
% jeu_instructions = {0b01100001: "CHARGE"}
% assert jeu_instructions[octet] == "CHARGE"
% assert len({chr(octet), str(octet), jeu_instructions[octet]}) == 3
%
% # Aucun bit du mot ne porte l'information « ceci est une instruction » : le
% # motif est identique dans les trois lectures.
% assert bin(octet) == "0b1100001"
% assert format(octet, "08b") == "01100001"
%
% # Q2 : huit coeurs, donc multiprocesseur.
% assert 8 > 1
% END-VERIFY
