# LOT 3 — Cours 1NSI-ARCHITECTURE-OS
3 fichiers de cours (architecture de von Neumann avec diagramme tikz
boites/fleches -- premiere figure d'architecture de la session --,
role des registres/UAL/sequenceur, mono/multiprocesseur ; simulateur
python complet d'execution d'instructions machine LOAD/ADD/STORE/HALT ;
fonctions d'un systeme d'exploitation, ligne de commande avec execution
de vraies commandes shell via subprocess dans un dossier temporaire,
droits/permissions Unix avec conversion en notation octale). Chaque
exemple calculable verifie par execution python reelle (y compris de
vraies commandes shell mkdir/touch/ls) avant redaction. 0 FAIL
verify_python.py, ruff clean, 0 erreur pdflatex des le premier essai
(dont le diagramme tikz).
