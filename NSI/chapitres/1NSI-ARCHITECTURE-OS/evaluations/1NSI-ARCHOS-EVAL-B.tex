% META: {"id": "1NSI-ARCHOS-EVAL-B", "chapitre": "1NSI-ARCHITECTURE-OS", "type_objet": "evaluation", "version": "B", "capacites": ["P-ARCH-01A", "P-ARCH-01B", "P-ARCH-03A", "P-ARCH-03B", "P-ARCH-03C"], "mode_creation": "ex_nihilo", "sources_inspiration": [], "duree_min": 45, "status": "verified"}
\section*{Évaluation B --- Architecture et systèmes d'exploitation}
\textit{Durée : 45 minutes. Ordinateur avec interpréteur Python autorisé.}

\baremeIndicatif{Ex. 1 : 8 pts (von Neumann, instructions) ; Ex. 2 : 8 pts (OS, permissions) ; Ex. 3 : 4 pts (ligne de commande)}

\bigskip

\textbf{Exercice 1} --- \textit{Von Neumann et instructions machine} \hfill (8 points)

\begin{enumerate}
  \item (3 pts) Nommer les quatre constituants principaux de l'architecture de von Neumann, et préciser lequel exécute les instructions.
  \item (3 pts) On ajoute au simulateur \lstinline{executer} du cours l'instruction \lstinline{("SUB", reg_dest, reg_src)} (soustraction : \lstinline{reg_dest <- reg_dest - reg_src}). Écrire le code de cette instruction.
  \item (2 pts) Dérouler l'exécution de ce programme et donner l'état final de \lstinline{memoire} :
\begin{python}
memoire = [20, 8, 0]
programme = [
    ("LOAD", "R0", 0),
    ("LOAD", "R1", 1),
    ("SUB", "R0", "R1"),
    ("STORE", "R0", 2),
    ("HALT",),
]
\end{python}
\end{enumerate}

% BEGIN-VERIFY
% def executer(programme, memoire):
%     registres = {}
%     for instruction in programme:
%         op = instruction[0]
%         if op == "LOAD":
%             _, reg, adresse = instruction
%             registres[reg] = memoire[adresse]
%         elif op == "SUB":
%             _, reg_dest, reg_src = instruction
%             registres[reg_dest] = registres[reg_dest] - registres[reg_src]
%         elif op == "STORE":
%             _, reg, adresse = instruction
%             memoire[adresse] = registres[reg]
%         elif op == "HALT":
%             break
%     return memoire, registres
% memoire = [20, 8, 0]
% programme = [
%     ("LOAD", "R0", 0),
%     ("LOAD", "R1", 1),
%     ("SUB", "R0", "R1"),
%     ("STORE", "R0", 2),
%     ("HALT",),
% ]
% memoire_finale, registres = executer(programme, memoire)
% assert memoire_finale == [20, 8, 12]
% END-VERIFY

\bigskip

\textbf{Exercice 2} --- \textit{Système d'exploitation et permissions} \hfill (8 points)

\begin{enumerate}
  \item (3 pts) Citer deux fonctions assurées par un système d'exploitation.
  \item (3 pts) Un fichier a les permissions \lstinline{"rw-rw-r--"}. En utilisant \lstinline{droit_accorde} du cours, déterminer si le propriétaire peut y écrire, si le groupe peut y écrire, et si les autres peuvent y écrire.
  \item (2 pts) Convertir ces permissions en notation octale avec \lstinline{permissions_vers_octal}.
\end{enumerate}

% BEGIN-VERIFY
% def droit_accorde(permissions, categorie, action):
%     index = {"proprietaire": 0, "groupe": 3, "autres": 6}
%     decalage = {"lecture": 0, "ecriture": 1, "execution": 2}
%     position = index[categorie] + decalage[action]
%     return permissions[position] != "-"
% def permissions_vers_octal(permissions):
%     groupes = [permissions[0:3], permissions[3:6], permissions[6:9]]
%     valeurs = []
%     for g in groupes:
%         v = (4 if g[0] == "r" else 0) + (2 if g[1] == "w" else 0) + (1 if g[2] == "x" else 0)
%         valeurs.append(v)
%     return "".join(str(v) for v in valeurs)
% perm = "rw-rw-r--"
% assert droit_accorde(perm, "proprietaire", "ecriture") is True
% assert droit_accorde(perm, "groupe", "ecriture") is True
% assert droit_accorde(perm, "autres", "ecriture") is False
% assert permissions_vers_octal(perm) == "664"
% END-VERIFY

\bigskip

\textbf{Exercice 3} --- \textit{Ligne de commande} \hfill (4 points)

Un utilisateur se trouve dans son dossier personnel, qui contient un dossier \lstinline{tp}
et un fichier \lstinline{notes.txt}.

\begin{enumerate}
  \item (2 pts) Écrire, dans l'ordre, les commandes qui créent un dossier \lstinline{tp/rendu},
  s'y déplacent, y créent un fichier vide \lstinline{reponses.txt}, puis listent le contenu
  du dossier courant.
  \item (1 pt) Quelle commande affiche le contenu de \lstinline{notes.txt} sans l'ouvrir dans
  un éditeur ? Depuis \lstinline{tp/rendu}, quel chemin faut-il lui donner ?
  \item (1 pt) Expliquer en une phrase pourquoi \lstinline{rm} est la commande la plus
  dangereuse de cette liste.
\end{enumerate}

% BEGIN-VERIFY
% import os, subprocess, tempfile
% # Les commandes attendues sont executees dans un dossier temporaire : la
% # sequence doit reellement produire l'arborescence decrite.
% with tempfile.TemporaryDirectory() as racine:
%     os.makedirs(os.path.join(racine, "tp"))
%     open(os.path.join(racine, "notes.txt"), "w").close()
%     subprocess.run(["mkdir", "tp/rendu"], cwd=racine, check=True)
%     rendu = os.path.join(racine, "tp", "rendu")
%     assert os.path.isdir(rendu)
%     subprocess.run(["touch", "reponses.txt"], cwd=rendu, check=True)
%     listing = subprocess.run(["ls"], cwd=rendu, capture_output=True, text=True, check=True)
%     assert listing.stdout.split() == ["reponses.txt"]
%     # Depuis tp/rendu, notes.txt se trouve deux niveaux au-dessus.
%     lecture = subprocess.run(["cat", "../../notes.txt"], cwd=rendu,
%                              capture_output=True, text=True, check=True)
%     assert lecture.returncode == 0
%     assert os.path.relpath(os.path.join(racine, "notes.txt"), rendu) == "../../notes.txt"
%     # rm supprime sans corbeille : le fichier n'existe plus apres coup.
%     subprocess.run(["rm", "reponses.txt"], cwd=rendu, check=True)
%     assert not os.path.exists(os.path.join(rendu, "reponses.txt"))
% END-VERIFY
