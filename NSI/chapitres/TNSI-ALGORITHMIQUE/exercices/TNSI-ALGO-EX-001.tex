% META: {"id": "TNSI-ALGO-EX-001", "chapitre": "TNSI-ALGORITHMIQUE", "type_objet": "exercice", "capacites": ["T-ALGO-01A", "T-ALGO-01C"], "capacites_codes": ["C1", "C3"], "methodes": [], "parcours": 1, "competences": ["programmer"], "duree_min": 15, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TNSI-ALGORITHMIQUE/exercices/TNSI-ALGO-EX-001.tex", "corrige_tex": "chapitres/TNSI-ALGORITHMIQUE/corriges/TNSI-ALGO-CO-001.tex", "status": "generated"}
% BEGIN-VERIFY
% class Noeud:
%     def __init__(self, valeur, gauche=None, droite=None):
%         self.valeur = valeur
%         self.gauche = gauche
%         self.droite = droite
% def compter_feuilles(arbre):
%     if arbre is None:
%         return 0
%     if arbre.gauche is None and arbre.droite is None:
%         return 1
%     return compter_feuilles(arbre.gauche) + compter_feuilles(arbre.droite)
% arbre = Noeud(1, Noeud(2, Noeud(4), Noeud(5)), Noeud(3))
% assert compter_feuilles(arbre) == 3
% assert compter_feuilles(Noeud(1)) == 1
% assert compter_feuilles(None) == 0
% END-VERIFY
\begin{exercice}{TNSI-ALGO-EX-001}{1}{15}
Écrire une fonction récursive \lstinline{compter_feuilles(arbre)} qui renvoie le nombre de \textbf{feuilles} (nœuds sans enfant) d'un arbre binaire représenté avec la classe \lstinline{Noeud} du cours. Tester sur l'arbre \lstinline{Noeud(1, Noeud(2, Noeud(4), Noeud(5)), Noeud(3))}, qui possède 3 feuilles.
\end{exercice}
