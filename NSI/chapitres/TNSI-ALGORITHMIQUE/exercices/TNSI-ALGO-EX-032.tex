% META: {"id": "TNSI-ALGO-EX-032", "chapitre": "TNSI-ALGORITHMIQUE", "type_objet": "exercice", "capacites": ["TNSI-ALGORITHMIQUE-C8"], "capacites_codes": ["C8"], "methodes": ["M1"], "parcours": 2, "competences": ["programmer"], "duree_min": 8, "mode_creation": "ex_nihilo", "sources_inspiration": [], "fichier_tex": "chapitres/TNSI-ALGORITHMIQUE/exercices/TNSI-ALGO-EX-032.tex", "corrige_tex": "chapitres/TNSI-ALGORITHMIQUE/corriges/TNSI-ALGO-CO-032.tex", "status": "approved"}
% BEGIN-VERIFY
% class Noeud:
%     def __init__(self, valeur, gauche=None, droite=None):
%         self.valeur = valeur
%         self.gauche = gauche
%         self.droite = droite
% def inserer(arbre, cle):
%     if arbre is None:
%         return Noeud(cle)
%     if cle < arbre.valeur:
%         arbre.gauche = inserer(arbre.gauche, cle)
%     elif cle > arbre.valeur:
%         arbre.droite = inserer(arbre.droite, cle)
%     return arbre
% def rechercher(arbre, cle):
%     if arbre is None:
%         return False
%     if cle == arbre.valeur:
%         return True
%     if cle < arbre.valeur:
%         return rechercher(arbre.gauche, cle)
%     return rechercher(arbre.droite, cle)
% abr = None
% for v in (7, 3, 9, 1, 5):
%     abr = inserer(abr, v)
% assert rechercher(abr, 5) is True
% assert rechercher(abr, 4) is False
% END-VERIFY
\begin{exercice}{TNSI-ALGO-EX-002}{1}{15}
En utilisant les fonctions \lstinline{inserer} et \lstinline{rechercher} du cours, construire un arbre binaire de recherche à partir des valeurs $7, 3, 9, 1, 5$ insérées dans cet ordre. Vérifier ensuite que $5$ est présent dans l'arbre et que $4$ n'y est pas.
\end{exercice}
