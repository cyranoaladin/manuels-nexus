# LOT 3 — Cours 1NSI-TABLES
4 fichiers de cours (import CSV avec csv.DictReader et conversion de
types, recherche par critere booleen avec comprehension de liste,
detection de doublons et test de coherence, tri avec sorted/key/reverse
et distinction sorted vs .sort(), fusion de deux tables par colonne
commune avec mise en evidence du domaine de valeurs). Un fil rouge
(table "adherents" d'un club sportif) reutilise dans les 4 fichiers.
Deux occurrences de la variable ambigue `l` (E741 ruff) detectees et
corrigees en `ligne`/`chaine` avant validation finale. Chaque exemple
et propriete verifie par execution python reelle avant redaction. 0
FAIL verify_python.py, ruff clean, 0 erreur pdflatex.
