# LOT 3 — Cours TNSI-ARCHITECTURES-MATERIELLES-SY
4 fichiers de cours (systeme sur puce ; processus : creation, tourniquet,
concurrence vs parallelisme, interblocage detecte par cycle dans un graphe
d'attente ; routage : RIP=BFS vs OSPF=Dijkstra-lite, divergence illustree
sur un cas ou les deux criteres different reellement (verifie par calcul
independant plutot que suppose) ; chiffrement XOR symetrique reversible
verifie par execution, HTTPS combinant asymetrique (echange de cle) et
symetrique (donnees), conformement a la limite du programme qui exclut la
negociation SSL detaillee.

10_C01 (systeme sur puce) est purement conceptuel (aucun bloc VERIFY/
listing) -> verdict manual_review attendu, coherent avec le fait que R2 ne
s'applique qu'au code montre a l'eleve. 0 FAIL sur les 3 autres fichiers.
