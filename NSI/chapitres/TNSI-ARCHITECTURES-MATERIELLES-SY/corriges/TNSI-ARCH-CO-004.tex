% META: {"id":"TNSI-ARCH-CO-004","chapitre":"TNSI-ARCHITECTURES-MATERIELLES-SY","type_objet":"corrige","exercice_ref":"TNSI-ARCH-EX-004","capacites_codes":["C6"],"sources_inspiration":[],"status":"approved","origin":"generated","status_history":["generated","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% def chiffrer_xor(message, cle):
%     return bytes(o ^ cle[i % len(cle)] for i, o in enumerate(message))
% message = b"BAC2027"
% cle = b"NSI"
% chiffre = chiffrer_xor(message, cle)
% dechiffre = chiffrer_xor(chiffre, cle)
% assert dechiffre == message
% assert chiffre != message
% END-VERIFY
\begin{corrige}{TNSI-ARCH-EX-004}
\begin{python}
message = b"BAC2027"
cle = b"NSI"
chiffre = chiffrer_xor(message, cle)
dechiffre = chiffrer_xor(chiffre, cle)
print(dechiffre == message)  # True
\end{python}
Ce chiffrement est dit \textbf{symétrique} car la \textbf{même clé} \texttt{cle} sert à la fois à chiffrer et à déchiffrer : il n'y a qu'un seul secret partagé, contrairement au chiffrement asymétrique qui utilise deux clés différentes (publique et privée).
\end{corrige}
