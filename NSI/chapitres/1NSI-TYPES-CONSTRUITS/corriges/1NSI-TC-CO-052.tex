% META: {"id":"1NSI-TC-CO-052","chapitre":"1NSI-TYPES-CONSTRUITS","type_objet":"corrige","capacites":["1NSI-TYPES-CONSTRUITS-C5"],"mode_creation":"ex_nihilo","sources_inspiration":[],"status":"approved","exercice_ref":"1NSI-TC-EX-052","origin":"needs_review","status_history":["needs_review","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% equipe = {"nom": "Phoenix", "score": 100}
% copie = equipe
% copie["score"] = 200
% assert equipe["score"] == 200
% assert equipe is copie
% # Version avec vraie copie
% equipe2 = {"nom": "Phoenix", "score": 100}
% copie2 = dict(equipe2)
% copie2["score"] = 200
% assert equipe2["score"] == 100
% assert copie2["score"] == 200
% assert equipe2 is not copie2
% END-VERIFY
\begin{corrige}{1NSI-TC-EX-052}
\begin{enumerate}
  \item Le programme affiche :
\begin{console}
200
True
\end{console}

  \item Oui, les dictionnaires sont \textbf{mutables}. On peut ajouter, modifier ou supprimer des paires clé-valeur après la création. Ici, \lstinline{copie["score"] = 200} modifie le dictionnaire en place. Comme \lstinline{copie = equipe} crée un alias (même objet), la modification est visible via \lstinline{equipe} aussi.

  \item Pour obtenir une copie indépendante, on utilise \lstinline{dict()} :
\begin{python}
equipe = {"nom": "Phoenix", "score": 100}
copie = dict(equipe)
copie["score"] = 200
print(equipe["score"])  # 100
print(copie["score"])   # 200
\end{python}
Après cette modification, \lstinline{equipe["score"]} vaut toujours \lstinline{100} car \lstinline{copie} est un objet distinct.
\end{enumerate}
\end{corrige}
