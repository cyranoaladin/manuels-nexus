% META: {"id":"1NSI-TC-CO-011","chapitre":"1NSI-TYPES-CONSTRUITS","type_objet":"corrige","capacites":["1NSI-TYPES-CONSTRUITS-C1"],"mode_creation":"ex_nihilo","sources_inspiration":[],"status":"approved","exercice_ref":"1NSI-TC-EX-011","origin":"needs_review","status_history":["needs_review","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% def classer_matchs(matchs):
%     victoires = []
%     defaites = []
%     nuls = []
%     for equipe, score_pour, score_contre in matchs:
%         if score_pour > score_contre:
%             victoires.append(equipe)
%         elif score_pour < score_contre:
%             defaites.append(equipe)
%         else:
%             nuls.append(equipe)
%     return (victoires, defaites, nuls)
% matchs = [("A", 3, 1), ("B", 0, 2), ("C", 1, 1)]
% v, d, n = classer_matchs(matchs)
% assert v == ["A"]
% assert d == ["B"]
% assert n == ["C"]
% matchs2 = [("X", 2, 2), ("Y", 2, 2)]
% v2, d2, n2 = classer_matchs(matchs2)
% assert v2 == [] and d2 == [] and n2 == ["X", "Y"]
% END-VERIFY
\begin{corrige}{1NSI-TC-EX-011}
On crée trois listes accumulatrices et on classe chaque équipe selon la comparaison des scores :

\begin{python}
def classer_matchs(matchs):
    victoires = []
    defaites = []
    nuls = []
    for equipe, score_pour, score_contre in matchs:
        if score_pour > score_contre:
            victoires.append(equipe)
        elif score_pour < score_contre:
            defaites.append(equipe)
        else:
            nuls.append(equipe)
    return (victoires, defaites, nuls)
\end{python}

Le déballage \lstinline{for equipe, score_pour, score_contre in matchs} extrait directement les trois composantes de chaque tuple. La structure conditionnelle \lstinline{if / elif / else} couvre les trois cas possibles (victoire, défaite, match nul). La fonction renvoie un tuple de trois listes.

Vérification : \lstinline{classer_matchs([("A", 3, 1), ("B", 0, 2), ("C", 1, 1)])} renvoie bien \lstinline{(["A"], ["B"], ["C"])}.
\end{corrige}
