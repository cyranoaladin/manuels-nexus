% META: {"id":"1NSI-TC-CO-019","chapitre":"1NSI-TYPES-CONSTRUITS","type_objet":"corrige","capacites":["1NSI-TYPES-CONSTRUITS-C2"],"mode_creation":"ex_nihilo","sources_inspiration":[],"status":"approved","exercice_ref":"1NSI-TC-EX-019","origin":"needs_review","status_history":["needs_review","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% def mystere(tab):
%     resultat = []
%     for i in range(len(tab)):
%         if tab[i] not in resultat:
%             resultat.append(tab[i])
%     return resultat
% assert mystere([3, 1, 3, 2, 1, 3]) == [3, 1, 2]
% assert mystere([]) == []
% assert mystere([1, 1, 1]) == [1]
% # Version sans indices
% def mystere_v2(tab):
%     resultat = []
%     for element in tab:
%         if element not in resultat:
%             resultat.append(element)
%     return resultat
% assert mystere_v2([3, 1, 3, 2, 1, 3]) == [3, 1, 2]
% END-VERIFY
\begin{corrige}{1NSI-TC-EX-019}
\begin{enumerate}
  \item Le programme affiche \lstinline{[3, 1, 2]}.

  Détail du parcours :
  \begin{itemize}
    \item $i=0$ : \lstinline{3} n'est pas dans \lstinline{resultat}, on l'ajoute $\to$ \lstinline{[3]}.
    \item $i=1$ : \lstinline{1} n'est pas dans \lstinline{resultat}, on l'ajoute $\to$ \lstinline{[3, 1]}.
    \item $i=2$ : \lstinline{3} est déjà dans \lstinline{resultat}, on ne l'ajoute pas.
    \item $i=3$ : \lstinline{2} n'est pas dans \lstinline{resultat}, on l'ajoute $\to$ \lstinline{[3, 1, 2]}.
    \item $i=4$ : \lstinline{1} est déjà dans \lstinline{resultat}, on ne l'ajoute pas.
    \item $i=5$ : \lstinline{3} est déjà dans \lstinline{resultat}, on ne l'ajoute pas.
  \end{itemize}

  \item La fonction supprime les doublons d'un tableau en conservant l'ordre de première apparition de chaque élément.

  \item Version parcourant directement les éléments :
\begin{python}
def mystere(tab):
    resultat = []
    for element in tab:
        if element not in resultat:
            resultat.append(element)
    return resultat
\end{python}
\end{enumerate}

Vérification : les résultats ont été confirmés par exécution.
\end{corrige}
