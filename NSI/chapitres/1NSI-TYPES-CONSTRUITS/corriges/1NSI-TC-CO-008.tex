% META: {"id":"1NSI-TC-CO-008","chapitre":"1NSI-TYPES-CONSTRUITS","type_objet":"corrige","capacites":["1NSI-TYPES-CONSTRUITS-C1"],"mode_creation":"ex_nihilo","sources_inspiration":[],"status":"approved","exercice_ref":"1NSI-TC-EX-008","origin":"needs_review","status_history":["needs_review","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% def station_la_plus_chaude(stations):
%     meilleure = stations[0]
%     for station in stations:
%         if station[1] > meilleure[1]:
%             meilleure = station
%     return meilleure[0]
% assert station_la_plus_chaude([("Paris", 32), ("Lyon", 35), ("Lille", 28)]) == "Lyon"
% assert station_la_plus_chaude([("Tunis", 40)]) == "Tunis"
% assert station_la_plus_chaude([("A", 20), ("B", 20)]) == "A"
% END-VERIFY
\begin{corrige}{1NSI-TC-EX-008}
On parcourt la liste en retenant le tuple ayant la plus grande température :

\begin{python}
def station_la_plus_chaude(stations):
    # On initialise avec la premiere station
    meilleure = stations[0]
    for station in stations:
        # On compare les temperatures (indice 1)
        if station[1] > meilleure[1]:
            meilleure = station
    # On renvoie uniquement le nom (indice 0)
    return meilleure[0]
\end{python}

La variable \lstinline{meilleure} stocke le tuple complet de la station candidate. À chaque itération, si la température courante dépasse le maximum connu, on met à jour \lstinline{meilleure}. En fin de boucle, \lstinline{meilleure[0]} donne le nom recherché.

Vérification : \lstinline{station_la_plus_chaude([("Paris", 32), ("Lyon", 35), ("Lille", 28)])} renvoie bien \lstinline{"Lyon"}.
\end{corrige}
