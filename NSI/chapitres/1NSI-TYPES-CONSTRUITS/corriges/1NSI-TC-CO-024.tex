% META: {"id":"1NSI-TC-CO-024","chapitre":"1NSI-TYPES-CONSTRUITS","type_objet":"corrige","capacites":["1NSI-TYPES-CONSTRUITS-C2"],"mode_creation":"ex_nihilo","sources_inspiration":[],"status":"approved","exercice_ref":"1NSI-TC-EX-024","origin":"needs_review","status_history":["needs_review","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% def moyennes_glissantes(releves, k):
%     if len(releves) < k:
%         return []
%     resultat = []
%     for i in range(len(releves) - k + 1):
%         somme = 0
%         for j in range(k):
%             somme = somme + releves[i + j]
%         resultat.append(somme / k)
%     return resultat
% assert moyennes_glissantes([10, 20, 30, 40, 50], 3) == [20.0, 30.0, 40.0]
% assert moyennes_glissantes([5, 5, 5], 2) == [5.0, 5.0]
% assert moyennes_glissantes([1], 3) == []
% assert moyennes_glissantes([10, 20], 1) == [10.0, 20.0]
% END-VERIFY
\begin{corrige}{1NSI-TC-EX-024}
\begin{python}
def moyennes_glissantes(releves, k):
    if len(releves) < k:
        return []
    resultat = []
    for i in range(len(releves) - k + 1):
        # Calcul de la moyenne des k elements a partir de i
        somme = 0
        for j in range(k):
            somme = somme + releves[i + j]
        resultat.append(somme / k)
    return resultat
\end{python}

\textbf{Principe :} pour chaque position $i$ de 0 à \lstinline{len(releves) - k}, on calcule la moyenne des $k$ éléments consécutifs \lstinline{releves[i]}, \lstinline{releves[i+1]}, \dots, \lstinline{releves[i+k-1]}. Le tableau résultat contient \lstinline{len(releves) - k + 1} valeurs.

\textbf{Exemple détaillé} avec \lstinline{releves = [10, 20, 30, 40, 50]} et $k = 3$ :
\begin{itemize}
  \item $i=0$ : moyenne de $10, 20, 30$ $= 20.0$
  \item $i=1$ : moyenne de $20, 30, 40$ $= 30.0$
  \item $i=2$ : moyenne de $30, 40, 50$ $= 40.0$
\end{itemize}

{\raggedright
Vérification : \lstinline{moyennes_glissantes([10, 20, 30, 40, 50], 3)} renvoie \lstinline{[20.0, 30.0, 40.0]}. Résultat confirmé par exécution.
\par}
\end{corrige}
