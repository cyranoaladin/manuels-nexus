% META: {"id":"1NSI-TC-CO-050","chapitre":"1NSI-TYPES-CONSTRUITS","type_objet":"corrige","capacites":["1NSI-TYPES-CONSTRUITS-C5"],"mode_creation":"ex_nihilo","sources_inspiration":[],"status":"approved","exercice_ref":"1NSI-TC-EX-050","origin":"needs_review","status_history":["needs_review","audited","accepted","approved"],"release_acceptance":"RELEASE_OWNER_BATCH_ACCEPTANCE","acceptance_closure_digest":"sha256:9b3ccf9a81c5520fbb7e03b7b2d3e7bf2057a02834b6d908bf3be5f49f3b553f","acceptance_receipt_digest":"sha256:4fad86f0282e0fa4e0419cca1ad6379ae7af5a280c04a4a90880f90a1c044242"}
% BEGIN-VERIFY
% grille = [[0, 0]] * 3
% grille[0][0] = 1
% assert grille == [[1, 0], [1, 0], [1, 0]]
% assert grille[0] is grille[1]
% assert grille[1] is grille[2]
% # Construction correcte
% grille2 = [[0, 0] for _ in range(3)]
% grille2[0][0] = 1
% assert grille2 == [[1, 0], [0, 0], [0, 0]]
% assert grille2[0] is not grille2[1]
% END-VERIFY
\begin{corrige}{1NSI-TC-EX-050}
\begin{enumerate}
  \item Le programme affiche :
\begin{console}
[1, 0]
[1, 0]
[1, 0]
\end{console}
Ce n'est pas le résultat attendu : on voulait modifier uniquement la première ligne, mais les trois lignes sont identiques.

  \item L'opérateur \lstinline{*} appliqué à une liste ne crée pas trois sous-listes indépendantes : il duplique trois fois \textbf{la même référence} vers l'unique sous-liste \lstinline{[0, 0]}. Les trois éléments de \lstinline{grille} sont des alias du même objet. Modifier \lstinline{grille[0][0]} modifie cet objet unique, ce qui se répercute sur les trois positions.

  \item On construit la grille avec une liste en compréhension, qui crée une nouvelle sous-liste à chaque itération :
\begin{python}
grille = [[0, 0] for _ in range(3)]
\end{python}
Ainsi, chaque ligne est un objet distinct et modifier \lstinline{grille[0][0]} n'affecte que la première ligne.
\end{enumerate}
\end{corrige}
