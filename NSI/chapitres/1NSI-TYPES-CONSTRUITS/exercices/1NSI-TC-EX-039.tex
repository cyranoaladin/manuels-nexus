% META: {"id": "1NSI-TC-EX-039", "chapitre": "1NSI-TYPES-CONSTRUITS", "type_objet": "exercice", "capacites": ["1NSI-TYPES-CONSTRUITS-C4"], "parcours": 2, "competences": ["programmer"], "duree_min": 10, "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "needs_review"}
% BEGIN-VERIFY
% def compter_occurrences(tab):
%     compteur = {}
%     for element in tab:
%         if element in compteur:
%             compteur[element] = compteur[element] + 1
%         else:
%             compteur[element] = 1
%     return compteur
% assert compter_occurrences(["a", "b", "a", "c", "b", "a"]) == {"a": 3, "b": 2, "c": 1}
% assert compter_occurrences([]) == {}
% assert compter_occurrences([1, 1, 1]) == {1: 3}
% END-VERIFY
\begin{exercice}{1NSI-TC-EX-039}{2}{10}
Écrire une fonction \lstinline{compter_occurrences(tab)} qui prend un tableau et
renvoie un dictionnaire associant à chaque élément son nombre d'apparitions.

\textbf{Exemple :}
\begin{console}
>>> compter_occurrences(["a", "b", "a", "c", "b", "a"])
{'a': 3, 'b': 2, 'c': 1}
\end{console}
\end{exercice}
