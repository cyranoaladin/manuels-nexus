% META: {"id": "1NSI-TC-EX-019", "chapitre": "1NSI-TYPES-CONSTRUITS", "type_objet": "exercice", "capacites": ["1NSI-TYPES-CONSTRUITS-C4"], "parcours": 1, "competences": ["analyser", "programmer"], "duree_min": 8, "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "approved", "capacites_codes": ["C4"], "methodes": ["M1"], "fichier_tex": "chapitres/1NSI-TYPES-CONSTRUITS/exercices/1NSI-TC-EX-019.tex", "corrige_tex": "chapitres/1NSI-TYPES-CONSTRUITS/corriges/1NSI-TC-CO-019.tex"}
% BEGIN-TRACE
% def mystere(tab):
%     resultat = []
%     for i in range(len(tab)):
%         if tab[i] not in resultat:
%             resultat.append(tab[i])
%     return resultat
% print(mystere([3, 1, 3, 2, 1, 3]))
% EXPECTED
% [3, 1, 2]
% END-TRACE
\begin{exercice}{1NSI-TC-EX-019}{2}{10}
On considère la fonction suivante :
\begin{python}
def mystere(tab):
    resultat = []
    for i in range(len(tab)):
        if tab[i] not in resultat:
            resultat.append(tab[i])
    return resultat
print(mystere([3, 1, 3, 2, 1, 3]))
\end{python}
\begin{enumerate}
  \item Donner, sans exécuter, l'affichage produit.
  \item Décrire en une phrase le rôle de cette fonction.
  \item Réécrire le corps de la fonction en parcourant directement les éléments
        (sans utiliser les indices).
\end{enumerate}
\end{exercice}
