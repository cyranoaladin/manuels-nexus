% META: {"id": "1NSI-TC-EX-009", "chapitre": "1NSI-TYPES-CONSTRUITS", "type_objet": "exercice", "capacites": ["1NSI-TYPES-CONSTRUITS-C1"], "parcours": 2, "competences": ["analyser", "programmer"], "duree_min": 10, "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "needs_review"}
% BEGIN-VERIFY
% def filtrer_par_rang(joueurs, rang_min):
%     resultat = []
%     for nom, elo, rang in joueurs:
%         if elo >= rang_min:
%             resultat.append((nom, elo, rang))
%     return resultat
% joueurs = [("Alice", 2400, "diamant"), ("Bob", 1800, "or"), ("Clara", 2100, "platine")]
% assert filtrer_par_rang(joueurs, 2000) == [("Alice", 2400, "diamant"), ("Clara", 2100, "platine")]
% assert filtrer_par_rang(joueurs, 3000) == []
% END-VERIFY
\begin{exercice}{1NSI-TC-EX-009}{2}{10}
On représente chaque joueur d'e-sport par un tuple \lstinline{(nom, elo, rang)}.

\begin{enumerate}
  \item Écrire une fonction \lstinline{filtrer_par_rang(joueurs, rang_min)} qui prend
        une liste de tuples et un entier \lstinline{rang_min}, et renvoie la liste des
        tuples dont le score Elo est supérieur ou égal à \lstinline{rang_min}.
  \item Tester ta fonction avec la liste suivante :
\begin{python}
joueurs = [("Alice", 2400, "diamant"),
           ("Bob", 1800, "or"),
           ("Clara", 2100, "platine")]
\end{python}
\end{enumerate}
\end{exercice}
