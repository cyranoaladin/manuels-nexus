% META: {"id": "1NSI-TC-EX-015", "chapitre": "1NSI-TYPES-CONSTRUITS", "type_objet": "exercice", "capacites": ["1NSI-TYPES-CONSTRUITS-C2"], "parcours": 1, "competences": ["analyser"], "duree_min": 5, "mode_creation": "ex_nihilo", "sources_inspiration": [], "status": "needs_review"}
% BEGIN-TRACE
% scores = [12, 8, 15, 3, 20]
% resultat = []
% for s in scores:
%     if s >= 10:
%         resultat.append(s)
% print(resultat)
% print(len(resultat))
% EXPECTED
% [12, 15, 20]
% 3
% END-TRACE
\begin{exercice}{1NSI-TC-EX-015}{1}{5}
On considère le programme suivant :
\begin{python}
scores = [12, 8, 15, 3, 20]
resultat = []
for s in scores:
    if s >= 10:
        resultat.append(s)
print(resultat)
print(len(resultat))
\end{python}
\begin{enumerate}
  \item Donner, sans exécuter le programme, les deux affichages produits.
  \item Quel est le rôle de ce programme ?
\end{enumerate}
\end{exercice}
