# LOT 3 — Cours TNSI-LANGAGES-ET-PROGRAMMATION
5 fichiers de cours (programme comme donnee + calculabilite + probleme de
l'arret avec argument intuitif de contradiction ; recursivite ecrite et
analysee via arbre d'appels, avec comptage reel du nombre d'appels de
fib_naif(10) = 177 verifie par execution instrumentee plutot que suppose ;
API/bibliotheques/modules avec statistics et docstrings ; les trois
paradigmes sur un meme exemple filé (somme des carres des nombres pairs) ;
5 causes de bugs -- flottants, effets de bord par aliasing, inegalites
strictes/larges, nommage masquant une fonction native -- chacune
demontree par execution reelle plutot que decrite abstraitement.

Verification prealable systematique du pattern \lstinline{...{...}...}
(bug trouve dans les chapitres precedents) : aucune occurrence dans ce
chapitre. 0 FAIL verify_python.py, 0 erreur dans le .log pdflatex.
